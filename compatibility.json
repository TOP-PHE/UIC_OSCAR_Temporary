{
  "$schema": "./compatibility.schema.json",
  "description": "Tested-together combinations of the Oscar_Server and Bruno_Collection. The server reads this at startup and surfaces a warning in /v1/health if the running combination is not listed.",
  "current_release": "2026.217",
  "releases": [
    {
      "release": "2026.217",
      "date": "2026-09-03",
      "server": "1.11.189",
      "collection": "OTST_V2.0.99",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "docs+news (Heuguet request, 2026-09-03: check that documentation and the OSCAR news include the 2026.215/2026.216 changes -- they did not). Welcome-page news (Oscar_Server/public/news/index.json): two new entries dated September 3, 2026 -- (1) Improvement: optional OSDM endpoints a provider has not implemented no longer fail the run (System-Info catalogs, GET Passenger, GET Refund Offer, GET Exchange Offer; 501 / OPERATION_NOT_PERMITTED / bare 404 -> INFO, bare 403/405/500 -> WARNING, 401 still fails) and the Report Builder Vendor Capability Matrix shows them as NOT_IMPLEMENTED (#488/#489); (2) Bug Fix: confirmedPrice, not provisionalPrice, after a confirmed refund (REFUNDED) or completed exchange (EXCHANGED), with the before/after INFO line and the open OTST point (#496).",
        "Documentation/Test_Coverage/OSCAR_Test_Coverage_Map.md brought in line with both changes: s1.2 System-Information status classification (was still described as version-gating 404s only), the shared not-implemented classifier on 04. GET Passenger / 11. GET Refund Offer / 12. GET Exchange Offer, the stage-scoped price member on every GET-Booking row of the refund and exchange tables (steps 12/14, 13/15/17), and the two failure-catalogue rows that still claimed 401/403/404/5xx all generate FAILING assertions and cited stale line numbers.",
        "Documentation/User_Guides/OSCAR - Tester User Guide.md s6 Reading the report + s8 Troubleshooting: what a passing not-implemented-by-this-provider (auto-detected) row means, INFO vs WARNING, reading the Vendor Capability Matrix, and the confirmedPrice expectation after refund/exchange (two new troubleshooting rows).",
        "CLAUDE.md s2: two new architecture bullets (the not-implemented policy with its RFC 9110 / osdm.io standards basis and the exact-request-name allowlist rationale; the lifecycle-scoped price member rule) and a s6 next-steps entry for the open OTST point on confirmedPrice net of refunds (#496). Server bumped to 1.11.189 for the Oscar_Server/public news change (same precedent as the #470/#473 news PRs); no collection change."
      ]
    },
    {
      "release": "2026.216",
      "date": "2026-09-03",
      "server": "1.11.188",
      "collection": "OTST_V2.0.99",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(collection): 14. GET Booking after Patch Refund asserted provisionalPrice at booking stage REFUNDED and FAILed two assertions on SBB INT (OTST review, Farruggia, relayed by Heuguet 2026-09-03, #496). After a confirmed refund the booking carries confirmedPrice (OSDM Booking schema: \"Sum of all prices of confirmed parts in the booking minus the sum of all confirmed refund amounts\"); provisionalPrice (\"Price of all unconfirmed pre-booked parts\") is legitimately absent. Root cause: bookings.js#postCreateBookingResponse keyed the lifecycle-scoped price member (#375) on /FULFILLED|CONFIRMED/ only. Extracted the mapping into an exported isPostConfirmationStage(): CONFIRMED / FULFILLED / REFUNDED / EXCHANGED -> confirmedPrice; PREBOOKED / ON_HOLD -> provisionalPrice. EXCHANGED (04-Exchange/15. GET Booking after Fulfillment) is corrected on the same principle; EXCHANGE_ONGOING (04-Exchange/13. GET Booking before Fulfillment) deliberately stays on provisionalPrice because the exchange operation creates new pre-booked parts that OSDM says provisionalPrice must include. No step file changed -- all nine GET-Booking call sites already pass the right stage.",
        "Adds an [INFO] line at REFUNDED/EXCHANGED stages showing confirmedPrice before vs after the after-sales operation (logged, not asserted): OSDM defines confirmedPrice net of confirmed refund amounts, but SBB INT still showed the pre-refund amount (2120 CHF, scale 2) after REFUNDED -- open point for OTST in #496, not failed until the expected provider behaviour is agreed.",
        "Tests: new Oscar_Server/tests/unit/bruno-bookings-stage-price.test.js covering the predicate for every status combination the collection actually passes (all nine GET-Booking call sites) plus edge cases (empty / bare string / lowercase / EXCHANGE_ONGOING not leak-matching EXCHANGED). Test-only Oscar_Server change; server bumped to 1.11.188 per the CLAUDE.md s4 rule."
      ]
    },
    {
      "release": "2026.215",
      "date": "2026-08-11",
      "server": "1.11.187",
      "collection": "OTST_V2.0.98",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(collection): optional/read-only GET endpoints no longer hard-FAIL when a provider legitimately doesn't implement them (OTST review request, Farruggia, 2026-07-29; widened after live field testing against SBB, Farruggia + Heuguet, 2026-08). Previously any non-200/non-known-deviation status FAILed outright.",
        "Round 1 (04. GET Passenger only): reused the Problem-body-aware classifier already proven safe on System-Info endpoints (#353, osdmCompliance.js classifySystemInfoStatus) -- auto-skip ONLY on HTTP 501, or a non-2xx carrying an OSDM Problem body whose code explicitly says OPERATION_NOT_PERMITTED/NOT_IMPLEMENTED/NOT_SUPPORTED/UNSUPPORTED.",
        "Round 2 (this note, widened after a real SBB run showed round 1 only partially worked -- SBB answers unimplemented endpoints with a bare 403/404/500 and NO confirming Problem body, so most of round 1's target cases still hard-failed): classifySystemInfoStatus now ALSO auto-skips on a bare 403/404/405/500 with no confirming body -- INFO when the signal is the clean OSDM one (501, 404, or a confirming Problem body), WARNING (accepted, but flags the ambiguity to the provider) otherwise. 401 stays a hard FAIL unconditionally -- a token problem, never an availability signal. This widening lives in ONE shared function, so it applies automatically everywhere that function is already called: all 10 files under 01-System Infos Requests/ (via handleSystemInfoStatus) and 04. GET Passenger (via #488's direct reuse) -- no changes needed to those files for this round. Newly wired into two more optional, read-only GETs that previously had ONLY a manual per-company Known Deviation escape hatch and no code-level 'not implemented' detection at all: 11. GET Refund Offer.yml and 12. GET Exchange Offer.yml (both: known-deviation check first, unchanged; then the classifier; hard FAIL only as a last resort). Deliberately NOT applied to any booking/refund/exchange MUTATION endpoint (POST/PATCH/DELETE) -- those keep their existing strict, bespoke 200(/204)-equality assertions; being lenient there would mask a real conformance failure on the endpoints OSCAR primarily exists to test, not just relax noise on auxiliary catalog/status lookups.",
        "Fixed a pre-existing #383-class double-registration bug while restructuring 11./12.'s status-handling block: both files previously registered the SAME 'expected 200, got X' failure as two separate test() calls on every non-200 (an unconditional first check plus a redundant second one), double-counting the assertion. Now registers once, matching the already-fixed pattern in 04. GET Passenger.",
        "Verified isEndpointApplicable('Passenger'/'Refund Offer'/'Exchange Offer') defaults to true (none are in the small version-gated-endpoint table, osdmVersion.js) before reuse, so the classifier's version-gating 404 branch (System-Info-specific) correctly never fires for any of these three.",
        "Considered and deliberately deferred: Report Builder's separate, cross-run 'Vendor Capability Matrix' (Oscar_Server/src/reports/structureResults.js classifyVendorCapability) already exists and is the natural home for a 'list of supported/not-supported endpoints' -- but it classifies PURELY from raw httpStatus + assertion pass/fail counts, with no knowledge of which endpoint a request was for. A same-shaped widening there (trust a passing assertion on 403/404/405/500 as NOT_IMPLEMENTED instead of ERROR) would also silently reclassify an unrelated NHF (negative-test) probe elsewhere in the collection that deliberately expects and correctly asserts one of these same codes -- a different, unreviewed risk from the one just accepted here. Flagged for the team rather than patched blind; the live per-run report already gives equivalent visibility today, since every auto-skip in this fix registers its own clearly-labelled passing assertion row.",
        "Verification: Bruno_Collection has no Jest harness (documented pre-existing gap) -- verified via js-yaml YAML-parse + before-request/after-response JS syntax-check on all three edited files, plus a full manual trace of every branch across all three (200 / known-deviation / auto-skip-via-501-or-404 (INFO) / auto-skip-via-bare-403-405-500 (WARNING) / 401-still-fails / any-other-status-still-fails).",
        "fix(docker): corrected the nanoid CVE-2026-67213/-67214 patch target from 3.3.17 to 3.3.18 -- Trivy kept flagging the same CVE against 3.3.17 on this PR's Container image scan; the actual fix landed one patch release later than assumed when #487/2026.214 first patched it. Also newly patched (same tarball-unpack technique, same reasoning): @faker-js/faker CVE-2026-73231 (HIGH, arbitrary code execution via attacker-controlled fake templates), bundled inside @usebruno/cli at node_modules/@faker-js/faker to power its {{$faker.*}} templating helper, bumped 9.9.0 -> 10.5.0. Confirmed Bruno_Collection never uses {{$faker...}} anywhere, so the major-version bump has no call surface in this project to break -- the package only needs to load cleanly. Both discovered via this PR's own required Container image scan (Trivy) check going red after the round-2 test fix above went green; Oscar_Server/package.json bumped to 1.11.187 accordingly (Dockerfile-only change otherwise).",
        "feat(reports): Vendor Capability Matrix (structureResults.js classifyVendorCapability) now also classifies a bare 403/405/500 as NOT_IMPLEMENTED, but ONLY on the exact, known optional/read-only capability-probe endpoints (the same ~13 request names already wired to osdmCompliance.js#classifySystemInfoStatus: all 10 files under 01-System Infos Requests/, 04. GET Passenger, 11. GET Refund Offer, 12. GET Exchange Offer) -- an exact-name allowlist, not a blanket status-code rule, so an unrelated NHF (negative-test) probe elsewhere that deliberately expects one of these same codes as its correct outcome is never reclassified. This directly resolves the earlier \"Considered and deliberately deferred\" note on this same entry: without this, the Vendor Capability Matrix (public/report-builder.html, the certifier-facing \"list of supported/not-supported endpoints\" -- OTST review request, Heuguet, 2026-09-03) still showed ERROR (or null, for 403/405) for the exact same responses the live per-run assertion had already accepted as a documented capability gap, e.g. a bare 500 on GET Refund Offer. classifyVendorCapability gained an optional 4th reqName parameter (backward compatible -- every existing call/test omitting it keeps the prior ERROR/null behavior); the 404 branch is unchanged and stays endpoint-independent, as it already was.",
        "Standards check (Heuguet, 2026-09-03; osdm.io/spec/errors-problems + RFC 9110 s15): OSDM defines no endpoint-level \"not implemented\" signal of its own -- it adopts the standard HTTP status codes (400/401/403/404/405/409/429/500/501/503) and leaves their meaning to RFC 9110, by which only 501 (functionality not supported), 404 (resource not found) and 405 (method not supported on this resource) genuinely mean \"not implemented/supported here\". 403 (authorization refused) and 500 (generic server error) are NOT that per the standard and stay in the list purely on the SBB field evidence, WARNING-tier. Three corrections applied from the review: (1) 406 DROPPED from the accepted list in both classifySystemInfoStatus and classifyVendorCapability -- not among OSDM's prescribed statuses, and its RFC meaning (content negotiation failed) most plausibly signals an unsupported OSDM version/media type, a different problem a tester should see rather than have auto-skipped; it was never in the field evidence, and a provider that genuinely answers 406 can still be baselined per company via Known Deviations (#430 path in handleSystemInfoStatus). (2) The provider-facing WARNING line now attributes the 404/501 expectation to RFC 9110 (\"the HTTP status semantics OSDM adopts\") instead of claiming OSDM \"expects\" it -- the OSDM spec does not say so, and vendors will read that line. (3) The Problem-code match now explicitly excludes OSDM's PARAMETER_NOT_SUPPORTED and VALUE_NOT_SUPPORTED, which describe the REQUEST (an unsupported parameter/value), not the endpoint; OPERATION_NOT_PERMITTED is the only on-point OSDM code (NOT_IMPLEMENTED/UNSUPPORTED are not OSDM codes and are kept only as tolerance for vendor-extension codes). Unit tests added for all three (406 -> fail / null; OPERATION_NOT_PERMITTED -> skip+INFO; PARAMETER_NOT_SUPPORTED / VALUE_NOT_SUPPORTED -> fail)."
      ]
    },
    {
      "release": "2026.214",
      "date": "2026-08-11",
      "server": "1.11.186",
      "collection": "OTST_V2.0.97",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(docker): patch nanoid CVE-2026-67213/-67214 (infinite loop in customAlphabet, both HIGH) inside the built image, blocking the Trivy container-scan gate on every open PR. Same shape as the #428 axios/form-data problem: nanoid 3.3.8 is exact-pinned (no semver range) as a DIRECT dependency of four of Bruno CLI's own sub-packages (@usebruno/converters, @usebruno/filestore, @usebruno/js, @usebruno/schema) inside the globally npm-installed @usebruno/cli tree -- npm refuses to budge an exact pin via reinstall, and a package.json overrides entry EOVERRIDEs against a direct dependency the same way axios did. Fixed the same way: unpack the patched nanoid@3.3.17 release tarball directly over every nested node_modules/nanoid copy in the Docker build, with a verification loop that fails the build if any copy is still on the old version. Targeted 3.3.17 (same 3.x major line as installed), not the 5.x fixed line -- nanoid 5 is ESM-only, a breaking API change; 3.3.17 is the API-stable drop-in.",
        "Root cause of the surfacing pattern: this CVE pair was published/added to Trivy's database sometime between 2026-08-07 (PR #478 last green) and 2026-08-11, so every PR against main -- regardless of its own diff -- started failing the required Container image scan (Trivy) check, exactly like the pre-existing body-parser/ip-address npm-audit findings (#482/#484) blocked PR #485. Not caused by, and not fixable from, any individual feature PR's own changes.",
        "Verification: nanoid 3.3.17 confirmed as a real published npm version (npm view). No local Docker available in this environment -- verified end-to-end via this PR's own required Docker image build + Container image scan (Trivy) CI jobs, matching how the axios/form-data precedent (#429) was itself verified. Oscar_Server/** change only (Dockerfile); no Bruno_Collection change, no application runtime behaviour change.",
        "Follow-up (this same PR, discovered mid-review): folded in a lockfile-only ip-address 10.2.0 -> 10.5.0 bump too, to break a circular CI dependency with the then-open ip-address Dependabot PR (#484, now closed as redundant/superseded once main carried the identical change) -- Trivy (required) blocked #484 until nanoid was patched here, while this PR's own npm-audit step was in turn blocked by the same pre-existing ip-address findings #484 fixed. Matches #484's diff exactly (transitive dependency of express-rate-limit, not promoted to direct)."
      ]
    },
    {
      "release": "2026.213",
      "date": "2026-07-07",
      "server": "1.11.185",
      "collection": "OTST_V2.0.97",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #477 — Test Data Discovery (Discover Timetable + Re-probe offers) and the Places API refresh route now apply the company's Dedicated Headers (API Config, extra_headers), which they previously ignored entirely — only the Bruno test-run path read them. Symptom: on a company requiring custom headers (e.g. SBB's tracestate/traceparent/accept-language), Discover Timetable 400'd on every searched day ('tracestate header is null or empty') while normal scenario runs worked fine, since those two code paths never shared header-building logic. New shared osdm-client.js helper mergeDedicatedHeaders(headers, companyRow, resolvedVars) parses companies.extra_headers and resolves {{var}} templates against the caller-supplied map (case-sensitive, unresolved var -> empty string, never the literal placeholder) — mirrors opencollection.yml's __extraHeaders block exactly, just in plain JS since there's no Bruno environment on this server-side path. Wired into all three call sites: discoverTimetable + reprobeOffers (company-test-resources.js, previously built their own local Requestor/subscription-key-only header set) and the Places refresh route (company-places.js, via buildTesterHeaders which had the identical gap).",
        "Server-only change; no Bruno_Collection/schema/scenario changes.",
        "Tests: 12 new unit tests for mergeDedicatedHeaders (literal values, {{var}} resolution, unresolved-var-to-empty-string, case sensitivity, malformed/missing extra_headers fail-open, non-array ignored, name-less entry skipped, override semantics, mutate-and-return) + 3 new integration tests capturing the real outgoing fetch headers on all three routes (discover-timetable, reprobe-offers, places/refresh), asserting both a literal dedicated header and a {{access_token}}-templated one. Full suite 54 suites / 1335 tests green (was 53/1321); eslint clean. Live browser verification not performed — same seeded-credential constraint as the #211 wizard picker; relying on unit + integration coverage that captures the actual HTTP headers at the fetch boundary."
      ]
    },
    {
      "release": "2026.212",
      "date": "2026-07-03",
      "server": "1.11.184",
      "collection": "OTST_V2.0.97",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "docs(news): announce #239 (book mandatory reservations via optionalReservationSelections) on the welcome page. Content-only change (public/news/index.json); no runtime behaviour affected. Verified: JSON parses, all 45 entries carry the required fields."
      ]
    },
    {
      "release": "2026.211",
      "date": "2026-07-03",
      "server": "1.11.183",
      "collection": "OTST_V2.0.97",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feature #239 — book a mandatory reservation via the booking request's optionalReservationSelections node ({reservationId} per reservationOfferPart), the OSDM mechanism for offers where a reservation is mandatory but no specific place/compartment needs stating. Independent of the existing placeSelections/accommodationSelection mechanism (#371/#373/#211), which additionally states WHICH place is wanted. New scenario-level boolean bookMandatoryReservations (datafile.schema.json) drives a new offers.js handleOptionalReservationSelections() (called alongside handleAccommodationAndPlaceSelection in postOfferResponse) that harvests all of the selected offer's reservationOfferParts.id into the optionalReservationSelections env var; requestsBuilder.js attaches it to each booked offer object (outbound-only for two-step returns, same simplification already established for placeSelections). Wizard gains a 'Book via optionalReservationSelections' toggle in Booking Flow Actions. bookings.js logs which mechanism a scenario used for report traceability — no new assertion needed since the existing generic validateOfferParts() reservation↔booked-reservation check already covers correctness regardless of the booking mechanism used.",
        "Not breaking: the new field is optional and additive, defaulting off — an older collection under this server, or this collection under an older server, both degrade gracefully (the mechanism is simply not exercised). min_collection unchanged at OTST_V2.0.95.",
        "Verification: full suite 53 suites / 1321 tests green (no server-side test file touched — bookMandatoryReservations only feeds a new Bruno_Collection mechanism); eslint + inline-script lint clean; node --check on every edited Bruno_Collection JS file; datafile.schema.json JSON-parses; opencollection.yml YAML-parses and its before-request hook JS syntax-checks clean, confirmed the new field name appears in its reset delete-list."
      ]
    },
    {
      "release": "2026.210",
      "date": "2026-07-03",
      "server": "1.11.182",
      "collection": "OTST_V2.0.96",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "docs(news): backfilled 18 welcome-page news entries covering everything merged since the last news update (2026-06-09, partial refund) through today — Night Train accommodation testing (#211), the env-yml credential-free security fix (#306), stop-place discovery via the Places API (#450), Test-Manager-gated registration (#449), fulfillment type/media declaration (#448), Test Finding <-> scenario linking (#447), CHAPS-onboarding auth diagnostics (masked token logging, credential trimming, raw-body profile, #437-443), company-wide dedicated headers (#427), the Test Findings category accordion and bulk-import (#409/#413), Test Findings & Open Points itself (#400/#401, the threaded conformance-dialogue register with known-deviation baselining), one-click batch ZIP downloads (#406), the Run Budget Ceiling admin field + catalog refundability sweep (#395), the IRT/NJ accommodation-aware testing programme (Accommodation type picker, real-compartment placeSelections, the 🎯 goal-check line, place-selection NHF probes, reservation spec-coverage — #372/#374/#380/#381/#382), run-setup UX polish (#364/#367), the Discovery offer probe + Re-probe button (#368/#370), the OSDM Trip Search Criteria wizard panel (#360), and the step-failure policy HARD_STOP/CONTINUE toggle (#362). Content-only change (public/news/index.json); no runtime behaviour affected. Verified: JSON parses, all 44 entries carry the required fields, served correctly by a live dev server, UTF-8 (emoji/em-dash) intact."
      ]
    },
    {
      "release": "2026.209",
      "date": "2026-07-03",
      "server": "1.11.181",
      "collection": "OTST_V2.0.96",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix: flaky CI test in tests/unit/runner.test.js ('links a reportGenerator HTML artifact when one is present') — the report-artifact linking step in runner.js filters candidate report files by mtime >= runStartTime (excluding stale reports left by a previous run). runStartTime was captured with plain Date.now() millisecond precision; some CI container filesystem storage drivers round/truncate a written file's reported mtime to coarser-than-millisecond precision, which could put a freshly-written file's mtime just BELOW runStartTime even though the write genuinely happened after — silently dropping the html_report artifact link. Root-caused via git history (PR touching only public/news/index.json triggered the exact same pre-existing test failure, confirming it isn't caused by any application change) and by re-reading the mtime-comparison code directly (runner.js, both usages of runStartTime). Fix: subtract a 2-second safety margin from runStartTime before the comparison — a real previous run's leftover report is always at minimum seconds old in practice (the guard exists for same-day, same-company leftovers, not sub-second races), so the margin costs nothing on the staleness guarantee while eliminating the truncation race. Verified: full suite 53 suites / 1321 tests green locally; runner.test.js specifically 19/19; eslint clean. Server 1.11.180 -> 1.11.181, collection unchanged."
      ]
    },
    {
      "release": "2026.208",
      "date": "2026-07-03",
      "server": "1.11.180",
      "collection": "OTST_V2.0.96",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feature #211 — Night Train Sales & Refund: the two SFR-specified scenario families (bed in shared compartment, offerMode INDIVIDUAL; private compartment, offerMode COLLECTIVE) are now reliably distinguishable and validated end-to-end. Investigation found most of the mechanism already existed from the earlier NJ/IRT accommodation programme (#377-#392) — offerMode, the COUCHETTE/BERTH accommodation-type picker, admission-by-reference validation, and multi-passenger booking into one shared reservation were all already wired end-to-end. The real gaps: (1) datafile.schema.json's accommodationSelection enum was stale (only SEAT/COMPARTMENT) while the wizard/code had used COUCHETTE/BERTH since #373 — synced to match; added a sibling accommodationGenderPreference enum (MEN/LADIES/MIXED). (2) offers.js handleAccommodationAndPlaceSelection() picked the first reservationOfferPart matching the desired accommodationType with NO regard for offerMode — a vendor offering both an INDIVIDUAL and a COLLECTIVE part of the same type could silently book the wrong one; now prefers a part whose own offerMode matches the scenario's, and a place whose placeProperties matches the desired gender preference (soft WARNING fallback, never a hard fail, when no match exists — provider-fairness precedent from #391/#436). Also harvests placeProperties from the matched availablePlace into selectedAccommodation, which flows into the booking request's accommodations[] via the existing Object.assign in requestsBuilder.js (no code change needed there). (3) New SFR-specific soft assertions: admissionOfferParts.minGroupItemsToBeBooked/maxGroupItemsToBeBooked type-checked when present (expectTypeOrNull, same idiom as existing nullable-scalar checks); COUCHETTE/BERTH availablePlaces WARN when no MEN/LADIES/MIXED placeProperties is present. (4) bookings.js validateAccommodationGoal(): the place-count-vs-party-size check is now a HARD assertion when the scenario declared an offerMode (exactly 1 place for INDIVIDUAL, place count == party size for COLLECTIVE) while staying a soft WARNING for every scenario that doesn't declare offerMode (unaffected, backward compatible); added a placeProperties request-vs-response echo check (WARNING only — OSDM doesn't guarantee the response echoes it). (5) New 'Preferred place gender' wizard pill picker in scenarios.js, wired the same way as the existing accommodation-type picker. Deliberately NOT touching #227 (passenger-level gender as an offer-request parameter) — the SFR's 'gender-dependent' language refers to the accommodation's placeProperties field, a different, already-partially-built mechanism; #227 stays reserved for the repo owner.",
        "Not breaking: every new field is optional and additive — an older server against this collection, or this server against the prior OTST_V2.0.95 collection, both degrade gracefully (the new preferences are simply not exercised). min_collection unchanged at OTST_V2.0.95.",
        "Verification: full suite 53 suites / 1321 tests green (no server-side test file touched directly — Bruno_Collection has no Jest harness, a documented pre-existing gap, out of scope here); eslint + inline-script lint clean; node --check on every edited Bruno_Collection JS file; datafile.schema.json JSON-parses; opencollection.yml YAML-parses and its before-request hook JS syntax-checks clean. Live browser verification of the new wizard pill picker was not completed this session — resetting a seeded dev user's password to obtain a test-manager session was correctly blocked by the permission system as an unauthorized credential mutation; the picker is a byte-for-byte pattern match of the already-proven accommodationSelection picker (same data-action/click-handler/scenarioParser wiring) plus a clean syntax check."
      ]
    },
    {
      "release": "2026.207",
      "date": "2026-07-03",
      "server": "1.11.179",
      "collection": "OTST_V2.0.95",
      "min_collection": "OTST_V2.0.95",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Security #306 — the ephemeral Bruno env yml no longer carries credentials on disk. Previously the per-run environments/<env>.yml held the decrypted OAuth access token, the Ocp-Apim-Subscription-Key and the oauth_extra/auth_key_secret value in plaintext (0o600, deleted after the run — but a SIGKILL/OOM/host-reboot between write and cleanup, or a mid-run volume snapshot, left them readable). Now buildEnvYml() writes only non-secret plumbing (api_base, data_base, requestor, runId, scenario override, extra headers) and executeRun() hands the three credentials to the Bruno child process via its environment (OSCAR_ACCESS_TOKEN / OSCAR_SUBSCRIPTION_KEY / OSCAR_OAUTH_EXTRA) on top of the existing strict env allowlist (ENCRYPTION_KEY/JWT_SECRET still never forwarded). The collection's before-request hook (opencollection.yml) seeds them into Bruno runtime vars via bru.getProcessEnv() — official Bruno API, works identically on the Linux workspace path and the Windows fallback — and only while the runtime var is still empty, so a token refreshed mid-run through the OSCAR loopback (#204) is never clobbered by the stale spawn-time value; standalone Bruno (no OSCAR_* env) is a strict no-op. The scenario-start env reset's explicit delete-list never touched the credential vars, verified. Crash-time acceptance criterion met by construction: no decrypted credential byte ever reaches a block device. Follow-on wording fixes: the env-file deletion failure is downgraded CRITICAL->warn (hygiene only now), and the TOKEN_FORMAT_ERROR run banner + docs no longer blame the token for YAML parse errors (it can no longer cause them — api_base/requestor still can). Docs updated: Server Admin Guide threat table now claims every credential-bearing path on disk is encrypted or eliminated; Specification §9.9/§10.1/§10.2/§12; Solution Architecture §14.3.4 + Issue-3 superseded note.",
        "BREAKING PAIRING: min_collection raised to OTST_V2.0.95 — a pre-V2.0.95 collection running under server >=1.11.179 never receives the access token (no seeding hook) and every OSDM call returns 401; conversely collection >=V2.0.95 under an older server still works (the hook only seeds when the yml did not provide the var). Deploy server and collection together, as refresh-collection.yml already does.",
        "Tests: runner.test.js 17 -> 19 (env yml on disk contains neither the secret values nor the credential variable names while spawn is in flight; spawn env carries the OSCAR_* vars exactly when configured; server secrets still absent). Full suite 53 suites / 1321 tests green; eslint clean; opencollection.yml YAML-parse + before-request JS syntax-check clean."
      ]
    },
    {
      "release": "2026.206",
      "date": "2026-07-02",
      "server": "1.11.178",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test coverage batch 6 (continuing the SonarCloud gate ratchet): src/server.js, the Express app entry point and the last remaining major coverage gap (236 uncovered lines, 0% covered). Architecturally different from every prior batch — requiring the module has real side effects (env-var validation with process.exit(1), JWT-secret DB bootstrap, queue event wiring, startup run reconciliation, and a real app.listen() that binds an OS port) rather than being a plain Express router. New tests/unit/server.test.js (30 tests) requires the module exactly once against a dedicated, never-dialed test port (supertest wraps the exported `app` directly and needs no real listening socket) and covers: GET /health (DB/queue/disk/process checks), GET /metrics, the datafile download route (filename-regex guard, unknown-company 404, non-loopback 401/403, owning-company 200, loopback-bypass 200), the loopback-only access-token-refresh endpoint (incl. force=1 and a resolveAccessToken-failure 502), the run-artifact download route (ownership check via canUserSeeRun, path/filename guards, a real AES-GCM tamper-detection 500 via a single flipped ciphertext byte), route-mounting wiring, and — via one isolated NODE_ENV=production re-require on a second dedicated port — the HTTPS-redirect middleware (Sonar S5146 open-redirect guard: the localhost and /metrics bypasses, already-https passthrough, the redirect itself). Deliberately out of scope, documented not forced: the process.exit(1) missing-env-var path, the Alertmanager startup config-seed hook (gated behind an unset env var so it naturally never runs), static/SPA file serving (Express's own tested behaviour), and the global error handler (no non-invasive way to force an unhandled throw through a real mounted route without modifying source). server.js 0% -> ~83% lines. Overall Oscar_Server/src line coverage ~83% -> ~88%; full suite 52 -> 53 suites, 1289 -> 1319 tests, all green. Bumped the documented OSCAR-Gate new_coverage floor 74% -> 83% (bump the actual SonarCloud condition to match). Tests-only + Sonar-config-doc; no runtime change; server 1.11.177 -> 1.11.178, collection unchanged OTST_V2.0.94.",
        "Verification: given the elevated risk (two real, uncaptured app.listen() sockets — the main require plus one isolated NODE_ENV=production re-require via jest.resetModules() — and two real, hardcoded, non-overridable data directories written to), this batch was written directly rather than delegated, after reading server.js end-to-end first. Caught and fixed two authoring mistakes before they became false-negative-masking bugs: (1) a path-traversal test asserted 400/404 on a URL Express itself normalizes before routing (hits the SPA fallback at 200 instead) — replaced with a same-segment filename that actually reaches and fails the route's own regex guard; (2) an artifact-tamper test truncated the encrypted file below HEADER_LEN (34 bytes), which made isEncryptedBuffer() treat it as legacy plaintext instead of tripping the AES-GCM auth-tag check — fixed by flipping one byte in the ciphertext region instead of truncating. Also fixed an FK-ordering bug in the file's own afterAll (deleting seeded users before their still-cascading-pending runs' company row violated runs.user_id's non-cascading FK). Stress-tested for flakiness given the real side effects: 6 standalone runs and 3 full-suite runs, all clean, before trusting the result. Confirmed data/datafiles/ and data/artifacts/ have zero leftover test files before and after; eslint clean; scanned for both CodeQL patterns that hit earlier batches — neither present."
      ]
    },
    {
      "release": "2026.205",
      "date": "2026-07-02",
      "server": "1.11.177",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test coverage batch 5 (continuing the SonarCloud gate ratchet): worker/runner.js, the Bruno CLI orchestrator and by far the single largest remaining coverage gap (388 uncovered lines, 16% covered) before this batch. New tests/unit/runner.test.js (17 tests) fully mocks child_process.spawn (never spawns a real subprocess) driving executeRun() through: auth/missing-run/missing-datafile/env-write-failure early exits; exit-code 0 vs non-zero final status; the process-error ('error' event) path; HTML report linking both directly (reportGenerator output) and via the mergeReport.js fallback, plus the second (mergeReport.js) spawn call and its own error path; JSON-results artifact copy; AUTH_401/TOKEN_FORMAT CLI-output detection surfaced onto runs.error_message; the emergency-stop terminal-state guard (a CANCELLED run is never resurrected to COMPLETED); and a real (not fake-timer), config-driven short-timeout kill test. runner.js 16% -> ~68% lines. Overall Oscar_Server/src line coverage ~77% -> ~83%; full suite 51 -> 52 suites, 1272 -> 1289 tests, all green. Bumped the documented OSCAR-Gate new_coverage floor 68% -> 74% (bump the actual SonarCloud condition to match). Deliberately out of scope, documented not forced: the Linux-only workspace create/cleanup functions (platform-gated, never engaged since no test sets scenarioOverride, matching real single-scenario/CI-agnostic behaviour) and the token-watchdog setInterval tick logic (disabled via TOKEN_WATCHDOG_INTERVAL_MS=0 for every test, avoiding a live interval that would otherwise keep Jest's process alive) — both already carry real production risk surface but needed a fundamentally different (fake-timer / platform-conditional) test strategy this batch didn't take on. Tests-only + Sonar-config-doc; no runtime change; server 1.11.176 -> 1.11.177, collection unchanged OTST_V2.0.94.",
        "Verification: given the elevated risk (a real, hardcoded, non-overridable ARTIFACTS_DIR under this same repo, plus real subprocess-spawn danger), this batch was written directly rather than delegated to a subagent, after first reading executeRun() end-to-end. Hit and fixed one real bug during authoring: emitting synthetic 'close'/'error' events on the fake child process after only a single setImmediate tick raced ahead of executeRun's own several-await preamble (mkdir/env-yml-write/datafile-read) — events fired before executeRun had even called spawn(), so its real listeners were never attached and the awaiting promise hung forever; fixed by polling for the Nth actual spawn() call before emitting. Full suite 52/52 green; confirmed data/artifacts/ has zero leftover test directories before and after a full run; eslint clean; scanned for both CodeQL patterns that hit earlier batches (insecure os.tmpdir() writes, unanchored new RegExp(string) checks) — neither present."
      ]
    },
    {
      "release": "2026.204",
      "date": "2026-07-02",
      "server": "1.11.176",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test coverage batch 4 (continuing the SonarCloud gate ratchet): closed the three biggest remaining non-route coverage gaps. New tests/unit/mailer.test.js (24 tests, new file) mocks nodemailer + seeds/restores server_config to exercise all four send*Email functions in both SMTP-configured and dev-mode-fallback branches, taking mailer.js 0% -> 100% (was completely untested). tests/unit/auth-middleware.test.js extended 20 -> 47 tests (cookie parsing incl. malformed/URL-decoded/multi-cookie, token_blacklist revocation branch, isTestManagerOrAbove + userFromRequest exhaustively over every role) taking middleware/auth.js ~62% -> 100%. tests/unit/auth-profiles.test.js extended 30 -> 57 tests (custom profile raw-body-format + placeholder-substitution edge cases, network/timeout/malformed-response failure paths, masked-diagnostic-logging assertions across all profiles) taking worker/auth-profiles.js ~85% -> ~99% (one line left: an unreachable _maskBody() fallback no adapter can actually produce, documented rather than reached through internals). Overall Oscar_Server/src line coverage ~74% -> ~77%; full suite 50 -> 51 suites, 1199 -> 1272 tests, all green. Bumped the documented OSCAR-Gate new_coverage floor 65% -> 68% (bump the actual SonarCloud condition to match). Tests-only + Sonar-config-doc; no runtime change; server 1.11.175 -> 1.11.176, collection unchanged OTST_V2.0.94.",
        "Verification: this batch used 3 parallel subagents; two independently flagged (and self-resolved) a transient mid-task file-read anomaly, so every file was independently re-verified from scratch after the fact rather than trusting any self-report — location/syntax-checked, re-run standalone with --coverage, scanned for both CodeQL patterns that hit earlier batches (insecure os.tmpdir() writes, unanchored new RegExp(string) URL/domain checks — neither present), and the exact test-count delta reconciled against origin/main via git show (1199 + 27 + 22 + 24 = 1272, confirmed exactly). Full combined suite 51/51 green, eslint clean."
      ]
    },
    {
      "release": "2026.203",
      "date": "2026-07-01",
      "server": "1.11.175",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test coverage batch 3 (continuing the SonarCloud gate ratchet): extended integration tests for the three biggest remaining coverage gaps. runs-routes.test.js grew from 10 to 75 tests (submit/list/queue-status/stop-all/batch+zip/logs/assertions/requests/artifacts/share/cancel/delete/bulk-delete/bulk-admin-action, with real seeded run graphs + encrypted artifacts) taking runs.js ~35% -> ~87% — the single largest uncovered file in the project. admin-routes.test.js grew from 40 to 73 tests (users/:id/approve, generate-reset-link, GET/PATCH /config with masking, alertmanager/apply, test-email dev-mode path, rotate-jwt-secret run last + self-restoring) taking admin.js ~60% -> ~89%. company-routes.test.js grew from 15 to 27 tests (extra_headers validation branches, the retired share_reports_with_certifier rejection, and the POST /datafile multipart upload path) taking company.js ~58% -> ~84%. Overall Oscar_Server/src line coverage ~65% -> ~74%; full suite 50 suites, 1089 -> 1199 tests, all green. Documented the OSCAR-Gate new_coverage floor bump 55% -> 65% (bump the actual SonarCloud condition to match). Tests-only + Sonar-config-doc; no runtime change; server 1.11.174 -> 1.11.175, collection unchanged OTST_V2.0.94.",
        "Verification: all extended test files independently re-run standalone with --coverage (not just trusting the authoring pass) — runs.js 75/75, admin.js 73/73, company.js 27/27; full suite 1199/1199 green; eslint clean on all three files; scanned for the CodeQL patterns that hit batch 1 (all os.tmpdir() usage wrapped in mkdtempSync, no unused imports). One real pre-existing behavior surfaced while writing tests (not a regression, not fixed here): POST /v1/company/datafile's multer filename callback treats certification_user as a platform role needing an explicit company_id, erroring 500 before the route's own test_manager-only 403 check runs for that specific role+endpoint combination — tests use a plain tester to exercise the intended 403 branch instead."
      ]
    },
    {
      "release": "2026.202",
      "date": "2026-07-01",
      "server": "1.11.174",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test coverage batch 2 (continuing the SonarCloud gate ratchet): version-info.test.js unit-tests utils/versionInfo.js (compatibility-matrix resolution: matrix-missing/malformed, untested combo, exact + '.x' wildcard match, unknown collection; 0% -> ~97%, 8 tests, jest.isolateModules + mkdtemp temp files); structure-results.test.js covers reports/structureResults.js (pure classifyVendorCapability + serializeBounded across all branches, plus extractStructuredResults over a seeded run with real AES-encrypted artifact files — PASS/FAIL requests, auth-header redaction, assertion counts; ~18% -> ~90%, 22 tests); and auth-routes.test.js was extended to cover the previously-untested auth surface — register/companies, register/check-token, the full password-reset flow (request/check-token/confirm + single-use + login-with-new-password), bootstrap/platform-user (wrong token/invalid role/create/duplicate), and /me + /logout + /sso-check (admin-only) (~49% -> ~86%). Overall Oscar_Server/src line coverage ~59% -> ~65%; full suite 48 -> 50 suites, 1043 -> 1089 tests, all green. Documented OSCAR-Gate new_coverage floor bump 50% -> 55% (bump the actual SonarCloud condition to match). Tests-only + Sonar-config-doc; no runtime change; server 1.11.173 -> 1.11.174, collection unchanged OTST_V2.0.94.",
        "Verification: all new/extended tests pass in the monorepo checkout; full suite 1089/1089 green with --coverage; eslint + inline-script lint clean; new test files scanned for the CodeQL patterns that hit batch 1 (no os.tmpdir writes — mkdtemp used; no unused imports)."
      ]
    },
    {
      "release": "2026.201",
      "date": "2026-07-01",
      "server": "1.11.173",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test coverage batch 1 (toward ratcheting up the SonarCloud new-coverage gate): added Jest+supertest integration tests for the three previously-untested (0% coverage) route files — company-test-resources.js (CRUD + guards + discover-timetable/reprobe-offers via bearer-cred + stubbed global.fetch; ~85% lines, 21 tests), reports.js (compare/comparisons/configured report-builder/templates/trends over a seeded runs+run_requests+run_events graph; ~98% lines, 33 tests), and company-test-framework.js (GET/PUT/DELETE + role gating + lazy salesFlows migration; ~94% lines, 15 tests). Overall Oscar_Server/src line coverage ~50% -> ~59%; full suite 45 -> 48 suites, 974 -> 1043 tests, all green. Documented the OSCAR-Gate new_coverage floor bump 35% -> 50% in sonar-project.properties (the actual condition must be bumped in the SonarCloud UI). Tests-only + Sonar-config-doc; no runtime change; server 1.11.172 -> 1.11.173, collection unchanged OTST_V2.0.94.",
        "Verification: all three new test files pass in the monorepo checkout against the current source; full suite 1043/1043 green with --coverage; eslint + inline-script lint clean. Note: the discover-timetable/reprobe-offers deep harvest-and-merge loops remain uncovered (they need a fully-shaped vendor trips payload) — a follow-up target."
      ]
    },
    {
      "release": "2026.200",
      "date": "2026-07-01",
      "server": "1.11.172",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Chore (SonarCloud gate cleanup, toward making the quality gate a required check): fixes the two conditions that kept main's gate red. (1) Reliability: added an associated label to all 38 <input>/<select>/<textarea> flagged by Web:InputWithoutLabelCheck across 8 public/*.html pages (login/register/verify/reset/forgot, profile, admin, run-detail, report-builder) — <label for> where a visible label existed, aria-label for compact toolbar controls and select-all checkboxes; clears new_reliability_rating C->A and the matching S6853 accessibility smells. (2) Duplication: osdmSchemas.js is a GENERATED file whose per-OSDM-version schema blocks are ~identical by design and accounted for ~2,288 of ~2,585 duplicated lines; added sonar.cpd.exclusions for it (still analyzed for bugs/smells, only copy-paste detection off), dropping new_duplicated_lines_density ~9.2% -> ~1%. Also bumped sonar.projectVersion 1.2.0 -> 1.11.172 (it was stale, which made ~28k legacy lines count as 'new code'; keep it in step with package.json so the Previous-Version new-code period tracks real releases). No server behavioural change; server 1.11.171 -> 1.11.172, collection unchanged OTST_V2.0.94.",
        "Verification: every label for= target confirmed to reference an existing element id across the 8 files; eslint + inline-script HTML lint clean; full jest suite unaffected (HTML/config only). After merge, main's SonarCloud gate is expected to go green on both previously-failing conditions, enabling 'SonarQube Quality Gate check' to be added to branch protection's required list."
      ]
    },
    {
      "release": "2026.199",
      "date": "2026-07-01",
      "server": "1.11.171",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feat #450: Test Config can bulk-download the vendor's OSDM stop places (GET /places) and cache them per company for a full-text origin/destination lookup, so testers pick real place URNs instead of typing urn:uic:stn:NNNNNNN by hand (manual entry still works). New places_cache table (one row/company, plaintext JSON — places are public reference data; migration 25, schema.sql). New routes company-places.js mounted at /v1/company: POST /places/refresh (test_manager only) pages through GET {api_base}/places deduping by id, bounded (100 pages / 100k places, logs if a cap truncates), reusing resolveAccessToken + a new shared src/utils/osdm-client.js (osdmGet + buildTesterHeaders extracted from the discover-timetable boilerplate); GET /places returns cache metadata or, with ?q=, ranked prefix-first matches over name+id (tester+test_manager readable, admin/certifier denied per #60). UI (public/js/scenarios.js): a Download places button + 'N places cached' status in the Test Data → Train Resources toolbar, and a reusable typeahead (attachPlaceAutocomplete, lazily wired via a focusin delegate on [data-place-lookup]) on the Timetable Discovery modal origin/destination and the Train editor origin/destination URN fields. OpenAPI documents both endpoints. Server-side + Test Config UI; server 1.11.170 -> 1.11.171, collection unchanged OTST_V2.0.94.",
        "Verification: new company-places.test.js (11 tests) — auth/role gating (tester reads, admin/certifier 403, refresh test_manager-only), ?q= filtering/prefix-ranking/limit-cap, refresh 400 with no api_base, and a fetch-stubbed refresh happy-path asserting cross-page dedupe + stop-when-no-new-ids + name-falls-back-to-id. db-migrations.test.js extended for migration 25 (places_cache created on a fresh DB and on an already-versioned DB missing the table). Full suite 45 suites / 974 tests green; eslint + inline-script lint clean. Manual browser run (seeded cache, no live vendor): Download-places status renders '6 place(s) cached', the typeahead filters (Basel/Zürich) and selecting fills the URN, manual typing still works."
      ]
    },
    {
      "release": "2026.198",
      "date": "2026-07-01",
      "server": "1.11.170",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #449 (follow-up to 2026.197): self-registration now matches the company by its stable SLUG (submitted verbatim from the /register/companies dropdown, whose <option> value is now the slug) instead of re-deriving a slug from the display name via makeSlug(). A company whose slug was frozen before a rename — real incident: display name 'Paxone', stored slug 'paxone-gmbh', makeSlug('Paxone')='paxone' != stored — was rejected as 'Unknown company'. register/request now validates companySlug and looks up WHERE slug=?; the picked company's canonical name AND slug are stored on the pending registration (new pending_registrations.company_slug column, migration 24, schema.sql); register/confirm resolves the company by that stored slug (falling back to makeSlug(company_name) only for a pre-migration pending row). OpenAPI updated (companyName -> companySlug). Server-side + registration UI; server 1.11.169 -> 1.11.170, collection unchanged OTST_V2.0.94.",
        "Verification: auth-routes.test.js switched to companySlug + a new end-to-end regression test seeding a name/slug-mismatched company ('Paxone'/'paxone-gmbh') and asserting request->confirm succeeds and creates a pending user under the right company; db-migrations.test.js covers migration 24. Full suite 44 suites / 962 tests green, eslint + inline-script lint clean. Manual browser run reproduced the exact Paxone mismatch (dropdown option label 'Paxone' / value 'paxone-gmbh') and confirmed the full request -> confirm -> pending flow now works."
      ]
    },
    {
      "release": "2026.197",
      "date": "2026-07-01",
      "server": "1.11.169",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feat #449: self-registration no longer requires the signup email to contain a fragment of the company name (emailMatchesCompany removed) — instead the applicant must pick a real, existing company (already enforced client-side by the /register/companies dropdown; now also enforced server-side in /register/request) and the new account lands as status='pending' until a Test Manager (or administrator, cross-company fallback) of that company approves it via a new POST /v1/company/users/:id/approve (mirrored at /v1/admin/users/:id/approve). Every Test Manager of the company is emailed (sendPendingApprovalEmail, mailer.js) when a registration is confirmed; a pending user cannot log in (403) until approved. Removed the register/confirm auto-create-company-from-free-text branch, which was unreachable from the UI dropdown and would otherwise have let a stranger self-activate into a brand-new, unverified company with nobody to approve them. New users.status column (migration 23, default 'active', schema.sql). Server-side only; server 1.11.168 -> 1.11.169, collection unchanged OTST_V2.0.94.",
        "Verification: auth-routes.test.js updated for the new contract (seeds a real company; confirms 201+pending/no-token; login 403 while pending; login succeeds after direct approval) plus a new 403-pending-login test. Full suite 44 test suites / 961 tests green. Manual run: register (email deliberately NOT matching the company name) -> dev-mode confirm link -> pending panel (no redirect) -> login 403 -> Test Manager sees Pending badge + Approve button on admin.html -> approve -> login succeeds. node --check clean on all touched server files."
      ]
    },
    {
      "release": "2026.196",
      "date": "2026-07-01",
      "server": "1.11.168",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feat #448: Test Framework gains a new Fulfillment section — pill pickers for fulfillmentType (ETICKET, CIT_PAPER, PASS_CHIP, PASS_REFERENCE) and fulfillmentMedia (PDF_A4, UIC_PDF, PKPASS, ALLOCATOR_APP, RCCST, RCT2, TICKETLESS), so a Test Manager can declare which OSDM fulfillment capabilities the provider actually supports. Wired to the pre-existing framework.fulfillment.{types,media} data model + generic fw-pill/fwTogglePill mechanism (same pattern as Seat Selection/Passenger Types); persists via the existing debounced auto-save. This activates the scenario-level 'E. Fulfillment' picker's existing fwFilter(..., fw.fulfillment.types/media) call, which worked correctly already but had no real input to filter against. Removed fwToggleFulfilPill, a zero-call-site duplicate of the generic handler. Scope: framework-level declaration only; the separate requestedFulfillmentOptionsList scenario editor (fixed in #436 to show the unrestricted OSDM set) is intentionally untouched — warning on an undeclared type/media there needs a different mechanism than frameworkGating.js's current boolean-flag rule engine, tracked as a follow-up. Server-side only; server 1.11.167 -> 1.11.168, collection unchanged OTST_V2.0.94.",
        "Verification: harness over the REAL extracted fwTogglePill + fwFilter — 12/12 (toggle on/off mutates only the targeted subKey; fwFilter reflects it immediately; clearing a category reverts to the unrestricted full set per the existing empty-means-full contract). node --check + eslint + inline-script HTML lint clean."
      ]
    },
    {
      "release": "2026.195",
      "date": "2026-06-25",
      "server": "1.11.167",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feat #447 (Wiremind): a Test Finding can now record the datafile scenario.code that revealed it, so re-testing a fix means selecting that exact scenario in Test Config instead of re-deriving which one it was. New nullable finding.scenario_code column (migration 22, schema.sql); exposed as scenarioCode on the findings API (create/patch/list/thread/bulk-import — same shape as step/expectedStatus). UI: a free-text field with autocomplete against the current datafile's scenario codes (any value accepted, so a since-renamed/deleted scenario keeps its record) on the create/edit form; the list row and thread header show a scenario chip linking to /scenarios.html. Server-side only; server 1.11.166 -> 1.11.167, collection unchanged OTST_V2.0.94.",
        "Verification: db-migrations.test.js extended — migration 22 applies on a fresh install and restores scenario_code on a DB already versioned past it (the #208-class upgrade regression guard). findings-routes.test.js — 2 new tests: scenarioCode round-trips create -> list -> thread, defaults to null, PATCH sets/clears it. Full suite 44 test suites / 960 tests green. node --check + eslint + inline-script HTML lint clean."
      ]
    },
    {
      "release": "2026.194",
      "date": "2026-06-18",
      "server": "1.11.166",
      "collection": "OTST_V2.0.94",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #445: OSDM scalar fields declared nullable:true no longer FAIL when the provider returns null. offers.js + refunds.js asserted the JS type and rejected null; a code generator emits these fields as null when not applicable (returned for consistency), which is spec-legal — so OSCAR raised false failures (reported on SBB FULL_FLEX: isReusable null, price.scale null, numericAvailability null, numberOfPrivateCompartments null). Now assert the declared type OR null via a shared expectTypeOrNull() helper in testCapture.js. Fields covered, all confirmed nullable:true in OSDM 3.8.0: Price/Amount.scale (admission/reservation price, afterSaleFee.scale x3, refund refundableAmount/refundFee/breakdown scales x4), isReusable, part-level + availablePlaces numericAvailability, numberOfPrivateCompartments. Required fields (price.amount, currency) keep strict checks; corrected a stale refunds.js comment that wrongly called scale 'required'. Collection-side; server 1.11.165 -> 1.11.166, collection OTST_V2.0.93 -> OTST_V2.0.94.",
        "Verification: harness over the REAL extracted expectTypeOrNull — 11/11 (null accepted for number/boolean; declared type still passes; a genuinely wrong type — string/object — still FAILS; undefined/absent still fails, since absent != null). node --check clean on testCapture.js/offers.js/refunds.js; a residual strict-assertion sweep over the nullable fields finds none remaining."
      ]
    },
    {
      "release": "2026.193",
      "date": "2026-06-18",
      "server": "1.11.165",
      "collection": "OTST_V2.0.93",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feat #442: the 'custom' OAuth profile gains body_format 'raw' — send the substituted token body VERBATIM, with no URLSearchParams re-encoding. The existing 'form' mode builds the body with URLSearchParams, which percent-escapes % -> %25 (and + -> %2B; . and _ pass through). A token endpoint that consumes the body bytes as-is (CHAPS/CD /auth/login/, reported via #440 follow-up) then sees a different client_secret -> 401 err_5002 'Invalid username or password', even though the identical request works in a standalone raw client. body_format 'raw' lets OSCAR match that byte-for-byte (Content-Type defaults to application/x-www-form-urlencoded, overridable via the template headers). Tester template: {method:POST, headers:{Content-Type:application/x-www-form-urlencoded}, body:'client_id={{client_id}}&client_secret={{client_secret}}&grant_type=client_credentials&scope={{scope}}', body_format:'raw', token_field:access_token}. Server-side; server 1.11.164 -> 1.11.165, collection unchanged OTST_V2.0.93.",
        "Diagnostic: in 'form' mode, when a credential value contains % or + (the characters form-encoding transforms), the run log names the field and points at body_format 'raw' — field name only, never the value (keeps the #437 default-deny mask contract). That one line pinpoints this class of 401.",
        "Verification: harness over the REAL extracted _substitute + SAFE_BODY_KEYS — 9/9: raw passthrough preserves client_secret=ab%cd_e.f+g verbatim (no %25/%2B) while form sends ab%25cd_e.f%2Bg; the hint flags only fields carrying %/+ and never a SAFE_BODY_KEYS key (grant_type with a + stays safe). node --check + eslint clean on auth-profiles.js."
      ]
    },
    {
      "release": "2026.192",
      "date": "2026-06-18",
      "server": "1.11.164",
      "collection": "OTST_V2.0.93",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #440: OSCAR trimmed token_url and oauth_scope but never client_id/client_secret, so a trailing space or newline pasted into the API-Config field was encrypted, stored and sent to the OAuth server verbatim -> 401 'Invalid username or password', even though the identical credential works in a standalone client that trims its inputs (reported on CHAPS/CD …/auth/login/). The #437 masked diagnostic could not reveal it (client_secret=*** masks a clean value and one with a trailing newline alike). Fix: me-credentials.js .trim()s every pasted secret before encrypting (as token_url/oauth_scope already were); access-token.js trims client_id/client_secret/scope/extra again at use-time so credentials saved before this fix are healed without re-entry, and the per-tester cache fingerprint now hashes the trimmed values. Server-side; server 1.11.163 -> 1.11.164, collection unchanged OTST_V2.0.93.",
        "Diagnostics (field/placeholder names only, never values — keeps the #437 default-deny mask contract): (1) a run-log note when stray whitespace was stripped from a credential, naming the field; (2) case-INSENSITIVE custom-template placeholders so {{CLIENT_ID}} resolves like {{client_id}} (was lowercase-only; a capitalised placeholder was sent literally -> same opaque 401); (3) a warning listing any unrecognised {{…}} placeholders (typos like {{secret}}) that would be sent literally.",
        "Verification: harness over the REAL extracted _substitute (now case-insensitive) and _unknownPlaceholders — 14/14 (lowercase/UPPER/Mixed/spaced placeholders resolve; unknown-placeholder detection flags typos, ignores known in any case, dedupes, null-safe). node --check clean on access-token.js, auth-profiles.js, me-credentials.js; credFp NUL-byte separator preserved."
      ]
    },
    {
      "release": "2026.191",
      "date": "2026-06-18",
      "server": "1.11.163",
      "collection": "OTST_V2.0.93",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix (follow-up to #437): the masked token-request diagnostic showed 'Host: <hostname>' (the HTTP Host header, which is hostname-only by spec), which read as a wrong/truncated URL during problem determination (e.g. CHAPS — showed 'Host: osdm-api-test.cd.cz' when the request target was https://osdm-api-test.cd.cz/auth/login/). _logMaskedRequest now shows the full request target (method + URL incl. path) instead: '[runner] Custom request (secrets masked) — POST https://osdm-api-test.cd.cz/auth/login/ | headers: … | body: …'. The full URL was always correct (it's also on the preceding line and the 200/4xx confirms the path); this just makes the masked line self-contained and unambiguous. Server-side; server 1.11.162 -> 1.11.163, collection unchanged OTST_V2.0.93.",
        "Verification: node --check + eslint clean; _maskHeaders/_maskBody unchanged (still 21/21 from #437)."
      ]
    },
    {
      "release": "2026.190",
      "date": "2026-06-18",
      "server": "1.11.162",
      "collection": "OTST_V2.0.93",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feature #437: log a secrets-masked dump of the token request for problem determination (e.g. debugging the CHAPS OAuth handshake). The token request body was deliberately never logged (it carries the client secret); now auth-profiles.js _doFetch emits one masked line covering every profile (basic/post/paxone/sqills/custom): Host (from the URL) + headers + body, with a DEFAULT-DENY mask — only an allowlist of structural fields keeps its value (headers: content-type/accept/host/user-agent/accept-encoding/connection; body: grant_type/response_type/body_format/token_field), everything else (client_id, client_secret, scope, accountName/accountSecret, Authorization, Ocp-Apim-Subscription-Key, api keys…) is masked to '***', with '(empty)' to distinguish a blank field from a populated one. A newly-introduced secret field is masked by default. Example (oauth2_post / CHAPS): '[runner] OAuth2[post] request (secrets masked) — Host: osdm-api-test.cd.cz | headers: Content-Type: application/x-www-form-urlencoded | Accept: application/json | body: grant_type=client_credentials&client_id=***&client_secret=***&scope=***'. Wrapped in try/catch so a diagnostics bug can never break the token fetch. Server-side; server 1.11.161 -> 1.11.162, collection unchanged OTST_V2.0.93.",
        "Verification: harness over the REAL extracted _maskHeaders/_maskBody — 21/21, including the core security property that no confidential value (client id/secret, scope, Basic value, Ocp-Apim key, account secrets) appears in the masked output across URLSearchParams (post/basic), JSON (paxone/sqills) and opaque bodies; default-deny masks unknown headers/keys; empty -> (empty). node --check + eslint clean."
      ]
    },
    {
      "release": "2026.189",
      "date": "2026-06-17",
      "server": "1.11.161",
      "collection": "OTST_V2.0.93",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix (follow-up to #433/#434/#435): the scenario 'Fulfillment Options' dropdowns still showed only 'E-ticket' for a new provider even after the enum + framework-editor fixes. Root cause: buildFulfillmentSection (public/js/scenarios.js) filtered the OSDM fulfillment enums by wizData.framework.fulfillment.types/media, which defaults to ['ETICKET']/['PDF_A4'] — and the only function that edits that (fwToggleFulfilPill) is not wired to any UI, so a new company (e.g. SBB) was permanently locked to E-ticket/PDF_A4 with no way to request TICKETLESS. The scenario fulfillment editor is where the tester EXPLICITLY chooses what to request, so it now offers the full OSDM set directly (typeList = ENUMS.fulfillmentType, mediaList = ENUMS.fulfillmentMedia) — removing the dead framework filter. The datafile schema (#434) already accepts these; the server-side framework gating still flags anything the provider hasn't declared. Also bumped multer 2.1.1 -> 2.2.0 (rides along) to clear two newly-published HIGH DoS advisories (GHSA-72gw-mp4g-v24j, GHSA-3p4h-7m6x-2hcm) that npm audit started flagging on every PR / latent on main; npm audit -> 0 vulnerabilities. Server-side; server 1.11.160 -> 1.11.161, collection unchanged OTST_V2.0.93.",
        "Verification: scenarios.js node --check clean; the now-unused fwFul local was removed (no dangling reference); the dropdown sources the same canonical ENUMS the datafile validation uses."
      ]
    },
    {
      "release": "2026.188",
      "date": "2026-06-17",
      "server": "1.11.160",
      "collection": "OTST_V2.0.93",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix (follow-up to #433/#434): the scenario WIZARD had the same OSDM enum drift the datafile schema did. The scenario Fulfillment Type dropdown is gated by the company's declared Test-Framework capabilities, but the framework editor's option lists (public/js/scenarios.js) contained INVALID non-OSDM values and missed valid ones — so SBB's dropdown offered only 'E-ticket' and TICKETLESS could never be declared. Sourced the four affected lists from the canonical ENUMS (single source of truth): WIZ_FULFIL_MEDIA (had bogus AZTEC_CODE/QR_CODE/NFC) -> 7 OSDM media incl. UIC_PDF/ALLOCATOR_APP/RCCST/RCT2/TICKETLESS; WIZ_FULFIL_TYPES (had bogus PAPER_TICKET) -> ETICKET/CIT_PAPER/PASS_CHIP/PASS_REFERENCE; WIZ_TRAVEL_CLASSES (had non-OSDM THIRD/BUSINESS) -> FIRST/SECOND/ANY_CLASS; WIZ_OFFER_MODES (had COMBINATION) -> INDIVIDUAL/COLLECTIVE. WIZ_SERVICE_CLASSES + WIZ_FLEXIBILITIES were already in sync. Flagged for a follow-up (narrower-but-all-valid, with dependent age/mapping code): WIZ_PAX_TYPES (missing PERSON/PRM_CHILD/COMPANION_DOG) and WIZ_OFFER_PARTS. To use a newly-available media/type in a scenario, declare it in the company's Test Framework first; the scenario dropdowns then offer it. Server-side; server 1.11.159 -> 1.11.160, collection unchanged OTST_V2.0.93.",
        "Verification: scenarios.js node --check clean; the 4 rewired constants now reference ENUMS (defined earlier at line 26) and the removed bogus values (AZTEC_CODE, QR_CODE, NFC, PAPER_TICKET, THIRD, BUSINESS, COMBINATION) have no other references in the file."
      ]
    },
    {
      "release": "2026.187",
      "date": "2026-06-17",
      "server": "1.11.159",
      "collection": "OTST_V2.0.93",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #433: the datafile JSON schema (json_validator/datafile.schema.json) rejected valid OSDM test data because three enums had drifted narrower than the OSDM spec the rest of OSCAR already uses. Surfaced on SBB: fulfillmentMedia TICKETLESS flagged 'Invalid JSON Data file structure'. Audited EVERY enum in the schema against the OSDM source-of-truth (library-bruno/model.js, library-bruno/osdmEnums.js, and the wizard public/js/scenarios.js); only three were stale and are now widened to match: fulfillmentMedia PDF_A4,UIC_PDF -> 7 (adds PKPASS, ALLOCATOR_APP, RCCST, RCT2, TICKETLESS); fulfillmentType ETICKET -> 4 (adds CIT_PAPER, PASS_CHIP, PASS_REFERENCE); passengers[].type PERSON -> the 20 OSDM_PASSENGER_TYPES (osdmEnums.js SSOT; runtime assertions in passengers.js/offers.js already accept all 20). serviceClass (5), travelClass (3), requestedOfferParts (8, incl. CONTINUOUS_SERVICE), flexibilities (3) and offerMode (2) were verified already in sync; OSCAR-internal harness enums (loggingType, scenarioAction, expired*Test, partialRefund*, placeSelectionMode, tripType, etc.) are out of scope. No runtime/behavior change — only the schema gate widened; OSCAR's flow already handles these values. Collection-side; server 1.11.158 -> 1.11.159, collection OTST_V2.0.92 -> OTST_V2.0.93.",
        "Verification: schema parses (JSON.parse); re-extracted the 3 enums (7/4/20 values, 0 SSOT passenger types missing); ajv (the runtime validator) — 11/11: media/type/pax accept TICKETLESS, PKPASS, PDF_A4, CIT_PAPER, ETICKET, DOG, YOUTH, PERSON and reject BOGUS/NOPE/ALIEN."
      ]
    },
    {
      "release": "2026.186",
      "date": "2026-06-17",
      "server": "1.11.158",
      "collection": "OTST_V2.0.92",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #430: a 401/403 on an endpoint a provider simply doesn't support no longer hard-stops the whole run. The #208 auth fail-fast (checkAuthRejection in library-bruno/auth.js) treated ANY non-token 401/403 as a dead token and called stopExecution(), overriding both the step-failure policy and the known-deviation system — so a new provider's 403 on GET /versions (System Version Check, step 00) aborted the run before anything else executed. checkAuthRejection now flags the rejection but does NOT stop when ANY of: (a) it's the System Version Check / GET /versions (an optional capability probe — a genuinely dead token is still caught at the first business request, one step later); (b) stepFailurePolicy != HARD_STOP (the tester chose CONTINUE); or (c) the step+status is a declared known deviation. The default dead-token fail-fast (HARD_STOP + undeclared + business endpoint) is unchanged, so the cascade protection stays. opencollection.yml passes the request URL into checkAuthRejection for robust /versions detection. Additionally handleSystemInfoStatus (osdmCompliance.js) honors known deviations: a baselined system-info non-2xx (e.g. a provider's 403 on /versions) is reported as a documented known deviation (WARNING, not a failure), like the refund/exchange 405 baseline — so the tester can baseline 'GET /versions -> 403' once and keep the run green. Collection-side; server 1.11.157 -> 1.11.158, collection OTST_V2.0.91 -> OTST_V2.0.92.",
        "Verification: harness over the REAL extracted checkAuthRejection + handleSystemInfoStatus — 14/14: non-401/403 and token steps ignored; 403/401 on /versions (by URL and by name) -> no stop; 403 on a business endpoint under HARD_STOP, undeclared -> STILL STOPS (regression guard, incl. default policy + 401); CONTINUE policy -> no stop; declared known deviation -> no stop; handleSystemInfoStatus: 200 -> ok, 403 undeclared -> fail registered, 403 baselined -> noteKnownDeviation + no fail, baselined-but-no-req -> safe fallback to fail. node --check on auth.js + osdmCompliance.js clean; opencollection.yml parses (js-yaml) and its after-response script parses as JS via new Function()."
      ]
    },
    {
      "release": "2026.185",
      "date": "2026-06-16",
      "server": "1.11.157",
      "collection": "OTST_V2.0.91",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Feature #426: company-wide 'dedicated headers' configurable from API Config — add operator-specific request headers without a code change. A Test Manager adds rows of { header name, value } in profile.html (the 'Company — Shared' card) via a '+ Add dedicated header' button; the value may be a literal or reference an existing variable in {{...}} (e.g. {{requestor}}, {{Ocp-Apim-Subscription-Key}}, {{access_token}}) resolved per tester at request time, so secrets stay out of the shared config. New companies.extra_headers column (JSON array, migration v21); GET /v1/company surfaces the parsed array; PATCH /v1/company accepts extra_headers (Test-Manager-only) and validates it (RFC 7230 header-name token, rejects CR/LF, caps 25 headers x 4096 chars, drops blank rows). runner.buildEnvYml emits an __extraHeaders env var (YAML-escaped JSON); opencollection.yml's before-request hook parses it, resolves {{var}} templates against the env, and req.setHeader(name, value) per entry — generalising the previously hardcoded Ocp-Apim-Subscription-Key injection so new headers no longer need a collection edit. Server 1.11.156 -> 1.11.157; collection OTST_V2.0.90 -> OTST_V2.0.91 (opencollection.yml).",
        "Verification: harness over the REAL extracted code — 40/40: normalizeExtraHeaders/parseExtraHeaders (valid + blank-name drop + rejects for space/colon names, CR/LF, >4096 value, >25 rows, non-array; hyphen+dot and Ocp-Apim-Subscription-Key names accepted; numeric/null value coercion; {{var}} value preserved verbatim); buildEnvYml __extraHeaders emission with quote/backslash YAML round-trip and 'no line when empty/undefined'; opencollection.yml resolver ({{var}} incl. hyphenated + whitespace-padded names, literal, partial 'Bearer {{access_token}}', unknown var -> empty, blank-name skip, malformed JSON caught -> no throw + no headers, absent -> no-op). npm run lint clean (eslint + inline-HTML script linter, 15 files); opencollection.yml parses (js-yaml)."
      ]
    },
    {
      "release": "2026.184",
      "date": "2026-06-16",
      "server": "1.11.156",
      "collection": "OTST_V2.0.90",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #428: the Container image scan (Trivy) gate failed on HIGH CVEs in the Bruno CLI's bundled deps, and the existing Dockerfile override silently no-op'd. axios 1.13.6 -> 1.18.0 (CVE-2026-42033/-42035/-42043/-42264 + -44486/-44487/-44488/-44492/-44494/-44495/-44496 — 11 HIGH) and form-data 4.0.4 -> 4.0.6 (CVE-2026-12143). Root cause: @usebruno/js and @usebruno/requests pin axios by EXACT version (1.13.6), so 'npm install axios@^1.15.2 --no-save' had no manifest entry to satisfy and npm pruned the fresh copy back to the pin — the image kept shipping 1.13.6 (it passed before only via a cached Docker layer; a cold-cache build re-exposes it, so it was latent on main too). Fix: axios is a DIRECT dep of @usebruno/cli so an npm 'overrides' entry errors EOVERRIDE; instead we bypass npm resolution and unpack the patched release tarball (npm pack) directly over every nested axios/form-data copy, then assert at build time that each is patched (fail the build otherwise — no broken image ships). axios 1.x / form-data 4.0.x are API-stable drop-ins with hoisted runtime deps. Dockerfile-only; collection unchanged OTST_V2.0.90. Server 1.11.155 -> 1.11.156."
      ]
    },
    {
      "release": "2026.183",
      "date": "2026-06-15",
      "server": "1.11.155",
      "collection": "OTST_V2.0.90",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #422: baseline the documented Turnit refund/exchange GET -> 405 (extends the #398 known-deviation hook). Turnit creates refund/exchange offers (POST /refund-offers, /exchange-offers -> 200 with the full body) but returns 405 on GET /refund-offers/{id} and GET /exchange-operations/{id} (no GET retrieval); steps 11. GET Refund Offer / 12. GET Exchange Offer used to hard-stop the scenario so the rest of the flow never ran. Two changes: (1) loopback.js _normStep now strips a Folder/ path prefix as well as the 'NN. ' prefix, so a baselined finding's report-derived step name (03-Refund/11. GET Refund Offer) matches the request label (11. GET Refund Offer) -> both normalise to 'get refund offer' (previously never matched). (2) The known-deviation hook (previously only in 04. GET Passenger.yml) is added to 03-Refund/11. GET Refund Offer.yml and 04-Exchange/12. GET Exchange Offer.yml: a documented 405 (the baselineInRun finding on the Turnit board) -> PASSING 'known deviation' + [WARNING] + proceed (the offer id came from step 10/11), instead of a hard stop. Undocumented non-200s still fail; the 200 happy path is untouched. Library-side (Bruno_Collection: library-bruno/loopback.js + the two yml steps). Note: this lets steps 12-16 (incl. 13. PATCH Refund Offer) run on a fresh Turnit run, which will show whether they complete.",
        "Verification: harness over the REAL extracted _normStep + knownDeviationFor -- 11/11 (folder-prefixed <-> short labels collapse equal; refund/exchange 405 match; legacy GET Passenger 501 still matches; 200 and undocumented 404 -> no match; active:false not enforced; empty -> null). node --check on loopback.js clean; both edited yml after-response scripts parse as valid JS via new Function(). Server 1.11.154 -> 1.11.155; collection OTST_V2.0.89 -> OTST_V2.0.90."
      ]
    },
    {
      "release": "2026.182",
      "date": "2026-06-15",
      "server": "1.11.154",
      "collection": "OTST_V2.0.89",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #420: 'Discover timetable' finds 0 trips on PAXONE — it queried at midnight. After #418/#419 cleared the 422, PAXONE discovery returned 200 but 0 trips every day, even on a route the SALE scenario proves runs. Confirmed from the PAXONE SALE report: the working request searches departureTime …T06:00:00 and returns trips[].legs[].timedLeg (exactly what harvestTrips reads — harvester is fine); discovery searched …T00:00:00 (midnight). PAXONE returns offers only AROUND the requested time, so a midnight query finds nothing (its sandbox trains run daytime). Fix in company-test-resources.js _tripSearch: query PAXONE at T08:00:00, gated on /paxone/i; Bileto (offset) and every other sandbox keep midnight (whole-day), unchanged. Also: each discovery day now reports its offers count in dayResults so a future 0-trips is self-diagnosing (offers>0 but trips==0 = harvest/shape issue; offers==0 = no service). Server-only; collection unchanged.",
        "Verification: harness over the REAL extracted _tripSearch — 6/6 (PAXONE -> T08:00:00 no offset; Bileto -> T00:00:00+00:00 unchanged; other -> T00:00:00 unchanged; undefined apiBase safe; case-insensitive). node --check clean. Server 1.11.153 -> 1.11.154; collection unchanged OTST_V2.0.89."
      ]
    },
    {
      "release": "2026.181",
      "date": "2026-06-15",
      "server": "1.11.153",
      "collection": "OTST_V2.0.89",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #418 (server-side companion to #416): the 'Discover timetable' feature (company-test-resources.js _discoveryBody) scans POST /offers across N days and was sending offerSearchCriteria:{}, so on PAXONE every searched day returned 422 VALIDATION_ERROR ('offerMode/currency is missing') and discovery reported 'No usable offer response across the searched days' — blocking PAXONE timetable discovery entirely. #416/#417 fixed the Bruno request builder (requestsBuilder.js) but missed this server path (the one behind the Discover-timetable button). _discoveryBody now sends {currency:'EUR', offerMode:'INDIVIDUAL'} for PAXONE (neither filters the harvested timetable), empty {} for every other sandbox (unchanged). Same /paxone/i api_base gate. Server-only (Oscar_Server/src/api/routes/company-test-resources.js); collection unchanged.",
        "Verification: harness over the REAL extracted _discoveryBody (source-extracted + eval'd, since the route module opens the DB on require) — 6/6 (PAXONE -> EUR+INDIVIDUAL with trip/passenger preserved; non-PAXONE -> empty; case-insensitive; non-offers endpoint -> bare trip; undefined apiBase safe). node --check clean. Server 1.11.152 -> 1.11.153; collection unchanged OTST_V2.0.89."
      ]
    },
    {
      "release": "2026.180",
      "date": "2026-06-15",
      "server": "1.11.152",
      "collection": "OTST_V2.0.89",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #416: PAXONE offer requests fail (422 VALIDATION_ERROR) when offerSearchCriteria.currency + offerMode are absent — both optional in OSDM but mandatory for PAXONE, blocking offer/discovery for any scenario that doesn't declare them (not tracked as a finding; PAXONE is simply stricter than the spec). New helper withPaxoneOfferSearchCriteriaDefaults() in requestsBuilder.js defaults currency (from the scenario's offerSearchCriteriaCurrency env, else EUR) + offerMode (INDIVIDUAL) on both the outbound (buildOfferCollectionRequest) and return (buildReturnOfferCollectionRequest) offer requests. Gated behind the existing isPaxone (api_base.includes('paxone')) check, so NO other sandbox's offer request changes; only the two missing keys are filled (any criteria the scenario already set pass through untouched), input never mutated. Library-side only (Bruno_Collection/library-bruno/requestsBuilder.js).",
        "Verification: harness over the REAL extracted withPaxoneOfferSearchCriteriaDefaults — 10/10 (absent/null -> EUR+INDIVIDUAL; absent + scenario currency CZK -> CZK+INDIVIDUAL; partial fills only the missing key; fully-specified unchanged with extra criteria preserved; empty-string treated as missing; non-object -> fresh defaults; input not mutated). node --check clean. Server 1.11.151 -> 1.11.152; collection OTST_V2.0.88 -> OTST_V2.0.89."
      ]
    },
    {
      "release": "2026.179",
      "date": "2026-06-15",
      "server": "1.11.151",
      "collection": "OTST_V2.0.88",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #414: GET Passenger false-fails when a provider returns passengers out of submitted order (Turnit). 03. PATCH Multi Passenger / 04. GET Passenger pair passengerIdList[i] with passengerAdditionalData[i] BY INDEX; passengerIdList was built in booking-return order (bookings.js) while the expected data is submitted order (scenarioParser.js), so a provider that reorders passengers made every per-passenger field (name/DOB/phone/email) compare against the wrong row — ~25 false-fails on a 5-pax run, all data correct. Proven on Turnit: submitted [PAX01..PAX05] returned [PAX05,PAX04,PAX02,PAX03,PAX01]. New testable helper alignPassengerIdsToSubmittedOrder(bookingPassengers, submittedRefs) orders passengerIdList back to submitted order keyed on externalRef (the retailer ref OSCAR sends and the provider echoes on the booking + GET /passengers/{id}). Safe fallback to booking order when refs are absent / counts mismatch / any ref unmappable, so providers that don't echo externalRef and in-order providers (identity no-op) are unaffected; a [WARNING] documents any realignment. Library-side only (Bruno_Collection/library-bruno/bookings.js).",
        "Verification: harness over the REAL extracted alignPassengerIdsToSubmittedOrder driven by the actual Turnit booking JSON + synthetic cases — 17/17 (real reorder aligns by externalRef, ids a permutation of booking ids; downstream comparison OLD 5/5 mismatches -> NEW 0/5; in-order identity no-op; no-ref/count-mismatch/unknown-ref/empty/null fall back to booking order). node --check clean. Server 1.11.150 -> 1.11.151; collection OTST_V2.0.87 -> OTST_V2.0.88."
      ]
    },
    {
      "release": "2026.178",
      "date": "2026-06-15",
      "server": "1.11.150",
      "collection": "OTST_V2.0.87",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test Findings & Open Points list reorganised into a category accordion (#412). The flat card stack is now a two-level accordion grouped by category (provider_deviation/oscar_issue/not_supported/spec_question/open), collapsed by default. Each category header shows the lifecycle breakdown as counts — New (not yet reviewed) / WIP (discussion ongoing) / Closed (settled), zeros muted. Expanding a category reveals the three lifecycle sub-groups themselves collapsed; expanding one lists its findings. Both levels persist their open/closed state across re-renders, with an Expand-all/Collapse-all toggle. Finding rows are leaner now the headers carry category + lifecycle (severity dot, title, in-runs/OSDM badges, reply count, step/HTTP line); clicking a row opens the existing thread. New/WIP/Closed is a display-only relabel of the stored open/discussing/resolved status (the thread-view status pills + badge adopt the same labels) — no API, schema, or data-model change. Front-end only: public/js/findings.js.",
        "Access verified unchanged (the Test Manager asked to confirm all company Test Managers see the same findings): GET /v1/company/findings resolves companyId = req.user.companyId and queries WHERE company_id = ? with no created_by filter, so every test_manager AND tester of a company shares one board; writes stay test_manager-only. Confirmed empirically on Bileto (2 Test Managers + 1 tester, same company).",
        "Verification: node --check on findings.js clean; public/js is outside the eslint scope (CI lints src/). Grouping + lifecycle-count logic previewed live against the 12-finding Bileto board (Provider deviation 9 / OSCAR issue 1 / Not supported 2) in both collapsed and expanded states. Findings API unchanged (findings-routes.test.js still applies). Server 1.11.149 -> 1.11.150; collection unchanged OTST_V2.0.87."
      ]
    },
    {
      "release": "2026.177",
      "date": "2026-06-14",
      "server": "1.11.149",
      "collection": "OTST_V2.0.87",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Fix #410 (#14/#15): placeSelection reservationId stale across offers. requestsBuilder.js now derives the booking placeSelections.reservationId from the SELECTED offer (bru env 'offer') at build time, instead of the reservationId env var that offers.js sets by first-match and keeps across offers (offers.js:1642 'already set -> keep it'). In multi-offer flows (..._RETURN, ADD_TO_BOOKING) that value could point to a reservationOfferPart of a DIFFERENT offer than the one booked -> the #377 pre-flight rejected it (Bileto #14: 'placeSelections[0].reservationId is not a reservationOfferPart of the selected offer'), and the same stale/unresolved id drove the Bileto add-reservation 400 (#15; cf. the offers.js:1630 comment documenting a prior Bileto 400 here). New resolvePlaceSelectionReservationId(): honour the env id only when it is a part of the selected offer; else pick the reservationOfferPart matching the chosen accommodation (selectedAccommodation), else the first reservation part; the #377 pre-flight stays as the guard; no offer context -> keep the env var (legacy). Resolves the deferred place-selection audit item. Library-side only (Bruno_Collection/library-bruno/requestsBuilder.js).",
        "Verification: harness over the REAL extracted resolvePlaceSelectionReservationId with stubbed bru/parseEnvJson/validationLogger — 8 checks (env-id-valid keep; stale-id re-derive = the Bileto #14 repro; accommodation-type match; no-offer keep; unset -> first part; offer-as-JSON-string parsed; valid-non-first keep; empty-parts keep). node --check clean. Server 1.11.148 -> 1.11.149; collection OTST_V2.0.86 -> OTST_V2.0.87."
      ]
    },
    {
      "release": "2026.176",
      "date": "2026-06-14",
      "server": "1.11.148",
      "collection": "OTST_V2.0.86",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Findings generic Import (#408). A '+ Import' button on the Test Findings page (test_manager) accepts a pasted JSON array of findings and creates each via the existing POST /v1/company/findings (loop; the create endpoint validates/whitelists per item). Each imported finding is authored 'OSCAR analysis' unless the item supplies createdBy. Replaces the hardcoded OBB seed (removed in #403) with a GENERIC, per-sandbox path — usable for Bileto / SNCF / any provider — and is the intended front door for the Phase 2A auto-analyzer (OSCAR proposes findings, Import ingests them). Frontend-only change to public/js/findings.js (new import view + import-open/cancel/run actions + import-text input); no server or schema change. Used to file the Bileto findings set from the 2026-06-14 report analysis.",
        "Verification: node --check on findings.js; npm run lint clean (eslint src + inline-script lint over 15 HTML files). The import reuses the #400-verified findings POST endpoint (integration-tested in findings-routes.test.js). Server 1.11.147 -> 1.11.148; collection unchanged OTST_V2.0.86."
      ]
    },
    {
      "release": "2026.175",
      "date": "2026-06-14",
      "server": "1.11.147",
      "collection": "OTST_V2.0.86",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "One-click bulk report download (#405). A '+ Reports' button on each Dashboard batch header streams a single ZIP of every run's artifacts in that batch — each run's HTML report + raw JSON results, entry-named by scenario — as {sandbox}_{date}_batch-{shortid}.zip. New endpoint GET /v1/runs/batch/:batchId/reports.zip: strictly company-scoped (WHERE batch_id AND company_id; a foreign company gets 404), each run_artifacts file path-checked under data/artifacts and decrypted at-rest (decryptFromFile) before being added to the archive, rate-limited (30/min). The download name comes from the server Content-Disposition (run.env_name_used -> sandbox, started_at -> date). Kills the cumbersome one-by-one report download and lets a whole sandbox batch be handed to OSCAR for finding-analysis as one file. The ZIP is produced by a new dependency-free store-mode writer utils/zip.js (self-contained CRC-32), in keeping with the codebase's hand-rolled-over-dependency convention (cf. utils/at-rest.js, the cookie parser in auth.js). The dashboard button lives inside the batch-header toggle row, so its click stops propagation to avoid collapsing the batch. UI + one read-only endpoint + a util; no schema or data-model change.",
        "Verification: tests/unit/zip.test.js (archive structure — local/central/EOCD signatures, entry counts, central-dir offset, CRC-32 cross-checked vs zlib.crc32, UTF-8 names/data, empty-list) + tests/integration/batch-reports-zip.test.js (seeds a 2-run batch with REAL encrypted artifacts; asserts 401 no-token, 404 unknown-batch, 404 cross-company scoping, and a 200 valid ZIP with 3 scenario-named entries + the friendly Content-Disposition). The hand-rolled zip was independently validated by .NET's System.IO.Compression reader (opened, enumerated, content + UTF-8 + CRC round-tripped). node --check + eslint + inline-script lint clean. Server 1.11.146 -> 1.11.147; collection unchanged OTST_V2.0.86."
      ]
    },
    {
      "release": "2026.174",
      "date": "2026-06-14",
      "server": "1.11.146",
      "collection": "OTST_V2.0.86",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Findings UX + report naming (#403). Two quality-of-life fixes after Test Findings & Open Points (#400) went live on real sandboxes. (1) Run-artifact downloads are self-describing: the JSON results file from a run page now downloads as {sandbox}_{date}_{scenario}.json (derived client-side in run-detail.html from run.env_name_used -> sandbox, run.started_at/queued_at -> date, run.scenario_code; sanitised to [A-Za-z0-9._-], length-capped at 120) instead of the anonymous bru_results.json, so a folder of downloaded reports is no longer indistinguishable. The displayed artifact name shows the friendly name (raw stored name on hover); HTML 'Open Report' is unchanged (it already carries the scenario). (2) Dropped the hardcoded OEBB/Nightjet seed import from the Findings page: the button rendered on EVERY sandbox (Bileto included), not just OEBB — removed (OBB_SEED const + importSeed() + the import-seed action + the empty-state button); the empty state now points to '+ Open a finding'. It was a Phase-1 convenience; the generic per-sandbox analyzer (Phase 2A, AI-assisted) is its proper replacement and OEBB's findings are already imported. UI-only; no engine, schema, route, or data-model change. eslint + node --check + inline-script lint clean. Server 1.11.145 -> 1.11.146; collection unchanged OTST_V2.0.86."
      ]
    },
    {
      "release": "2026.173",
      "date": "2026-06-14",
      "server": "1.11.145",
      "collection": "OTST_V2.0.86",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Test Findings & Open Points (#400). The #398 known-deviation checklist is reframed from a Test-Config wizard panel into a threaded, per-test-system conformance dialogue on its own main-menu page (/findings.html, nav item for testers + test managers). Server store: finding + finding_comment tables (schema.sql, idempotent — picked up on next boot). REST: GET/POST/PATCH/DELETE /v1/company/findings + POST /v1/company/findings/:id/comments; reads = the vendor's own test_manager + testers, writes = test_manager only (administrators + certifiers have no access, mirroring GET /v1/company/datafile). Each finding carries an observation + OSCAR's spec reading, a SOFT category (open | provider_deviation | oscar_issue | not_supported | spec_question), a SOFT severity (major | minor | not_supported), a status (open -> discussing -> resolved, auto-advanced on first reply), a baseline_in_run switch and a raise_to_osdm flag. Soft wording is deliberate: a finding may be the provider's deviation OR OSCAR's own bug — the thread decides, so OSCAR never pre-labels a provider. Runtime link: a finding baselined with a step + numeric expected_status is projected server-side (utils/knownDeviationProjection) into the datafile's top-level knownDeviations[] in the exact {step, expectedStatus, note, active} shape the #398 engine consumes — regenerated on every finding change AND on every datafile save (PUT /v1/company/datafile/json overwrites the array from the register), so a wizard save can't wipe or hand-edit it; the datafile is decrypted/re-encrypted at rest. The unchanged #398 Bruno engine (loopback.knownDeviationFor / scenarioParser.setKnownDeviations / 04. GET Passenger.yml) consumes it as before. The #398 wizard panel (1.11.144) is fully excised from scenarios.js. Ships an importable OEBB/Nightjet starter set = OSCAR's 2026-06-14 analysis (GET-passenger 501 [baselined — the run-failing core gap] + System-Info 501s [not-supported] + RI vocabulary passenger[0].details.* + refundable=NO over a below-price schedule + exchangeable=NO on FULL_FLEXIBLE + expiry-error vagueness + step-14 provisionalPrice/reservation-status).",
        "Verification: integration test tests/integration/findings-routes.test.js + a standalone supertest harness over the REAL route + db + at-rest (temp DB) — 23 checks: auth/role gating (401 no token, 403 certifier read, 200 tester read, 403 tester write), create + defaults, 400 no-title, list with comment counts, threaded comment with open->discussing auto-advance, PATCH classify, baseline guard (refuses baseline_in_run without a step + numeric status), 404s, buildProjection mapping (only baselined rows with step + numeric status, mapped to {step,expectedStatus,note,active:true}; empty for unknown company), and an END-TO-END loop (PUT datafile -> POST baselined finding -> GET datafile shows the projected knownDeviations entry). eslint clean on the new/edited src; node --check on findings.js + scenarios.js; #398 panel grep-confirmed gone (no dangling deviationChecklistHtml / knownDeviations references in scenarios.js). Server 1.11.144 -> 1.11.145; collection unchanged at OTST_V2.0.86 (no Bruno change this cut). Operator: declare a sandbox's findings in Test Findings & Open Points and baseline the ones the team accepts (step + HTTP status) — runs then read PASS-with-documented instead of FAILED on those."
      ]
    },
    {
      "release": "2026.172",
      "date": "2026-06-13",
      "server": "1.11.144",
      "collection": "OTST_V2.0.86",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Known-deviation baseline (#398). A provider's documented non-conformances stop dragging every run to FAILED. Declared in the UI per test-system (Test-Manager-gated), persisted as a top-level knownDeviations[] in that sandbox's datafile (sibling of systemInfoParameters; schema-validated), each entry {step, expectedStatus, note, active}. Engine: scenarioParser.setKnownDeviations loads it into __knownDeviations; loopback.knownDeviationFor matches a step's HTTP status against the ticked deviations (tolerant of the 'NN. ' request-name prefix; active:false = kept on record, not enforced); 04. GET Passenger.yml emits a PASSING 'known deviation (documented)' row + WARNING instead of throwing when 501 matches, so a clean OBB run is no longer FAILED by the already-reported GET /passengers gap. Any OTHER status still fails for real (the baseline can't hide a regression); noteKnownDeviation bumps a per-run tally. UI: a dedicated 'Known deviations' card in the scenarios section (renderScenariosSection) with add/remove/tick rows; saveDatafile normalises (integer status, drops incomplete rows) so the persisted datafile stays schema-clean. Safe no-op until a deviation is ticked (defaults to []). No change to the server's pass/fail computation — entirely Bruno-side + datafile/UI.",
        "Verification: harness over the REAL extracted functions (knownDeviationFor / noteKnownDeviation / setKnownDeviations) — 22 checks: prefix-tolerant + status-coerced matching, active:false skip, success/undocumented-status no-match, WARNING+tally+dedup, parser load/reset/malformed-guard. The wizard panel renders from the real deviationChecklistHtml (toggles + rows + empty state). node --check on scenarios.js + loopback.js + scenarioParser.js; schema JSON valid; eslint clean. Cut 1 = status-level deviations (covers GET Passenger 501, every run); assertion-level deviations (the step-14 trio) are a documented fast-follow on the same framework. Server 1.11.143 -> 1.11.144; collection OTST_V2.0.85 -> OTST_V2.0.86. Operator: register each sandbox's accepted gaps in Wizard -> Scenarios -> Known deviations; runs then read PASS with documented deviations instead of FAILED."
      ]
    },
    {
      "release": "2026.171",
      "date": "2026-06-13",
      "server": "1.11.143",
      "collection": "OTST_V2.0.85",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Matcher-credibility audit round 1 (#396). The audit (the direction chosen after #395) swept every offer<->booking pairing function for the first-match-by-type disease that produced false findings this week; the historically-diseased matchers (afterSalesConditions #390, appliedPassengerTypes #384) are confirmed fixed, and this PR fixes the one the audit found in an un-audited flow. (1) Exchange-fee validation carried the pre-#391 disease: validateExchangeFeesConsistentWithAfterSalesConditions summed afterSaleFee across ALL EXCHANGE windows of ALL parts and hard-asserted equality to exchangeFee (and to a second naive sum in the admissionReservationAncillaryOfferPartsAftersalesConditions env var) — a part with a two-window schedule (50% before travel / 100% after) contributed 150%, manufacturing a false 'exchange fee mismatch' on any multi-window/multi-part offer (the same class as the OBB afterSaleFee retraction). Now schedule-aware on effectiveRefundability: expected fee = sum of the ACTIVE window's fee per value-bearing part, decode-safe (hard-asserts only when every value-bearing part has a decodable active EXCHANGE schedule and one currency; otherwise INFO + skip). It also moved OUT of the inverted `if (appliedOverruleCode)` gating into the normal flow and skips under overrule (an overrule overrides the schedule) — previously the normal exchange was never checked and the overrule case compared against the wrong baseline. (2) 🎯 accommodation goal: bookedRes.find(id) || bookedRes[0] silently validated the goal against the FIRST reservation when the id was not echoed — in a multi-reservation booking that can green-light the wrong compartment. Now id -> requested accommodation type -> first, with a WARNING when the last fallback is ambiguous (>1 reservation).",
        "Verification: harness over the REAL extracted functions with stubbed bru/test/validationLogger and the REAL afterSalesRules.js — exchange decode 7 (single active window match/mismatch; TWO-WINDOW fee=active-window PASSES while fee=old-naive-sum now FAILS 'engine and its own declared schedule disagree'; OBB reality exchangeable=NO/no-EXCHANGE-windows -> INFO skip with zero assertions; overruleCode sent -> skip; no numeric exchangeFee -> early return); 🎯 target 5 (single id match no warning; multi id match; multi no-id type-fallback finds the COUCHETTE not the first SEAT; multi no-id no-type -> first + WARNING; single never warns). node --check x2; eslint clean. Deferred (tracked, not guessed): offers.js place-selection first-subtype pick feeds the live booking placeSelections — needs a real multi-subtype scenario. Server 1.11.142 -> 1.11.143; collection OTST_V2.0.84 -> OTST_V2.0.85."
      ]
    },
    {
      "release": "2026.170",
      "date": "2026-06-12",
      "server": "1.11.142",
      "collection": "OTST_V2.0.84",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Bundle #393 + #394. (1) Catalog-wide flag-vs-schedule sweep (#393): the per-scenario verdict (#391) analyses only the SELECTED offer, but the OBB pattern is catalog-wide — 16 of 24 NJ offers pin refundable=NO while their own REFUND schedules charge below the price (Komfort 50%, Normalpreis free). offers.js sweepCatalogFlagVsSchedule runs after the per-offer validations over ALL offers' value-bearing parts (price>0; 0-price reservation parts skipped — the flag legitimately rules there, #391) and emits ONE summary R9 WARNING per action when contradictions exist: '16 of 24 offers declare refundable=NO on a value-bearing part while their own REFUND schedule charges less than the price (8 even with a FREE window) — per the spec enum these are WITH_CONDITION; 8 offer(s) are consistent (every REFUND window charges the full price)'. Silent when nothing contradicts (R8). Every offers response now produces catalog-scale dossier evidence, not just the selected offer's. (2) Run Budget Ceiling in the admin panel (#394): computeEffectiveRunTimeoutMs clamps every run budget at RUN_HARD_MAX_TIMEOUT_MS (default 1800000 = 30 min), which was env-only and invisible — the panel accepted RUN_TIMEOUT_MS up to 3600000 and everything above the ceiling silently did nothing. OBB's preBookableUntil = offer +30 min sharp, so the expired-OFFER (OTO) wait (~1860s incl. buffers) missed the cap by ONE MINUTE on every run, unfixable from the UI. CONFIG_SCHEMA gains the key (number, 60000..7200000); the generic GET/PUT handlers make it visible, validated and persisted to server_config, which getConfig prefers over the env var on the NEXT run (no restart). Panel label 'Run Budget Ceiling (ms)'; RUN_TIMEOUT_MS description names the clamp; Server Admin Guide row updated.",
        "Verification: #393 harness 19 checks (mixed-catalog counts, silence-when-consistent, degenerate inputs, EXCHANGE mirror, synthetic 24-offer catalog reproduces the 16 / 8-free / 8-consistent dossier numbers); #394 node --check + CONFIG_LABELS check (the new key rides the existing generic config pipeline — no new code paths). Operator action: to make the OBB expired-offer (OTO) test runnable, set Run Budget Ceiling to e.g. 3600000 in Admin -> Server Config (or env RUN_HARD_MAX_TIMEOUT_MS in OSCAR_Deploy docker-compose) and arm the scenario Max wait >= 35 min; RTO (~17 min) and BTO (~18 min) already fit under the default 30-min ceiling as separate runs. Server 1.11.141 -> 1.11.142; collection OTST_V2.0.83 -> OTST_V2.0.84. Chip shows 2026.170 / server-v1.11.142 / OTST_V2.0.84 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.169",
      "date": "2026-06-12",
      "server": "1.11.141",
      "collection": "OTST_V2.0.83",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Schedule-aware effective refundability (#391), from the OBB exchange on the 18-offer NJ catalog (admissions pinned to refundable=NO while the schedules said three different things). Rule: a part is effectively refundable iff SOME declared REFUND window charges a fee BELOW the part's price — mere presence of conditions proves nothing (Sparschiene: one window fee=100% -> the schedule CONFIRMS NO). (1) NEW pure module afterSalesRules.js: effectiveRefundability(part, at, action) — fee-vs-price per window, active-window fee at an instant, contradiction classification (FLAG_NO_SCHEDULE_REFUNDABLE / FLAG_WC_NO_SCHEDULE / FLAG_YES_ZERO_WINDOW); expectedRefundForParts = sum(price − active fee), no-active-window contributes 0. (2) Offer verdict (ensureYesWhenRefundOrExchangeSelected) is schedule-first: Komfort/Normalpreis (flag NO, 50%/free window) PASS with the precise R9 WARNING ('the flag does not summarize the rules; per the spec enum, WITH_CONDITION is the value for exactly this schedule — distributors filtering on the flag would hide a refundable product'); Sparschiene FAILS decoded ('every declared REFUND window charges the full price — a refund would return 0; the schedule CONFIRMS the flag') with NO false contradiction warning; WITH_CONDITION without any declared condition -> WARNING (the flag promises conditions a client cannot read); flag-only parts keep the pre-#391 behaviour. (3) Refund permissibility (refunds.js) re-keyed on EFFECTIVE refundability: gate 1 every-part-effectively-non-refundable (schedule-confirmed wording), gate 2 value gate on effectively-permitting parts (the schedule-legal Normalpreis full refund no longer raises the false 'locked value' row), gate 3 NEW schedule-decode assertion: refundableAmount must equal sum(price − active-window fee) right now — a Sparschiene payout or a post-departure full refund fails 'the engine and its own declared schedule disagree' (the refund-after-departure discriminator, machine-checked). Overrule still bypasses; currency/scale guarded.",
        "Verification: harness with the VERBATIM catalog families — rules 8 (Sparschiene schedule-confirms-NO no contradiction; Komfort 50% + Normalpreis free -> WITH_CONDITION + FLAG_NO_SCHEDULE_REFUNDABLE; after-travel active fee = full price; 0-price part -> flag rules; WC-without-schedule + YES-with-full-fee flagged); verdict 6 (Normalpreis/Komfort PASS + Marcel-sentence WARNING; Sparschiene FAIL decoded with ZERO warnings; flag-only NO fail; EXCHANGE mirror); permissibility 8 (Normalpreis pre-travel full refund GREEN + 'schedule decode OK 88950' — yesterday's false positive gone; Sparschiene payout fails gate 1 schedule-decoded; Komfort 50% match green / over-payout fails 'says 12225'; AFTER-DEPARTURE full payout fails 'says 0'; gate-1 flag-only wording; flag-only YES -> decode skipped, green; overrule silent). node --check x3; eslint + inline-script lint green. Server 1.11.140 -> 1.11.141; collection OTST_V2.0.82 -> OTST_V2.0.83."
      ]
    },
    {
      "release": "2026.168",
      "date": "2026-06-12",
      "server": "1.11.140",
      "collection": "OTST_V2.0.82",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Window-aware condition pairing & value-aware refund gate (#389), confirmed with OBB (Marcel Koseler) + tester review of 2026.167. (1) validateAfterSalesConditions paired offer->booking conditions with find()-by-condition-type — with TWO REFUND windows (free until the eve of travel / full fee after) every offer window was compared against the booking's FIRST window of that type, manufacturing 'the offer said 88950, the booking says 0' on perfectly mirrored payloads (booking echoes both windows, same instants in Z vs +02:00). Now: pair by type + identical validity INSTANTS (timezone-insensitive) -> consumption among same-type leftovers -> re-use with one WARNING; DEBUG names the pairing; new WARNING when the paired windows genuinely differ (schedules diverge between offer and booking). The 'systematic afterSaleFee zeroing' OBB finding (60990/87950/88950 -> 0) is formally RETRACTED — it was this matcher, the same first-match disease as the #383 appliedPassengerTypes fix. (2) Value-aware refund permissibility: the #387 gate failed only when EVERY part declared refundable=NO; the tester's mixed run (admission NO @ 88950 + reservation WITH_CONDITION @ 0, active fee-0 window) passed with an INFO while the refunded 88950 was exactly the NO-flagged admission's value. New gate (after all-NO, before the window checks; skipped under overrule; guarded to one currency+scale across compared amounts): refundableAmount + refundFee must not exceed the summed price of parts with refundable != NO — excess -> failing decoded row 'the refund returns 88950 EUR but the parts permitting a refund carry only 0 — the value comes from part(s) declaring refundable=NO... The provider pays out value its own flag declares locked.' Dossier impact: zeroing retracted; the refundable=NO contradiction SHARPENS (engine pays out flag-locked value).",
        "Verification: harness over the REAL extracted functions with VERBATIM payloads — (a) Marcel's screenshot offer/booking (two REFUND windows, +02:00 vs Z, fees 0/88950 both sides): ZERO failing rows, both windows 'paired by validity window'; reversed booking order still green; GENUINE zeroing (booking window[1] really 0) -> exactly ONE row blaming the right window; fewer booking conditions -> one re-use WARNING; divergent windows -> 'schedules differ' nuance with fees still compared. (b) The tester's release-167 run (admission NO @ 88950 + reservation WITH_CONDITION @ 0, refund 88950 fee 0): ONE failing decoded row; YES-part covering the value -> green; refund within permitted siblings -> green + OK DEBUG; mixed currencies -> gate skipped with DEBUG; all-NO branch intact; overrule still silent. node --check x2; eslint + inline-script lint green. Server 1.11.139 -> 1.11.140; collection OTST_V2.0.81 -> OTST_V2.0.82."
      ]
    },
    {
      "release": "2026.167",
      "date": "2026-06-12",
      "server": "1.11.139",
      "collection": "OTST_V2.0.81",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Refundability verdict & report readability (#387), from Patrick's review of the NHF_RFND assertions. (1) ONE defect = ONE row: the refundable/exchangeable scenario-vs-offer verdict had THREE overlapping validators (validateSelectedOfferAdmission 'All admissionOfferParts ... are refundable', validateAdmissions per-part 'Refundable should be YES', ensureYesWhenRefundOrExchangeSelected) — the last now OWNS the row; the other two demote to DEBUG context. Provider-fairness: WITH_CONDITION now PASSES with an INFO (refund/exchange under conditions is legitimate; the after-sales step verifies the window); only NO/absent fails, decoded ('OSCAR continues so the refund leg documents what the provider actually does'). R9 WARNING when the flag contradicts the part's own conditions (refundable=NO yet a REFUND fee schedule present — observed on OBB). (2) NEW refund permissibility cross-check (refunds.js validateRefundPermissibility, called once at '10. POST Refund Offers'; skipped when an overruleCode bypasses policy): all touched parts refundable=NO → failing decoded row (the OBB case: full refund fee 0 PROPOSED on refundable=NO); WITH_CONDITION → an ACTIVE declared REFUND window must exist (none → WARNING 'granted outside its own declared windows'); when exactly one distinct active-window fee exists, compare with the refund offer's fee (mismatch → WARNING 'the engine and the declared schedule disagree'). (3) Report UI (run-detail assertions): the decoded failure message used to be clipped to 60 chars in a 180px right-hand column — now a full-width wrapped line under the failing row (.assertion-msg, pre-wrap, nothing hidden).",
        "Verification: harness over the REAL extracted functions — ensureYesWhenRefundOrExchangeSelected on Patrick's exact OBB shape (refundable=NO + REFUND schedule → ONE decoded row + contradiction WARNING; WITH_CONDITION → pass + INFO; YES → pass; EXCHANGE mirror; duplicates textually gone from offers.js); validateRefundPermissibility 6 cases (all-NO + schedule → decoded fail incl. 'contradicting their own flag'; WITH_CONDITION active window fee-match → INFO only; no active window → WARNING; fee 500 vs 0 → schedule-vs-engine WARNING; overrule → silent skip; YES → OK line). Report UI structural (truncation gone, .assertion-msg full-width pre-wrap present). node --check x2; eslint + inline-script lint green. Server 1.11.138 -> 1.11.139; collection OTST_V2.0.80 -> OTST_V2.0.81."
      ]
    },
    {
      "release": "2026.166",
      "date": "2026-06-12",
      "server": "1.11.138",
      "collection": "OTST_V2.0.80",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Expired-flow queue resilience (#385). Tester finding: all NHF armed on an OBB REFUND scenario; queue declared '3 sub-runs (OTO, BTO, RTO)'; the run degenerated into a single happy pass. Root causes: (1) the skip path (expiredOfferTest over budget — OBB part validUntil = TRAVEL date, 11 days away) only disarmed the timer; advanceExpiredFlowQueueOrFinish runs only on a GRADED timer, so BTO/RTO (individually feasible, ~15-16 min deadlines) never got their pass; (2) wrong deadline clock; (3) probes stood down for a timer that never actually waited; (4) RI probe armed but silently inapplicable on OBB's unmapped vocabulary. Fixes: (1) advanceExpiredFlowQueueAfterSkip() — every scenario-complete tail (14 ymls) consults the queue BEFORE the next-scenario/stop logic; detection via the per-timer __<flag>Armed marker ('true' = graded → no-op; 'false' = skipped at step; unset = step never fired → both advance, arm next, route to 01); tally '[INFO] NHF expired-flow queue finished: G of N graded, S skipped' whenever skips occurred. (2) captureExpiredOfferDeadline(): earliest of part validUntil (spec: USE validity, 'Time until the offer can be used, e.g. travel') and Offer.preBookableUntil ('time until the offer can be pre-booked') — Bileto hold windows keep working, OBB gets the real purchasability gate. (3) 02 yml: expired-offer plan/skip decided BEFORE buildBookingRequest + placeProbeCurrent honours __expiredOfferArmed !== 'false' → a skipped OTO frees the step for the 🧪 sweep in the same pass; the guard also stops probe-pass self-loops from re-planning. (4) processRequestedInformation: WARNING when requestedInformationProbe is armed but the RI vocabulary is unmapped ('Probe inert for this scenario'). Operational note for OBB: set Max wait ~20 min on expired-booking / expired-refund-offer (deadlines ~15-16 min; empty Max wait = 10-min server budget → skip).",
        "Verification: harness over the REAL extracted functions — queue machine 7 states incl. the exact OBB replay (OTO skipped → BTO armed + routed to 01 → BTO route-miss decoded 'never fired' → RTO graded → tally '1 of 3 graded, 2 skipped'; all-skip tally; exhausted-index silent; clean run silent); captureExpiredOfferDeadline 4 shapes (OBB → preBookableUntil wins; Bileto → part hold kept; part earlier than preBookableUntil → earliest wins; no clocks → empty + DEBUG); probe slot 3 cases (skipped timer → probes run; armed → stand down; undecided → conservative stand-down). node --check x5; eslint + inline-script lint green. Server 1.11.137 -> 1.11.138; collection OTST_V2.0.79 -> OTST_V2.0.80."
      ]
    },
    {
      "release": "2026.165",
      "date": "2026-06-12",
      "server": "1.11.137",
      "collection": "OTST_V2.0.79",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Booking validation accuracy round (#383), from Patrick's audit of the OBB SALE_WIEN_HAMBURG_1LEG_2ADT_3CHD_5BERTH run. (1) Option B re-read dedupe (approved): the offer<->booking afterSaleFee mismatch family registers ONE failing row per root cause, at create-booking; the 05/07 booking re-reads seeing the IDENTICAL part/condition/values log '[WARNING] defect already recorded at create-booking — still present at this read' instead of new rows; a CHANGED value at a re-read is a NEW finding and fails normally (state-transition coverage kept); passing confirmations stay. 6 rows -> 2 on the OBB zeroed-fee defect (offer 87950 -> booking 0; admission + reservation stay separate — two payload objects to fix). Keys in __bookingFindingKeys, reset per scenario (parser + opencollection delete-lists). (2) appliedPassengerTypes 1:1 consumption matcher: old find()-by-type compared the FIRST same-type booking entry N times (log showed PAX1, PAX1, 00003, 00003, 00003 — OSCAR's matcher, not OBB collapsing passengers). Order now: exact passengerRef match (providers keeping request refs) -> first UNCONSUMED same-type entry (UUID sandboxes) -> re-use with one R9 'fewer entries than the offer' note. New R9 nuance when booking entries share passengerRefs (per-passenger entitlements indistinguishable). Ref-matched entries now catch real type mismatches the old matcher could never see. (3) 🎯 goal MET line names the allocated places (coach X place Y; defensive over coachNumber/coach + placeNumber/number/place) and warns (R9) when reservedPlaces count < party size — legitimate for multi-passenger compartments (e.g. 3 DOUBLE_WC places for 5 pax), under-allocation otherwise; the list tells which. (4) FIX one-failure-one-row: 03/04/05/07 ymls registered the HTTP-status test TWICE on non-200 (normal + failure-branch duplicate before failStepOrContinue/loopbackOrStop). Follow-up probe noted in #383: REFUND with refund date after departure discriminates whether the refund engine reads the offer schedule (echo cosmetic) or the booking echo (money bug — free refund after travel).",
        "Verification: harness over the REAL extracted functions — dedupe (02 mismatch -> 1 decoded row + key; 05 same values -> 0 rows + WARNING, passing confirmations kept; 07 changed value -> NEW failing row; create step never softens, e.g. return-inbound 02). Matcher (OBB-style same-namespace refs -> all 1:1, '00002 matched 1:1' not PAX1-collapse, zero warnings; UUID sandbox -> consumption fallback u1/u2 distinct; fewer-entries booking -> lenient + ONE reuse warning; collapsed refs -> 'only 1 distinct passengerRef(s) across 5' nuance). Goal (3 places/5 pax -> MET line lists coach/place + party WARNING; 5/5 -> silent). Yml sweep: exactly one status-test registration left in each of the 4 files. node --check x2; eslint + inline-script lint green. Server 1.11.136 -> 1.11.137; collection OTST_V2.0.78 -> OTST_V2.0.79."
      ]
    },
    {
      "release": "2026.164",
      "date": "2026-06-12",
      "server": "1.11.136",
      "collection": "OTST_V2.0.78",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "NJ conformance PR3 (#379) — reservation spec-coverage depth, closing the 3-PR programme (#377 goal chain, #378 NHF sweep). (1) 🧭 offer-set orientation: one INFO line before any filter (families x offers, subtypes, no-reservation count) so the certifier reads the provider's hand at a glance. (2) Coverage referential integrity (R8): selected offer's tripCoverage (both forms, via offerTripCoverage) must reference trips/legs existing in the SAME OfferCollectionResponse — decoded failure when broken ('coverage references legId L9 which is not a leg of trip T1'), DEBUG when OK or not applicable. (3) Place-selection capability notes (R9, never fail) on the CHOSEN part: supportedPlaceSelectionFlows + availablePlacePreferences.graphicalReservation related to the scenario's seat-selection mode — seat-map mode vs automatic-only flows → WARNING; manual-only flows on a no-map scenario → INFO (spec expects /availabilities/vehicle-place-map before /bookings); graphicalReservation 'NO' under a map mode → WARNING; unknown flow values → x-extensible-enum note. (4) Capacity nuance: part-/place-level numericAvailability below the requested party → one WARNING. (5) FAIRNESS FIX: the hard oneOf [SEAT, COUCHETTE, BERTH, VEHICLE, STORAGE] on availablePlaces.accommodationType contradicted the spec (x-extensible-enum — custom codes are legal) and would have FAILED any provider extension; membership is now an R9 WARNING, structural checks stay hard; accommodationSubType (open code list) has no membership check.",
        "Verification: harness over the REAL extracted functions — validateReservations (HAMMOCK place: zero failing assertions + enum WARNING + capacity WARNING listing part- and place-level; clean BERTH part: zero warnings), assertOfferCoverageIntegrity (OK→R8-silent DEBUG; bad leg / missing trip → one decoded failure each; no coverage → skip), notePlaceSelectionCapabilities (6 cases incl. TELEPATHY x-ext note and graphicalReservation NO), 🧭 orientation line content over a 3-offer set, and the BERTH selection path regression (reservationId/selectedAccommodation/tripLegCoverage persisted + flows INFO, all green). node --check; eslint + inline-script lint green. Server 1.11.135 -> 1.11.136; collection OTST_V2.0.77 -> OTST_V2.0.78. NOTE: stacked on 2026.163 (#378/PR #381) which stacks on 2026.162 (#377/PR #380) — merge in order #380 -> #381 -> this."
      ]
    },
    {
      "release": "2026.163",
      "date": "2026-06-12",
      "server": "1.11.135",
      "collection": "OTST_V2.0.77",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        [
          "NJ conformance PR2 (#378) — place-selection Non-Happy-Flow probe sweep. Patrick's principle: 'each time we implement a new feature we must think about the type of Non Happy Flow we can implement'; design per his preference (one scenario sweeping all probes, purchaser-sweep #258 precedent). Three probes, run INSIDE '02. POST Create Booking' via self-loop (one corrupted POST per probe, then a final CLEAN pass; a rejected probe consumes nothing provider-side): (1) omitPlaceSelections — rejection expected; 2xx WITH placeAllocation → WARNING (OSDM-tolerated auto-allocation, placeSelections is optional in BookingRequest); 2xx with NO allocation → FAIL (ambiguous booking). (2) unknownAccommodationType — requests 'HAMMOCK' (x-extensible-enum); 400+Problem recommended; 2xx → WARNING (tolerant reader; FAIL if HAMMOCK comes back allocated); 5xx → FAIL. (3) wrongReservationId — references a reservation part existing in no offer; 2xx → FAIL (referential integrity). Each pass: 🧪 [WARNING] banner + one outcome row; rejections graded for RFC-9457 shape (4xx not 5xx, Problem body, field identification) via validateProblemResponse with the new optional prefix param (existing call sites byte-identical). Wrongly-accepted probe → sweep stops, run continues WITH that booking (it flows into the normal post-booking steps incl. the #377 🎯 goal chain). Auth 401/403 → WARNING only, not graded (our problem, not a provider verdict). Auto-skips with one WARNING: no placeSelections to corrupt, two-step return scenarios, expiredOfferTest armed. Wizard: NHF customisation gains the '🪑 Place-selection probes' sub-group (3 pill toggles, dashed-border boxes, armed badges roll into the NHF badge). Pre-flight self-check (#377) is skipped on corrupted passes (deliberate corruption) and runs on the clean pass."
        ],
        "Verification: integration harness over the REAL modules (placeProbes.js + requestsBuilder.js + requestedInformation.js chain under a stubbed Bruno runtime) — gating (off/finished/expired-owned/return-owned/warn-once), corruption (omit/HAMMOCK/fake-id + no-selections auto-skip ends sweep), reject grading (400→next-probe with prefixed N1/N2 rows green, 422 last→clean-pass, 500→decoded FAIL, 401→advance with NO assertions), accept grading (omit+auto-allocation→WARN row only; omit w/o allocation→decoded FAIL; HAMMOCK ignored→tolerant WARN row; HAMMOCK echoed→FAIL; wrong-id→FAIL; sweep stop + index pinned), and a FULL 3-probe sweep through the real buildBookingRequest (pass1 omit + pre-flight skipped, pass2 HAMMOCK, pass3 fake id, clean pass body intact + pre-flight R8-silent green). node --check x4 + wizard Function-parse; eslint + inline-script lint green. Server 1.11.134 -> 1.11.135; collection OTST_V2.0.76 -> OTST_V2.0.77. NOTE: stacked on release 2026.162 (#377/PR #380) — merge that PR first."
      ]
    },
    {
      "release": "2026.162",
      "date": "2026-06-12",
      "server": "1.11.134",
      "collection": "OTST_V2.0.76",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        [
          "NJ conformance PR1 (#377) — accommodation goal-closing chain. Tester finding: nowhere did the run state that the BERTH requested by the scenario was what actually got booked. (1) 🎯 goal assertions at the create-booking step (gated to '02. POST Create Booking' so booking re-reads don't duplicate the rows): the booking must carry the selected reservation part; when the booked Reservation echoes placeAllocation (OSDM-required members when present: accommodationType, accommodationSubType, reservedPlaces, tripLegCoverage) the allocation must match the requested family + the offered compartment subtype, reservedPlaces non-empty, coverage matching the selection; absent placeAllocation → one [WARNING] capability note (no failure); one '🎯 Accommodation goal MET: requested BERTH → offer advertised BERTH/SINGLE_SWC (part …) → booking allocated …' INFO line. (2) Pre-flight self-check on OUR booking request (R8 — assertion registered ONLY on failure, one DEBUG when clean): reservationId ∈ selected offer parts, accommodations ⊆ availablePlaces (type and subtype), tripLegCoverage within the offer coverage. (3) FIX: offerTripCoverage now also maps the spec's TripCoverage form (coveredTripId + coveredLegIds) — what providers actually send (seen on OBB); the #371 flat-form-only mapping silently never fired and always fell back to per-place coverage. Part of the #377/#378/#379 NJ conformance programme (PR2: NHF place-selection probe sweep; PR3: reservation spec-coverage)."
        ],
        "Verification: harness over the REAL extracted functions — offerTripCoverage maps both coverage forms; validateAccommodationGoal 5 cases (full allocation → 5 green asserts + MET line; family mismatch decoded 'the provider changed the accommodation family'; no placeAllocation → WARNING + no failure; reservation dropped → decoded failure; no accommodationSelection → fully silent); assertPlaceSelectionConsistency 4 cases (consistent → R8-silent DEBUG; wrong reservationId / foreign coverage (against the covered* form) / unadvertised HAMMOCK type → one failing decoded assertion each). node --check x3; eslint + inline-script lint green. Server 1.11.133 -> 1.11.134; Bruno_Collection/VERSION OTST_V2.0.75 -> OTST_V2.0.76."
      ]
    },
    {
      "release": "2026.161",
      "date": "2026-06-11",
      "server": "1.11.133",
      "collection": "OTST_V2.0.75",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        [
          "Lifecycle-aware booking price assertions + decodable afterSaleFee mismatch (#375) — found on the first successful OBB NJ/BERTH end-to-end run. (1) postCreateBookingResponse asserted BOTH provisionalPrice AND confirmedPrice at every stage, but OSDM scopes them to the lifecycle (provisional pre-confirmation, confirmed once FULFILLED/CONFIRMED): every conformant provider got 2 false ✕ per booking read (PREBOOKED lacks confirmedPrice; post-fulfillment lacks provisionalPrice) and the combined field check crashed with a raw 'Cannot read properties of undefined' TypeError. Now keyed on the expectedBookedOffersStatus parameter the function already receives: the stage member is required with a decoded failure ('provisionalPrice missing — OSDM expects it at this booking stage (PREBOOKED)'), the other member's presence/absence is logged as allowed, no TypeError path remains, and provisionalPriceAmount/confirmedPriceAmount env vars (read by refund maths) are set per-presence — previously only when both existed, i.e. never on conformant data. (2) The afterSalesConditions fee comparison emits a provider-language message ('Booking does not echo the offer's REFUND fee amount: the offer said 60990, the booking says 0 — the booking must mirror the offer's after-sales conditions') instead of the chai 'expected +0 to deeply equal 60990' tail (a #349 leftover). The OBB fee mismatch itself is a genuine provider finding and stays red."
        ],
        "Verification: harness drives the REAL extracted price block — PREBOOKED with only provisionalPrice all-green (absence of confirmedPrice logged as allowed, env set per presence); FULFILLED with only confirmedPrice all-green; genuinely missing stage member → decoded failure with NO TypeError; missing currency field named decodably; both-present tolerated with both envs set. node --check; eslint + inline-script lint green. Server 1.11.132 -> 1.11.133; Bruno_Collection/VERSION OTST_V2.0.74 -> OTST_V2.0.75. Assertion counts: 2 false failures per booking read disappear on conformant providers."
      ]
    },
    {
      "release": "2026.160",
      "date": "2026-06-11",
      "server": "1.11.132",
      "collection": "OTST_V2.0.74",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Wizard Accommodation type picker (#373) — Test Manager configuring a night-train scenario could not find WHERE to choose the reservation type: scenarioParser has always mapped scenario.accommodationSelection to the Bruno env (offer filtering by place family + the #371 booking placeSelections key on it) but the wizard never rendered a field for it, so the IRT/NJ compartment choice was only reachable via hand-edited datafiles. New single-select pill row in Booking Flow Actions next to the Seat-selection mode block (exactly where the tester looked): '— any —' (default, wire-identical) / Seat / Couchette / Berth, with explanatory tooltips. Deliberately NOT gated behind the framework's place-selection authorisation — for IRT/NJ the accommodation drives offer selection and the booking placeSelections even without the graphical seat map. Server display-only (scenarios.js); collection unchanged (OTST_V2.0.74).",
        "Verification: node --check + eslint + inline-script lint green; pill handler mirrors the existing set-place-mode single-select pattern ('' clears the field to null → default behaviour); scenario field round-trips through the datafile to the existing scenarioParser mapping (no Bruno change needed)."
      ]
    },
    {
      "release": "2026.159",
      "date": "2026-06-11",
      "server": "1.11.131",
      "collection": "OTST_V2.0.74",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        [
          "Booking placeSelections for IRT/NJ mandatory-reservation offers (#371, OBB requirement relayed by the Test Manager). OBB returns one offer per bookable compartment (1 ReservationOfferPart each) and validates that the booking request states the compartment via placeSelections: reservationId = reservationOfferParts[].id; accommodations[].accommodationType/SubType from reservationOfferParts[].availablePlaces (e.g. COUCHETTE/COUCHETTE_COMFORT_4) with the booking passengerRefs; tripLegCoverage from offer.tripCoverage. Implementation reuses the existing place-selection pipeline: offers.js persists the selected part's real accommodation (selectedAccommodation env) on BOTH the seat and couchette/berth branches and getTripLegCoverage/the seat branch now prefer offer.tripCoverage (object or array) over per-place tripLegCoverage (kept as fallback); requestsBuilder.accommodationAndPlaceSelection emits the real accommodations when selectedAccommodation is present (gate extended so BERTH/IRT produce placeSelections), keeps the legacy hardcoded COUCHETTE shape (ANY_SEAT + placeProperties[MEN]) when the offer carries no availablePlaces detail, and stays wire-identical ([]) for scenarios without accommodation/place selection. selectedAccommodation added to both env delete-lists (scenarioParser + opencollection)."
        ],
        "Verification: harness drives the REAL extracted functions with OBB's example bookingRequest values — the built placeSelections deep-equals the example shape (reservationId Qtr7M_..., COUCHETTE/COUCHETTE_COMFORT_4 with passengerRefs ['0'], tripLegCoverage tripId/legId, no placeProperties); offer.tripCoverage preferred with per-place fallback intact (object + array forms, junk filtered); legacy COUCHETTE shape byte-identical without selectedAccommodation; BERTH now covered; no-selection scenarios emit []. node --check x3; eslint + inline-script lint green. Server 1.11.130 -> 1.11.131; Bruno_Collection/VERSION OTST_V2.0.73 -> OTST_V2.0.74."
      ]
    },
    {
      "release": "2026.158",
      "date": "2026-06-11",
      "server": "1.11.130",
      "collection": "OTST_V2.0.73",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Offer-probe follow-up (#369) — (1) NEW '🔄 Re-probe offers' button in the Test Data step: POST /v1/company/test-resources/reprobe-offers re-runs the anonymous-adult offer probe (3 dates via searchDates) for every distinct O&D across the TRAIN resources and refreshes data.offerProbe on the affected sets (no timetable harvest, no other train mutation); toast summarises routes/sets/findings; manual for now (automatic refresh later). (2) FIX (Test Manager's 'doubt without evidence' confirmed by audit): _collectAncillaries preferred AncillaryOfferPart.type — the offer-part DISCRIMINATOR ('AncillaryOfferPart') — over category (the actual ancillary kind), so the Service Configuration ancillary catalog could be seeded with a useless literal; now category-first and /offerpart$/i type values are never collected. travelClass/serviceClass collection audited clean. summarizeOfferProbe factored into the timetable-discovery service and shared by both routes.",
        "Verification: summarizeOfferProbe harness (null at 0 probed days; no-offer finding keeps the provider echo; 2-of-3 days + only-SECOND + only-NON_FLEXIBLE produce 3 findings); node --check on service/route/wizard; eslint + inline-script lint green. Server 1.11.129 -> 1.11.130; collection unchanged (OTST_V2.0.73)."
      ]
    },
    {
      "release": "2026.157",
      "date": "2026-06-11",
      "server": "1.11.129",
      "collection": "OTST_V2.0.73",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Timetable Discovery offer probe (#365) — a discovered route does not guarantee offers. classifyOfferProbe (timetable-discovery service) classifies each offers response the discovery already receives (anonymous 1-adult): no offers (with the provider warnings/problems echo when present), travel classes seen, flexibilities seen. The discovery route aggregates across probed days and persists data.offerProbe {probedAt, daysProbed, daysWithOffers, classes, flexibilities, findings[]} on EVERY train set of the searched O&D (created and updated), and returns it in the response. Findings: 'No offer for an anonymous adult on any of the N probed day(s)' / 'Offers only in SECOND class (no FIRST)' / 'Offers only NON-FLEXIBLE (Desired Flexibility will not match)' / 'Offers on X of Y probed day(s)'. Wizard Test Data step: collapsible warnings panel above the train list (count + per-train findings + probe date) and a per-train warning chip with hover details, read from RAW resource data (normalizeTrainData strips unknown members). trips-collection responses (no offers[]) are not counted as probed days; findings refresh on the next Discover timetable of the route. Note: manually re-saving a train from the editor drops offerProbe until the next discovery — acceptable v1, findings are advisory.",
        "Verification: classifyOfferProbe harness (trips-collection → null; empty offers without explanation; empty offers with problem echo; classes/flex dedup + uppercase across offerSummary and flat fields); node --check on service/route/scenarios.js; eslint + inline-script lint green. Server 1.11.128 -> 1.11.129; collection unchanged (OTST_V2.0.73). No change to the discovery wire calls — the probe reuses the responses already fetched."
      ]
    },
    {
      "release": "2026.156",
      "date": "2026-06-11",
      "server": "1.11.128",
      "collection": "OTST_V2.0.73",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "UX follow-up (#366) on the 2026.155 round — tester feedback: (1) the Departure day select was hard to find next to Trip Type; moved into the trip grid immediately before the Departure time field on SEARCH trips (trip-level above the legs on SPECIFICATION — one date covers all legs); (2) toasts were still missed at the top and read smaller than the old alert(); now positioned in the upper third of the viewport overlapping the page content, larger text (14.5px) and padding, min-width 340px, stronger shadow, 180ms entrance animation. Server display-only (scenarios.js + nav.js); collection unchanged (OTST_V2.0.73). Also filed #365: extend Timetable Discovery to probe offer availability per discovered route (anonymous 1-adult POST /offers) and surface per-route warnings (no offer / only Second class / only Non-flex …) behind a warning icon on the Test Data section title — tracked, not yet implemented."
      ]
    },
    {
      "release": "2026.155",
      "date": "2026-06-11",
      "server": "1.11.127",
      "collection": "OTST_V2.0.73",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Run-setup UX round (#363) — three Test Manager findings on 2026.154. (1) Departure day per trip: wizard select next to Trip Type — Auto (default, today + departureDateFromToday lead time, unchanged) or Monday…Sunday for trains that only run certain days; the parser keeps the lead time (aftersales buffer) and advances 0–6 days to the next matching weekday (resolveTripDateForWeekday, noon-anchored against DST), one '[INFO] 📅 Departure day …' line states the resolved date; applies to SEARCH trips and SPECIFICATION legs (one date per trip); weekday chosen over a hard-coded calendar date per the Test Manager's own rationale (a weekday stays valid forever, a date expires). (2) Placeholder-vs-value confusion: wizard-wide ::placeholder styling (lighter + italic, scenarios.css) + automatic 'e.g. ' prefix on trip-field proposals. (3) All 27 native alert() popups (run.html, dashboard.html, run-detail.html, scenarios.js) replaced by in-page toasts rendered inside the OSCAR UI (top-centre under the nav, auto-dismiss 7s / errors 12s, dismissable, info/success/warning/error styling; oscarToast/oscarToastAfterNav in nav.js; submit→redirect messages survive navigation via sessionStorage shown on the next page; confirm() decision dialogs stay native).",
        "Verification: Node harness over the real resolveTripDateForWeekday (7 cases: Wed→Sat +3, same-day no shift, week wrap + case-insensitive, Auto/null/unknown → base unchanged, month rollover, INFO line content); zero bare alert( left in the four files; node --check on scenarioParser.js/scenarios.js/nav.js; eslint + inline-script lint green. Server 1.11.126 -> 1.11.127; Bruno_Collection/VERSION OTST_V2.0.72 -> OTST_V2.0.73. Wire format unchanged (departure date string only moves to a different valid day when a weekday is chosen)."
      ]
    },
    {
      "release": "2026.154",
      "date": "2026-06-10",
      "server": "1.11.126",
      "collection": "OTST_V2.0.72",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Step-failure policy (#361) — tester finding on an OBB run: a failed GET /bookings/{id}/passengers abandoned the scenario (loopbackOrStop's only branches are next-scenario or end-run), costing fulfillment/post-fulfillment coverage. New scenario parameter STEP FAILURE POLICY (wizard, next to Logging Level): HARD_STOP (default — historical behaviour, existing datafiles unchanged) vs CONTINUE (failure stays recorded with red assertions and a FAILED scenario verdict; one '[WARNING] <step> failed — step-failure policy CONTINUE: proceeding to \"<next step>\"' line; flow routes to the same successor as the success path, incl. the deferred-purchaser routing in 04). One central helper failStepOrContinue(label, nextStep, {critical}) in loopback.js; offer/booking failures always critical (hard regardless of policy); 03 PATCH Multi Passenger + 04 GET Passenger policy-controlled; fulfillment stays hard in v1; the malformed-body '⛔ Exiting script' throws in 03/04 (which killed the script before any routing) became a registered failing assertion + the same policy call. scenarioParser sets the stepFailurePolicy env at scenario start (default HARD_STOP, case-insensitive) and both env delete-lists (scenarioParser + opencollection) stay in sync. Purchaser steps 12–14: phase 2.",
        "Verification: Node harness over the REAL failStepOrContinue — 6 cases (default hard-stops via loopback; CONTINUE routes onward with exactly one warning; critical ignores CONTINUE; missing nextStep falls back hard; last-scenario hard-stop calls stopExecution; lowercase policy value accepted). Both rewritten after-response scripts extracted from the ymls, syntax-checked, asserted policy-wired with the bare throws removed. node --check on loopback.js/scenarioParser.js/scenarios.js; eslint + inline-script lint green. Server 1.11.125 -> 1.11.126; Bruno_Collection/VERSION OTST_V2.0.71 -> OTST_V2.0.72. Wire format unchanged; behaviour unchanged unless a scenario opts into CONTINUE."
      ]
    },
    {
      "release": "2026.153",
      "date": "2026-06-10",
      "server": "1.11.125",
      "collection": "OTST_V2.0.71",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "OSDM Trip Search Criteria v1 (#359, requested by Marcel Koseler / ÖBB PV AG). Wizard: collapsed '🔎 Trip Search Criteria' sub-panel in the Trip requirement (SEARCH type) with an 'N set' badge — search time basis (Departure default / Arrival, which searches by the existing Arrival field via OSDM arrivalTime), Via 1/2 (UIC + optional ISO dwellTime), Not-via (comma-separated UICs), Transfer limit, Number of results/before/after, Ignore realtime data; stored flat under tripRequirement.trip.searchCriteria.*; setTripFieldByPath autovivifies missing sub-objects. scenarioParser builds the OSDM members: arrivalTime replaces departureTime on ARRIVAL basis (LocalDateTime convention + Bileto OffsetDateTime exception preserved; missing Arrival → WARNING + departure fallback); vias[] {viaPlace, dwellTime}; notVias[] {notViaPlace[]}; parameters gains transferLimit/numberOfResults(/Before/After)/ignoreRealtimeData merged with the train-binding dataFilter; paxone keeps its no-parameters exception (top-level members still sent, one INFO note); one '[INFO] 🔎 Trip search criteria applied — …' summary line. Wire shape byte-identical when nothing is configured. Out of v1 scope (in #359): per-via dataFilters, ptMode/serviceBrand filters, policy/mobility filters, embed.",
        "Verification: Node harness drives the REAL osdmTripSearchCriteria extracted from scenarioParser with the REAL bundled OSDM 3.5 spec as key-allowlist — 6 cases: legacy shape unchanged when unset; full criteria (arrival basis drops departureTime, 2 vias incl. dwell, notVia comma-split, params merged with vehicleFilter, every object allowlisted against TripSearchCriteria/TripVia/NotVia/TripParameters spec properties); paxone parameter suppression with vias kept; ARRIVAL-without-Arrival fallback + WARNING; junk numeric ignored + WARNING; Bileto OffsetDateTime kept on arrivalTime. node --check on scenarios.js + scenarioParser.js; eslint + inline-script lint green. Server 1.11.124 -> 1.11.125; Bruno_Collection/VERSION OTST_V2.0.70 -> OTST_V2.0.71."
      ]
    },
    {
      "release": "2026.152",
      "date": "2026-06-10",
      "server": "1.11.124",
      "collection": "OTST_V2.0.70",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "reportGenerator [DEBUG] gate (#357) — tester on 2026.151 with a loggingType=INFO scenario still saw grey [DEBUG] lines: reportGenerator.js printed three ROUTINE lines per scenario via direct console.log ('↩ Same scenario detected…', '🗑 Previous run data cleared.', '✅ Report directory: …') plus three error-path diagnostics, all bypassing the validationLogger pyramid. The file is sandbox-self-contained (cannot require displays.js) → local _debugLog() gate, suppressed unless loggingType=DEBUG/FULL (no-bru context defaults to INFO/silent). offers.js's two error-path preflight diagnostics route through validationLogger. The ~15 'globalThis exposure skipped' catch-fallbacks fire only on exceptions and stay as-is. Documented boundary: Bruno-CLI-emitted lines ('(request skipped via pre-request script)' echoes, post-failure stack frames) are CLASSIFIED debug by the runner and cannot be un-emitted by loggingType — the dashboard info+ filter hides them; the DB keeps everything for forensics.",
        "Verification: _debugLog extracted from the real file and exercised (INFO suppressed, DEBUG printed, no-bru silent); node --check on both files; eslint + inline-script lint green. Server 1.11.123 -> 1.11.124 (version pairing only, no server code change); Bruno_Collection/VERSION OTST_V2.0.69 -> OTST_V2.0.70."
      ]
    },
    {
      "release": "2026.151",
      "date": "2026-06-10",
      "server": "1.11.123",
      "collection": "OTST_V2.0.69",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Execution Log & System-Info polish round (#355) — four tester findings on the first 2026.150 run, bundled at the Test Manager's request. (1) Section headers no longer position:sticky — the open section's header (📂 Runner) pinned to the top of the log box while its lines scrolled underneath (read as a stuck display, overlapped the first visible line); headers scroll naturally now. (2) Benerail token-skip line landed in the Runner section: the library's token-skip fires in the PRE-request script, before the Bruno CLI row that opens 00-Access Token — LogParser now attributes 'Skipping [x access token]' lines to 00-Access Token by construction; scope-gate [INFO] skips keep their current suite. (3) [DEBUG] lines still appeared at loggingType=INFO: two direct console.log sites bypassed the validationLogger pyramid (token skips + per-request 'Report updated →' in opencollection.yml) — both now gated on the dataset loggingType (not emitted at INFO). (4) GET Coach By Id + GET Product By ProductId: a 501 produced an [ERROR] prerequisite-failed cascade (+failing assertion + stack frames) because the list-prerequisite was checked BEFORE the status; restructured so the shared handleSystemInfoStatus (#353) runs FIRST (501/unsupported → ONE skip line; out-of-version 404 → skip; auth/4xx/5xx → decoded failures, replacing the expect(...).to.eql(200) chains — 10 fewer chai-tail variants from the #349 list); the prerequisite hard-fail becomes cascade-kill (ONE '[INFO] Context: …' line pointing at the root cause, no extra failing assertion); on a 200 the body is validated regardless of how the id was obtained. (5) Empty-offers warning printed TWICE with a false '0 passenger(s)': postOfferResponse logged the full [WARNING] and then threw the same message, which the caller re-throws inside bruTest('Offers found in response', …) whose failure echo printed it again verbatim — standalone log removed (the echo owns the message; same for the malformed-envelope [ERROR] twin); the passenger count read only jsonData.passengers while providers echo under anonymousPassengerSpecifications (OBB) or passengersList — all three checked, and an absent echo now reads 'no echoed passenger list' instead of claiming zero.",
        "Verification: Node harness over the real LogParser (benerail skip → 00-Access Token, scope-gate skip keeps suite, CLI rows unaffected); both edited after-response scripts extracted from the ymls, syntax-checked via new Function and structure-asserted (shared handler first, hard-fail prerequisite gone, context after status); eslint + inline-script lint green; #353's classify/handle harness still covers the 501/skip semantics. Server 1.11.122 -> 1.11.123; Bruno_Collection/VERSION OTST_V2.0.68 -> OTST_V2.0.69. Assertion counts drop further on providers with unimplemented By-Id endpoints (no more prerequisite/cascade failures)."
      ]
    },
    {
      "release": "2026.150",
      "date": "2026-06-10",
      "server": "1.11.122",
      "collection": "OTST_V2.0.68",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "System-Info 'not supported' = SKIP + LogParser section fix (#353) — tester question on 2026.149: 'why for GET passenger-categories do we not skip like for the other ones?'. (1) classifySystemInfoStatus: the provider-declares-unsupported branch (HTTP 501, or Problem code OPERATION_NOT_PERMITTED/NOT_IMPLEMENTED/NOT_SUPPORTED) flips outcome 'fail' → 'skip', matching the out-of-version handling — a clean 501 is the OSDM-conformant signal for an unimplemented OPTIONAL endpoint, so no assertion is registered (the old code SAID 'remaining checks are skipped' but registered a failing test with red ✕ + [ERROR] + ~10 stack frames). Right signal (501/404) → one [INFO] 'not implemented by this provider — endpoint out of scope, skipped'; wrong signal (e.g. 400+OPERATION_NOT_PERMITTED) → one [WARNING] with the conformance note (OSDM expects 501/404). Genuine failures (401/403, plain in-version 404, 5xx) unchanged; all 8 .yml callers use the boolean return so behaviour composes. (2) runner LogParser: folder/request matcher accepted any 'text/text (parens)' line — '[DEBUG] 📊 Report updated → /app/…/report.html (39 assertions)' and '✕ GET /passenger-categories → … (HTTP 501 …)' became bogus per-area sections (📂 ✕ GET) stealing subsequent lines. Lines with an explicit [LEVEL] tag are exempt from suite/request detection; assertion markers checked FIRST and now include ✕ (U+2715 — what Bruno actually prints, distinct from ✗) and ✔/×.",
        "Verification: Node harness over the real classify/handle functions (501+code → skip INFO; bare 501 → skip INFO; 400+code → skip WARNING incl. note; 404/401/500 still fail; 200 ok; handle skip path registers 0 assertions, 1 log line, returns false) and over the real LogParser class extracted from runner.js (CLI rows forward+backslash attribute, skip-echo attributes, the two screenshot garbage lines keep the current suite, ✓/✕ classified assertion, [INFO]/[ERROR] narration with slash+parens keeps suite). eslint + inline-script lint green. Bruno_Collection/VERSION OTST_V2.0.67 -> OTST_V2.0.68; server 1.11.121 -> 1.11.122. Assertion counts drop by 1 per unsupported System-Info endpoint (the contradictory failure disappears); no wire-format change."
      ]
    },
    {
      "release": "2026.149",
      "date": "2026-06-10",
      "server": "1.11.121",
      "collection": "OTST_V2.0.67",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Execution Log follow-up (#351) — tester feedback on the first 2026.148 run. (1) Dashboard level filter becomes a PYRAMID matching loggingType semantics: info+ = INFO+WARNING+ERROR (DEBUG hidden), warn+ = WARNING+ERROR, error = errors only, All = everything incl. DEBUG; the buttons were exact-match before (selecting info hid warnings/errors); dead stdout/stderr buttons removed (runner only stores debug/info/warn/error since round 1); unknown levels stay visible (safety net). (2) Log SPLIT per area like the assertions panel: one collapsible sticky-header section per suite in chronological order with line counts; sections compose with level/endpoint/search filters (fully-filtered sections hide, counts show visible/total); streaming lines join the current section; suite re-entry (loop-back) starts a new section preserving chronology. (3) FIX multi-line [DEBUG] dumps leaking into the INFO view: only the first physical line of a multi-line message carried the tag — continuation lines (offerId:…, offerSummary:{…) were level-guessed as info by the runner; validationLogger now propagates the tag to EVERY line (R1), and the Selected-Offer full-object console.log dump is removed outright (R6 — payload replay; the offer is in the HTTP-traffic viewer); the [INFO] id + per-part refundable/exchangeable summary stays, each line explicitly tagged.",
        "Verification: the page's REAL functions (extracted from run-detail.html at test time) run against a DOM shim in Node — 9 checks: pyramid visibility per level incl. the reported case (info shows warn+error, hides debug), section order/counts, empty-section hiding at warn+, endpoint-filter and search composition, streaming continuation, suite re-entry chronology; validationLogger multi-line tag propagation tested for DEBUG/WARNING with single-line messages untouched; eslint + inline-script lint green. Bruno_Collection/VERSION OTST_V2.0.66 -> OTST_V2.0.67; server 1.11.120 -> 1.11.121. Display-only on the server side; Bruno-side log volume drops (~30 untagged dump lines per offer selection removed)."
      ]
    },
    {
      "release": "2026.148",
      "date": "2026-06-10",
      "server": "1.11.120",
      "collection": "OTST_V2.0.66",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Request/response traces (#350) — dashboard self-sufficiency + report completeness. (1) Execution Log gains an 'All endpoints' dropdown: filter the log to one tested endpoint via the request_name attribution the LogParser already writes on run_events, composing with the level buttons and search (mirrors the assertions panel's per-request grouping). (2) HTTP Traffic viewer renders the captured request AND response headers as a collapsed 'Headers (N)' table between meta and body — all headers incl. non-mandatory debug ones (traceparent, tracestate, correlation ids); data was already stored/decrypted by the API, only rendering was missing. (3) Per-scenario AND merged HTML reports add the missing Response Headers panel (request-side existed); capture verified complete on both directions (req.headers / res.headers = every header Bruno sends/receives, explicit + script-set; transport-added Host/Content-Length not exposed by the runtime — documented limitation). (4) Credential masking switches from full '[REDACTED]' to a graduated PARTIAL mask at all three capture sites (structureResults → run_requests, reportGenerator, mergeReport): ≥24 chars keep first 10 + '…[masked N chars]…' + last 4 (Bearer shows scheme + JWT head, tail distinguishes tokens); 8–23 chars keep first 3 + last 2 (identity-style values); shorter stay fully redacted. 'Requestor' (no x- prefix) added to the sensitive sets — it previously reached run_requests unmasked and was only masked at report render time. Render-time maskers pass already-masked values through (no double-masking destroying the tail) while legacy raw data still gets the old render-time mask. Auth-endpoint BODIES stay fully redacted.",
        "Verification: Node harness runs the real capture→render chain extracted from all three files (long bearer keeps head+tail, Requestor keeps 3+2, traceparent/Content-Type untouched, no double-mask, legacy '[REDACTED]' and raw-bearer paths preserved, <8-char secrets fully redacted); renderHeadersBlock tested for sort/escape/count and degenerate inputs (null, non-JSON, empty). eslint + inline-script lint green. Bruno_Collection/VERSION OTST_V2.0.65 -> OTST_V2.0.66; server 1.11.119 -> 1.11.120. No wire-format change; run_requests now stores partially-masked credential headers for NEW runs (old rows keep full redaction marker)."
      ]
    },
    {
      "release": "2026.147",
      "date": "2026-06-10",
      "server": "1.11.119",
      "collection": "OTST_V2.0.65",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Log-audit round 2 — 17 decodability fixes from Patrick's line-by-line tester walkthrough of a Bileto run. Theme: the INFO log tells the flow story a tester can act on; plumbing goes to DEBUG; failures speak the provider's language. Items: (1) vendor token-skip lines → DEBUG with truthful wording (on OSCAR the token is injected server-side; the old 'not for X sandbox' read as a matching bug); (2) OBB Access Token request added to 00-Access Token for standalone Bruno + ca-certificates installed in the Docker image, killing the per-run OpenSSL '/etc/ssl/certs' stderr noise at the source; (3) FIX false '[WARNING] passengersList #N has only 0 passenger(s)' — the check read jsonData.passengersLists (plural) which never exists, so the per-pax partial-refund warning fired on every scenario regardless of the real count; (4) FIX expiredFlow queue require — library_base double-nested the path inside library-bruno/, so the require failed on EVERY run (confusing warning at 0 timers armed; multi-timer auto-expansion silently dead since it shipped); sibling require + [ERROR] escalation on genuine failure; (5) 14 full-JSON [INFO] dumps (passengerSpec/purchaserSpec/exchange bodies/trip internals) → DEBUG, partial-refund 'armed' line split into compact INFO signal + DEBUG payload; (6) 'Correct data set was found' → 'Scenario X loaded from the data file — linked test data resolved (tripRequirement #, passengersList #, fulfillmentOptions #)'; the not-found twin lists available codes + wizard fix path; (7) reportGenerator internals (report directory path, accumulator bookkeeping) → DEBUG; (8) OSDM version mismatch: narrative [INFO]→[WARNING] with action path, inverted expect(foundMatch).to.be.false replaced by an honest soft-check test title, malformed '[✅ INFO]' tag fixed (the match confirmation was invisible at default level), 'sytem' typo; (9) per-request 'Report updated → <container path>' → DEBUG; (10) bruTest pass-echo [INFO]→[DEBUG] — every passing assertion appeared TWICE (library echo + Bruno's native ✓ row); the native row is now the single INFO-level confirmation; failure echo stays [WARNING]; (11) decodable compliance failures: index dumps ('index 0, 1, … 207') → 'required property X is missing or not of type T on ALL 208 CoachDeckLayout entries' / count+10-sample for subsets; deep-schema dumps grouped by distinct problem ('416 schema issue(s) across 208 entries — 2 distinct problem(s): …'); all 28 expect(c.ok, c.message).to.be.true callsites → plain throw (kills chai's ': expected false to be true' tail); (12) env-sanity guards (api_base/library_base/data_base) no longer register 3 passing tests + 3 echoes per request (~120 lines + 63 filler assertions per run): silent check, ONE '[INFO] Environment OK — …' per run, a test only when a variable is missing (with fix path); '200 OK, body received' narratives → count-at-INFO / DEBUG; (13) runner inferLevel: 'at …' stack frames after failed assertions → debug (~10 lines per failure), Error: message lines stay error; (14) classifySystemInfoStatus is Problem-body-aware: 'GET /passenger-categories → not supported by this provider (HTTP 400 + OPERATION_NOT_PERMITTED)' in ONE line incl. the conformance note (OSDM expects 501/404 for unimplemented endpoints); clean 501 no longer mislabelled 'Server Error'; handleSystemInfoStatus fail branch throws plain Error (no chai tail); all 8 System-Info callsites pass the parsed body; (15) auth preflight (duplicate POST /offers) runs ONCE PER RUN (skipped when the run already proved the token) and reports in ONE line; 33 '[INFO] ➤/►' function-name breadcrumbs → DEBUG; (16) envelope warnings/problems: '[WARNING] No warnings found' inverted-semantics happy path → one [DEBUG]; present-path was broken ('[object Object]', empty-array false hit, untagged header) → one structured line per warning ([WARNING]) / per problem ([ERROR]) with code/title/status/detail + pointers; (17) NEW shape-conformance assertion on non-empty envelope arrays: each entry must carry code + title/detail (same bar as the NHF Problem probes); non-URN vendor codes get a [WARNING] note; clean envelopes register no assertion.",
        "Verification: every behavioural item carries a Node-harness check in its commit (208-entry CoachDeckLayout fixture, Bileto 400+OPERATION_NOT_PERMITTED classification, envelope shapes ×4, expiredFlow sibling require, passenger-count lookup, inferLevel rule order against real run lines). Bruno_Collection/VERSION OTST_V2.0.64 -> OTST_V2.0.65; server 1.11.118 -> 1.11.119. Report assertion counts DROP by ~63 filler passes per run (env-sanity) — pass-rates become meaningful; no payload/wire-format change; failure-only behaviour changes: version-mismatch test title now carries the verdict, envelope shape assertion added, 'not supported' classification renamed."
      ]
    },
    {
      "release": "2026.146",
      "date": "2026-06-10",
      "server": "1.11.118",
      "collection": "OTST_V2.0.64",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(datafile validator — two false positives in the BUNDLED schema/walker): with the local schema serving finally working end-to-end (v1.11.115), a Bileto run surfaced two failures that were OUR bugs, not the datafile's. (1) The bundled json_validator/datafile.schema.json still carried 'required':['currency'] inside scenario.offerSearchCriteria — stale: ALL offer-search criteria (currency, offerMode, requestedOfferParts, …) were made optional per OSDM back in the April 2026 fix, and the wizard intentionally writes only the fields the tester picked (possibly an empty object). Every scenario authored without an explicit currency produced a false 'Required field scenarios[N].offerSearchCriteria.currency is missing'. The required block is removed. (2) validators.js validateValueAgainstSchema treated a PRESENT-but-null property as 'Required property missing' unless its NAME was on a hardcoded whitelist (gender, overruleCode, …) — a maintenance trap: placeSelectionMode, added later with schema type ['string','null'] and null in its enum, wasn't on the list, so every scenario carrying the perfectly legal 'placeSelectionMode': null was flagged. The walker now derives nullability from the SCHEMA itself (type includes 'null', or the enum lists null) with the legacy name list kept as fallback; a null on a genuinely non-nullable field now reports the precise condition (''X' is null but the schema type (…) does not allow null') instead of the misleading 'required missing'.",
        "feat(⛔ header decodability): the 'Invalid JSON Data file structure' header was tagged [INFO] with no error count — easy to misread as a schema-ACCESS problem (it fires only AFTER the schema was fetched 2xx and parsed). Now: '[ERROR] ⛔ Invalid JSON Data file structure (N error(s) — details below). Schema was fetched OK from: <url>'.",
        "Verified with a Node harness driving the real validateDataFileJsonWithTemplate against the real bundled schema: (A) scenario with offerSearchCriteria lacking currency + placeSelectionMode:null → both former false positives gone; (B) wrong-type placeSelectionMode:123 → still flagged; (C) missing tripRequirementId → still flagged; (D) null on non-nullable 'code' → new precise message. Bruno_Collection/VERSION OTST_V2.0.63 -> OTST_V2.0.64; server 1.11.117 -> 1.11.118. No payload change; only datafile-validation verdicts and message wording."
      ]
    },
    {
      "release": "2026.145",
      "date": "2026-06-10",
      "server": "1.11.117",
      "collection": "OTST_V2.0.63",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(run_events.ts, milliseconds never stored): the v1.11.113 dashboard change (slice(11,23) on the timestamp) claimed to 'unhide' millisecond precision — but the run_events.ts column was populated by its SQLite schema default datetime('now'), which is SECOND-precision ('2026-06-09 21:25:16'). There were no milliseconds to unhide; the user kept seeing [21:25:16] even on release-2026.144. Fix: logEvent() in Oscar_Server/src/worker/runner.js now passes ts explicitly as new Date().toISOString() ('2026-06-10T07:42:13.123Z') on both INSERT sites (regular event + cap-reached warning). Same UTC storage convention as everywhere else; the dashboard slice(11,23) now yields 'HH:MM:SS.mmm' as designed. Pre-existing rows keep the second-precision format and degrade gracefully (slice shows 'HH:MM:SS'). All run_events consumers paginate/order by the autoincrement id, never by ts string comparison, so mixed formats are safe. No schema migration needed — the column is TEXT; the DEFAULT stays as a fallback.",
        "fix(run-detail log truncated at ~500 lines — 'log stops before the offer request'): a FAILED Bileto run (709 assertions, 21 HTTP requests — the full offer/booking/refund flow executed) showed an execution log that ended mid-system-infos. Root cause: the GET /v1/runs/:id/logs endpoint caps each fetch at 500 rows (LIMIT 500 with a since_id cursor), and run-detail.html stopped polling the moment the run reported a terminal status (COMPLETED/FAILED/CANCELLED) — so opening a finished run fetched exactly ONE page and stranded everything behind the cursor. The run never stopped; the log VIEW did. Fix in three parts: (1) the logs endpoint now returns has_more (rows hit the SQL limit → backlog continues); (2) the dashboard poll loop drains the backlog with immediate 50ms follow-up fetches while has_more (length>=500 fallback), and only then lets the terminal status end the loop; (3) the terminal-status side-effects (artifacts/assertions/requests loads, delete + share buttons) are guarded by a terminalHandled flag so the extra drain iterations don't re-fire them. Verified in a browser harness against a stubbed 1250-event FAILED-run backlog: 1250/1250 lines rendered across 3 pages, side-effect loaders called exactly once, polling stopped cleanly after the drain.",
        "Server 1.11.116 -> 1.11.117; collection unchanged at OTST_V2.0.63 (server-only change). No payload change, no behaviour change beyond timestamp precision and complete log display."
      ]
    },
    {
      "release": "2026.144",
      "date": "2026-06-09",
      "server": "1.11.116",
      "collection": "OTST_V2.0.63",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(run-detail.html, invisible warn/debug log lines): regression-by-omission from v1.11.113 — the runner-side level inference started storing 'warn' and 'debug' levels in volume, but run-detail.html's log box only had color rules for l-stdout/l-stderr/l-info/l-error. Lines with l-warn / l-debug inherited the page's dark text color on the dark #1e1e1e log box and rendered near-invisible (user screenshot: whole blocks of barely-readable text between green INFO lines). Fix: .l-warn{color:#ffb74d} (amber), .l-debug{color:#9e9e9e} (muted but readable grey), plus a safety-net default color:#d4d4d4 on .log-line itself (placed BEFORE the .l-* rules — equal specificity, source order decides) so any FUTURE level without an explicit rule stays readable instead of vanishing. Also added 'warn' and 'debug' buttons to the level filter row (previously only All/stdout/stderr/info/error, so the new levels could not be isolated). report-builder.html got the matching .log-level.l-debug chip style (light page — was readable but unbadged). Verified by injecting all 7 level samples against the served stylesheet: stdout #d4d4d4, info #81c784, warn #ffb74d, error #ef5350, debug #9e9e9e, stderr #ff9800, unknown-level fallback #d4d4d4.",
        "Server 1.11.115 -> 1.11.116; collection unchanged at OTST_V2.0.63 (HTML-only change). No payload change, no behaviour change beyond display."
      ]
    },
    {
      "release": "2026.143",
      "date": "2026-06-09",
      "server": "1.11.115",
      "collection": "OTST_V2.0.63",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "docs(news): added the 'Partial refund is here' announcement to the welcome-page news carousel (Oscar_Server/public/news/index.json). Announces the #218 partial-refund capability shipped across v1.11.100–v1.11.106: the two combinable scope axes (per-leg via RefundSpecification.bookingPartIds with first/last/outbound/inbound targets; per-passenger via RefundSpecification.passengerIds with first/last targets), the OSDM v3.8 refundSpecifications[] wire format, the provider-modelling adaptation (Paxone one-fulfillment-per-passenger vs Bileto/Sqills one-per-booking), the scope-aware refundableAmount alignment assertion, the REFUND_PARTIAL framework-gating declaration, the graceful WARNING+full-refund degradation when the booking can't satisfy the requested scope, and the 21-test resolver coverage.",
        "fix(JSON_SCHEMA_URL rollout — the v1.11.112 fix never reached running installs): a user run on 2026-06-09 still validated against the deprecated raw.githubusercontent.com/.../exch_dev/.../datafile.schema.json URL and produced the known false-positive \"Required field 'scenarios[N].offerSearchCriteriaListId' is missing\" cascade — AFTER release-2026.140 was deployed. Root cause is twofold. (1) The v1.11.112 change updated .env.example (template for NEW installs) and added the local schema route, but an existing VPS keeps its OSCAR_Deploy/.env untouched — the old URL stays until the operator edits it. (2) Our own remediation instruction — in the v1.11.112 [WARNING] text AND the installation guide — said 'docker compose restart oscar', which DOES NOT apply .env edits: env_file values are baked into a container at CREATE time; restart keeps the same container. (Watchtower image updates clone the old container's env too.) The only command that applies an edited .env is 'docker compose up -d oscar' (recreate on config change). Three fixes: validators.js [WARNING] now instructs 'docker compose up -d oscar — NOT restart' with the reason; installation-guide.md remediation block updated the same way plus a printenv verification step; the stale exch_dev URL example in the Server Admin Guide replaced with the local-route value.",
        "feat(runner.js, JSON_SCHEMA_URL default): when JSON_SCHEMA_URL is unset, the runner now defaults to the self-served loopback route (http://127.0.0.1:<PORT>/json_validator/datafile.schema.json, PORT-aware) instead of writing an empty json_schema env var that failed every datafile validation with 'Missing env var json_schema'. Operators can now simply DELETE the line from .env (then 'docker compose up -d oscar') and get correct behaviour out of the box.",
        "Bruno_Collection/VERSION OTST_V2.0.62 -> OTST_V2.0.63 (validators.js warning text); server 1.11.114 -> 1.11.115. No data-file schema change, no payload change, no assertion-behaviour change."
      ]
    },
    {
      "release": "2026.142",
      "date": "2026-06-09",
      "server": "1.11.114",
      "collection": "OTST_V2.0.62",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#336, refund/booking assertion clarity): 10 small fixes across Bruno_Collection/library-bruno/bookings.js + refunds.js that together make refund-flow test failures decodable in the dashboard. User feedback: \"those failed messages on Refund Offer are quite difficult to decode for me\" — many failures were either misleading (titles preformatted unrelated fields' values as if everything passed), noisy (one provider gap cascading into 18+ near-duplicate failures), or pointing the operator at the wrong root cause.",
        "fix(#336, bookings.js cascade-kill on afterSalesConditions): validateAfterSalesConditions used to fire one parent 'exist in both' test AND THEN unconditionally iterate offerConditions.forEach() emitting one child 'REFUND exists in booking' test per offered condition — so when the booking response was missing afterSalesConditions entirely, a 3-admission+3-reservation×2-conditions-each offer produced 18 failures from a single root cause. Now: when bookedConditions is empty, emit ONE explicit parent failure naming the root cause (offer declared N condition(s) [REFUND,REFUND,...] — booking returned 0; per-condition checks SKIPPED to avoid duplicate cascading failures) and early-return before the forEach.",
        "fix(#336, bookings.js FulfillmentStatus enum was stale): bookings.js:698 enum did not include FULFILLED, so every FULFILLED fulfillment under a v3.8 booking failed the D1 enum check — even though our own fulfillments.js:144-146 lists FULFILLED as expected. Added FULFILLED. Also: the enum-check error message now NAMES the full valid list ('Valid OSDM values: [AVAILABLE,USED,...,FULFILLED].') so the reader doesn't have to grep the source to find allowable values.",
        "fix(#336, bookings.js fulfillmentStatus null guard): the v3.8 fulfillmentStatus optional field was guarded by `!== undefined`, which let the JSON-literal-null case through and stringified it into a nonsense test title like `'null' is a valid FulfillmentSummaryStatus`. Changed to `!= null` so both null AND undefined are treated as 'absent' (which is the v3.8 spec semantic). Skip-INFO message updated to acknowledge either source. Same valid-list naming added to the error message as the FulfillmentStatus fix above.",
        "fix(#336, bookings.js fulfillmentDocumentRefs unresolved): the #253/#336 v3.8 cross-check failure message correctly named the unresolved ref UUIDs and the sibling-id pool, but didn't spell out the root cause. Added a plain-language one-liner: 'Provider emits both fulfillmentDocumentRefs[] AND a sibling fulfillmentDocuments[] list, but the UUIDs don't reconcile — the refs and the docs are independently generated instead of linked.' Same payload, dramatically clearer report.",
        "fix(#336, bookings.js price-field expect context): the 'Price fields exist (currency, scale) in provisionalPrice and confirmedPrice' assertion's expect() calls now include the actual JSON value of the field that was missing — 'provisionalPrice.scale missing in booking (got: undefined)' is self-explaining; the prior bare 'provisionalPrice.scale missing' left the reader unsure whether it was null, undefined, or a wrong type.",
        "fix(#336, bookings.js BookingPartStatus error message): consistency with the FulfillmentStatus fix — the BookingPartStatus enum error now also names the valid list.",
        "fix(#336, refunds.js validFrom/validUntil date guards): `new Date(null)` returns epoch 0 (1970-01-01), not Invalid Date — so the `!isNaN(getTime())` guards in refunds.js let JSON null through. The test then fired with an empty title and failed at `expect(refundOffer.validFrom).to.exist`, leaving 'expected null to exist' as the only breadcrumb. Replaced the parseable-date check with a _toDate helper that only attempts new Date() on non-null strings, and a new _checkDatePresent helper that emits an explicit, self-explaining ABSENT-vs-MALFORMED failure when the field doesn't pass. Applied to createdOn, validFrom, validUntil. Plus added context strings to the createdOn-in-the-past, validUntil-future, and validUntil-15min-window assertions so each failure names what specifically went wrong.",
        "fix(#336, refunds.js refundableAmount/refundFee title built from shape, not happy-path): the test titles used to preformat 'amount: 0, currency: CZK' even when scale was missing — so failures looked like everything passed up to a mystery 'expected undefined to be a number'. Rewrote the label-builder _priceShape() to report each sub-field as either 'amount=0' (if valid) or 'amount=MISSING(got null)' (if not) — the broken sub-field now shows in the test title even when other fields look fine. Plus per-field expect() context strings naming exactly which OSDM Price sub-field failed and why ('refundableAmount.scale is not a number (OSDM Price.scale: required integer, typically 0)').",
        "Bruno_Collection/VERSION OTST_V2.0.61 -> OTST_V2.0.62; server 1.11.113 -> 1.11.114. 21 partial-refund + 26 framework-gating unit tests still pass. No data-file schema change. No payload / wire-format change. Passing scenarios are unaffected — only failure-message text and one cascade structure changed."
      ]
    },
    {
      "release": "2026.141",
      "date": "2026-06-09",
      "server": "1.11.113",
      "collection": "OTST_V2.0.61",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#336, dashboard ms display): run-detail.html log timestamps were truncated to seconds (slice(11,19) → '19:12:42'), so two events emitted in the same wall-clock second showed identical timestamps and could not be correlated with provider-side logs at sub-second precision. Changed to slice(11,23) → '19:12:42.123'. UI-only change, no payload change. Run log persistence already stored full ISO ms timestamps — this fix just unhides them on the dashboard.",
        "feat(#336, runner-side level inference): the Bruno child-process stdout/stderr listeners in Oscar_Server/src/worker/runner.js were storing every line with level='stdout' or 'stderr' regardless of content — so the level filter in the dashboard was useless for Bruno-emitted lines (a [ERROR] line and a [DEBUG] line both showed as 'stdout'). Added inferLevel(line, streamFallback) that classifies by: (1) explicit [LEVEL] tag → that level, (2) Bruno CLI native markers (✓ → info / ✕ → error), (3) JS stack-trace shapes (Error:/AssertionError:/at … lines → error), (4) known harmless platform noise (OpenSSL 'Cannot open directory /etc/ssl/certs' → warn instead of leaking as stderr), (5) stderr-stream fallback → error, (6) stdout-stream fallback → info. The dashboard level filter now actually filters correctly for the full run output.",
        "feat(#336, Bruno library [LEVEL] sweep): added [INFO]/[WARN]/[WARNING]/[ERROR]/[DEBUG] prefixes to ~25 untagged console.log/console.error calls across Bruno_Collection/library-bruno/ (auth.js, bookings.js, displays.js, envUtils.js, exchanges.js, expiredFlow.js, fulfillments.js, loopback.js, mergeReport.js, model.js, offers.js, osdmCompliance.js, osdmSchema.js, osdmSchemas.js, osdmVersion.js, partialRefund.js, passengers.js, refunds.js, reportGenerator.js, requestedInformation.js, scenarioParser.js, validators.js). Every line now carries its true severity (e.g. the omnipresent '[library-bruno] globalThis exposure skipped' diagnostic is now [DEBUG] — only visible at the user's choice). Combined with the runner-side inference above, the dashboard level filter is now reliable end-to-end.",
        "fix(#336, #253 followup — fulfillmentDocuments empty-array): the v3.8 fulfillmentDocumentRefs → siblingDocuments[].id cross-check in bookings.js validateFulfillmentDocumentRefs() was guarded by `if (Array.isArray(siblingDocs))`, which correctly skipped the check when callers didn't pass siblingDocs at all, but INCORRECTLY ran the check (and reported every ref as 'unresolved') when callers passed an empty array — the real-world pre-issuance shape `fulfillmentDocuments: []`. Tightened the guard to also require length > 0, and split the else-branch in two: an empty-but-present array now logs '[INFO] sibling fulfillmentDocuments[] is present but empty — legal pre-issuance shape', while a fully absent array keeps the original 'not provided to validator' [INFO]. No assertion change for the populated-array happy path. Test name updated to '...when siblings present' to clarify the conditional scope.",
        "Bruno_Collection/VERSION OTST_V2.0.60 -> OTST_V2.0.61; server 1.11.112 -> 1.11.113. 21 partial-refund + 26 framework-gating unit tests still pass. No data-file schema change. No payload/wire-format change."
      ]
    },
    {
      "release": "2026.140",
      "date": "2026-06-09",
      "server": "1.11.112",
      "collection": "OTST_V2.0.60",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#333, SEARCH branch endDatetime): the SEARCH trip branch in scenarioParser.js was unconditionally calling subTripDate(tripRequirement.trip.endDatetime, ...), which threw when endDatetime was absent — but endDatetime is OPTIONAL on a TripSearchCriteria per OSDM spec (you specify a departure time, not a window). osdmTripSearchCriteria() itself doesn't even USE the endDateTime — the value is computed, passed to TripLegDefinition as arg 4, and discarded when the TripSearchCriteria is built (only `startDateTime` is used — see line 1171). So OSCAR was rejecting valid OSDM datafiles. The v1.11.110 try/catch I added caught the throw and emitted a [ERROR] blaming the data, which sent new users on a wild goose chase. Fix: guard the subTripDate call behind a truthiness check, pass null when absent. SPECIFICATION branch unchanged (each leg's endDatetime IS required when you're specifying exact trips). The OBB Nightjet datafile that triggered this now resolves cleanly to offerTripSearchCriteria.",
        "feat(#333, serve schema from OSCAR_Server): a new public Express route in Oscar_Server/src/server.js — GET /json_validator/datafile.schema.json — serves the JSON schema bundled with the Bruno collection (read from the same /collection bind-mount the runner already uses). Removes the dependency on a publicly reachable URL for datafile schema validation. Before this PR, operators had to either point JSON_SCHEMA_URL at an external GitHub URL (fragile — depends on the repo staying public and the branch / file path not moving) or set up their own static HTTP server in the docker-compose (undocumented). Now the schema is always co-located with the running collection — they ship together so versions can't drift. .env.example default updated to http://127.0.0.1:3001/json_validator/datafile.schema.json.",
        "docs(#333, installation-guide): new section in Documentation/Server_Operations/installation-guide.md documenting JSON_SCHEMA_URL — explains the v1.11.112+ self-hosted default, names the obsolete OSDM-testing/exch_dev URL that older installs may still have, and gives the exact one-line steps to update an existing .env on the VPS plus `docker compose restart oscar` to apply.",
        "fix(#333, [WARNING] wording): the v1.11.109 [WARNING] in validators.js said 'update the company's environment file' — wrong because there is no per-company env file; the value is built fresh server-side from JSON_SCHEMA_URL on the OSCAR server itself. Updated to point at the actual fix path: 'edit OSCAR_Deploy/.env, set JSON_SCHEMA_URL to http://127.0.0.1:3001/json_validator/datafile.schema.json, restart with docker compose restart oscar'. Also separated 'deprecated repo' (the exch_dev / OSDM-testing case) from 'any GitHub URL is fragile' so the warning is informative for both.",
        "Bruno_Collection/VERSION OTST_V2.0.59 -> OTST_V2.0.60; server 1.11.111 -> 1.11.112. 21 partial-refund + 26 framework-gating unit tests still pass. No data-file schema change. No backend behaviour change beyond serving one additional static file."
      ]
    },
    {
      "release": "2026.139",
      "date": "2026-06-09",
      "server": "1.11.111",
      "collection": "OTST_V2.0.59",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(displays.js, log levels): the DEBUG case in validationLogger was BACKWARDS — it passed only [DEBUG] and [INFO] messages and DROPPED [WARN] / [WARNING] / [ERROR]. So a Test Manager setting loggingType=DEBUG (expecting MORE detail than INFO) actually saw FEWER critical lines than the default — including the v1.11.110 trip-branch diagnostic [ERROR] lines that the OBB onboarding case relied on. Surfaced exactly that way: after v1.11.110 deployed, the user switched to DEBUG, the trip-branch [ERROR] vanished. Fixed to match the standard logging pyramid — DEBUG is the most verbose level and now passes [DEBUG] + [INFO] + [WARN] + [WARNING] + [ERROR] (everything except untagged messages, which FULL still picks up). After this fix: at INFO level the trip-branch [ERROR] still appears (no change); at DEBUG level the [ERROR] now appears as well, AND the [DEBUG] structure dumps appear, AND the user has a complete picture. The user can switch to DEBUG and KEEP all the warnings/errors they had at INFO. Other levels (FULL, INFO, WARN, ERROR) unchanged.",
        "Bruno_Collection/VERSION OTST_V2.0.58 -> OTST_V2.0.59; server 1.11.110 -> 1.11.111. No data-file schema change, no backend behaviour change, no wizard UI change. 21 partial-refund + 26 framework-gating unit tests still pass."
      ]
    },
    {
      "release": "2026.138",
      "date": "2026-06-09",
      "server": "1.11.110",
      "collection": "OTST_V2.0.58",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#330, trip-branch validation): the SEARCH and SPECIFICATION trip branches in `Bruno_Collection/library-bruno/scenarioParser.js` were reading `tripRequirement.trip.startDatetime / .origin / .destination` (SEARCH) and `tripRequirement.legs[*].origin / .destination / .startDatetime / .endDatetime` (SPECIFICATION) without checking they exist first. When the datafile had a tripRequirement entry whose id matched the scenario (so the v1.11.109 upfront 'unresolved tripRequirementId' [ERROR] didn't fire) but the .trip sub-object or .legs[] array was missing or incomplete, the branch threw at the first undefined access — and the throw was swallowed silently, leaving offerTripSearchCriteria / offerTripSpecifications unset. The user only saw the downstream 'Required scenario variable offerTripSearchCriteria is empty' from buildOfferCollectionRequest, with no [INFO]/[ERROR] line naming the actual data-shape gap. This release adds two layers of diagnostic: (1) BEFORE entering the branch, validate that the required fields exist on the trip / legs; if not, emit a precise [ERROR] naming the missing field(s) and the wizard section to fix, plus a [DEBUG] dump of the actual structure (visible only at loggingType=DEBUG/FULL — does NOT add noise to default INFO-level reports). (2) WRAP the switch in try/catch so any deeper unexpected throw (subTripDate refusing a date format, a malformed sub-field the validators didn't catch) surfaces with the tripRequirement context instead of propagating as 'Cannot read property X of undefined'. The downstream parseEnvJson hint from v1.11.109 still fires after, but the user now sees the root cause ('SEARCH trip missing origin/destination/startDatetime') BEFORE the consequence ('offerTripSearchCriteria empty'). All new log lines use [ERROR] / [DEBUG] levels — they only fire when something is actually broken AND only the [ERROR] line shows at default INFO level; the [DEBUG] dump stays invisible unless explicitly enabled.",
        "Bruno_Collection/VERSION OTST_V2.0.57 -> OTST_V2.0.58; server 1.11.109 -> 1.11.110. No data-file schema change, no backend behaviour change, no wizard UI change. 21 partial-refund + 26 framework-gating unit tests still pass."
      ]
    },
    {
      "release": "2026.137",
      "date": "2026-06-09",
      "server": "1.11.109",
      "collection": "OTST_V2.0.57",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#328, new-user diagnostics): four precise [ERROR]/[WARNING] log lines surfaced by an OBB onboarding case where a tester chased a confusing 'Required scenario variable offerTripSearchCriteria is empty or not set' down to a stale schema URL and an unresolved tripRequirementId. (1) `Bruno_Collection/library-bruno/validators.js` — detect when json_schema points at `github*.com` and emit a [WARNING] explaining that the UnionInternationalCheminsdeFer/OSDM-testing repo (exch_dev branch and others) is deprecated as a schema reference and is out of sync with the modern OSCAR datafile shape (it still requires the legacy offerSearchCriteriaList linkage that produces false-positive 'Required property offerSearchCriteriaList is missing'); message names the fix (use http://localhost:8080/json_validator/datafile.schema.json). (2) `Bruno_Collection/library-bruno/scenarioParser.js` — upfront [ERROR] when scenario.tripRequirementId doesn't resolve to any datafile.tripRequirements[].id, naming the unresolved id, the available ids, and the fix path (Test Data → Trip Requirements section in the wizard). Same upfront check added for (3) passengersListId and (4) requestedFulfillmentOptionsListId, each pointing at the matching wizard section. (5) `Bruno_Collection/library-bruno/envUtils.js` — when parseEnvJson throws on offerTripSearchCriteria / offerTripSpecifications / offerPassengerSpecifications / offerSearchCriteria, the message now appends a per-variable hint naming the upstream resolver and the most-likely cause (unresolved scenario id-reference), so the tester sees the precise gap instead of a generic 'check data_base' message. Resolves the OBB onboarding case where the tester chased an opaque 'TripSearchCriteria empty' error through scenarioParser before discovering the stale env file. No data-file schema change, no behaviour change beyond the new diagnostics.",
        "Bruno_Collection/VERSION OTST_V2.0.56 -> OTST_V2.0.57; server 1.11.108 -> 1.11.109. 21 partial-refund + 26 framework-gating unit tests still pass."
      ]
    },
    {
      "release": "2026.136",
      "date": "2026-06-09",
      "server": "1.11.108",
      "collection": "OTST_V2.0.56",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(refunds, semantic): `AppliedOverruleCode is null as expected` was firing as a false-positive failure when the provider OMITS the appliedOverruleCode field from the JSON response (so chai sees undefined while the test expects null). Both omitted-field and explicit-null are valid OSDM responses meaning the same thing — 'no overrule applied'. validateRefundAppliedOverruleCode in `Bruno_Collection/library-bruno/refunds.js` now normalises undefined -> null before the chai equality check. The assertion title also annotates which form the provider returned so the report reader sees it explicitly: 'AppliedOverruleCode is null as expected (actual: undefined -> treated as null)'.",
        "fix(refunds, clarity): the `Refund offer[N] refundFee exists and is valid, amount: 0, currency: EUR` wording was confusing — 'exists and is valid' sounded like an economic claim about the fee, but per OSDM (RefundOffer.refundFee: 'Amount kept by the carrier and/or distributor') the assertion is structural — the Price object must be present even when amount=0 (no retention). Rephrased to name the structural check explicitly and annotate amount=0 vs amount>0 with the OSDM semantic: 'Refund offer[0] refundFee Price structure is well-formed — amount: 0 EUR (= no carrier retention), scale: 2' for zero; '... amount: 1000 EUR (= kept by carrier per OSDM), scale: 2' for non-zero.",
        "fix(http assertions, clarity): the `Status code is 200` test name was misleading when the response was NOT 200 — the title asserted '200' as if it were a claim ('Status code is 200 ✗ expected 405 to deeply equal 200'). Renamed to a self-documenting dynamic name `HTTP response status — expected 200, actual: ${res.getStatus()}` so the reader immediately sees both expected and actual in the test name. Sweep across 17 .yml files (main test + the throw-on-failure branch). Two patterns deliberately left: `bruTest('Status code is 200 — STOP on failure', ...)` (the suffix already self-documents) and `test('Status code is 200 or 202', ...)` (different assertion).",
        "Bruno_Collection/VERSION OTST_V2.0.55 -> OTST_V2.0.56; server 1.11.107 -> 1.11.108. No data-file schema change, no backend change, no wizard UI change. 21 partial-refund + 26 framework-gating unit tests still pass."
      ]
    },
    {
      "release": "2026.135",
      "date": "2026-06-09",
      "server": "1.11.107",
      "collection": "OTST_V2.0.55",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#324, tester ergonomics): every Bruno step now logs a millisecond-precision UTC timestamp on start, with a Europe/Paris local-time annotation. Previously each step printed `⏩ [STEP] Executing request : <name>` with no wall-clock — testers correlating an OSCAR run with provider-side logs (Paxone, Bileto, …) had to do that math from the run start time + cumulative durations. Now the step-start line reads: `⏩ [STEP] [2026-06-09T07:23:26.087Z (= 2026-06-09 09:23:26.087 Europe/Paris)] Executing request : 10. POST Refund Offers`. Implemented as a single `logStepStart(req)` helper in `Bruno_Collection/library-bruno/displays.js` (matches the same Europe/Paris timestamp pattern shipped for refund-offer createdOn/validFrom/validUntil in v1.11.106). Swept across all 30 .yml files that previously hardcoded the console.log line. The bracketed prefix sits AFTER the existing `⏩ [STEP]` marker so any downstream tool matching on that marker still finds the line.",
        "feat(#325, tester ergonomics): Copy + Download buttons on every request and response body in both the live dashboard (run-detail.html) AND the Open Report (report-builder.html). The dashboard previously showed the body in a 360px-tall scrollable box with no per-message Copy or Download, forcing testers to switch to the Open Report and select-all-and-copy manually. Now: (1) the dashboard renderMessage() emits Copy / Download / ⌄ Expand buttons next to the existing Raw/Tree toggle; Copy puts the body on the clipboard, Download saves a self-named JSON file, Expand removes the 360px scroll cap in place; (2) the Open Report's existing Copy button gains a sibling Download button using the same filename pattern. Filename: `${scenarioCode}_${stepName}_${kind}.json` with non-filesystem-safe characters (\\ / : * ? \" < > | whitespace) collapsed to underscores so files move cleanly between Windows, macOS, and Linux. A file downloaded from either view (dashboard vs Open Report) has the same name. The 100 KB server-side MAX_BODY_SIZE cap behaviour is unchanged (separate config knob via MAX_BODY_SIZE env var); when a body hits that cap the truncation marker `[truncated NNN bytes]` is visible in the copy/download too, so testers see the truncation explicitly.",
        "Bruno_Collection/VERSION OTST_V2.0.54 -> OTST_V2.0.55; server 1.11.106 -> 1.11.107. No data-file schema change, no backend behaviour change, no wizard UI change. Resolves issues #324 and #325. 21 partial-refund + 26 framework-gating unit tests still pass."
      ]
    },
    {
      "release": "2026.134",
      "date": "2026-06-09",
      "server": "1.11.106",
      "collection": "OTST_V2.0.54",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(refunds, semantic): the 'Refundable amount is 0 because overruleCode is null or CODE_DOES_NOT_EXIST' assertion was inverted. It encoded the assumption 'no overrule -> refund denied -> amount must be 0' as a global truth, but that's only correct in one specific case (booking outside every applicable afterSalesCondition window). The general truth is: WITHOUT overrule, the provider OWNS the rules (time/fare/distance/internal commercial — OSCAR can't predict them) and the response is authoritative; WITH overrule, the distributor declared the exceptional circumstance and the provider's contract is to refund the full booking value with no fee. Rewrote validateRefundableAmountLocal in `Bruno_Collection/library-bruno/refunds.js` to encode this correctly: (a) a new structural-bounds assertion that ALWAYS applies regardless of overrule (amount and fee non-negative; sum <= confirmedPrice; currencies consistent); (b) overrule-set branch verifies the overrule contract (full-refund: amount == confirmedPrice AND fee == 0; partial-with-overrule: fee == 0 AND fee+amount < confirmedPrice); (c) no-overrule branch logs what the provider returned but does NOT assert a specific value (OSCAR is not the source of truth for the provider's refund rules). The partial-refund identity (fee+amount < confirmedPrice strict) is preserved in both flows. Two consequences worth noting: a provider that ignored an overrule and returned a partial amount (historically silent pass) now fails; a no-overrule scenario whose booking is within the refund window (historically false positive failure) now passes with an [INFO] line surfacing the values for human reconciliation. The Paxone partial-refund scenario reported on 2026-06-09 — where no overrule was sent and Paxone returned 5000 EUR for a booking 9 days out — now correctly passes (was failing 'expected 5000 to equal 0').",
        "fix(refunds, clarity): rename the partial-refund alignment assertion from 'Partial refund: response refundableAmount matches expected partial sum' to 'Partial refund: request/response alignment — response refundableAmount matches the sum of in-scope parts'. The historical wording sounded like a maths claim; the new wording names what is being verified (request vs response alignment). The error message already includes the wire-scope shape (e.g. `fulfillment[b90ddfa8...].bookingParts (6 part(s))`) so a failure is self-explanatory.",
        "fix(refunds, clarity): provider timestamps come back in UTC (e.g. createdOn=2026-06-09T05:23:26+00:00); the assertion messages now append a parenthetical Europe/Paris equivalent so the report shows both the UTC string and the local-time read at once (e.g. '... is valid and in the past: 2026-06-09T05:23:26+00:00 (= 2026-06-09 07:23:26 Europe/Paris)'). No assertion-logic change; pure display improvement that prevents the tester from mentally doing the +1h/+2h offset against their local clock.",
        "Bruno_Collection/VERSION OTST_V2.0.53 -> OTST_V2.0.54; server 1.11.105 -> 1.11.106. No data-file schema change, no backend change, no wizard UI change. 21 existing partial-refund unit tests + 26 framework-gating unit tests still pass."
      ]
    },
    {
      "release": "2026.133",
      "date": "2026-06-08",
      "server": "1.11.105",
      "collection": "OTST_V2.0.53",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#218 follow-up, golden rule): the Test Framework now gates scenario features. A scenario may not silently exercise a capability the framework hasn't declared — 'what is not defined in the framework cannot be tested'. Implemented as SOFT validation (option (a)-with-scan): existing scenarios keep working, runs are never blocked, but warnings are surfaced at three levels so the gap is visible. First feature gated: partial refund (partialRefundByLeg / partialRefundByPax → REFUND_PARTIAL in salesFlows[]). New `Oscar_Server/src/utils/frameworkGating.js` is the single source of truth for the field↔flow rule table (`gatingRules()`), the per-scenario warning resolver (`scenarioWarnings()`), the lazy server-side migration (`applyFrameworkMigration()`), and the datafile annotator (`annotateDatafile()`). Three integration points: (1) `GET /v1/company/test-framework` now runs a one-time lazy migration per framework — when the framework's salesFlows[] doesn't include REFUND_PARTIAL but at least one of the company's scenarios arms partialRefundByLeg/byPax, REFUND_PARTIAL is added to salesFlows[], a `_salesFlowsMigratedAt` audit stamp is written, and the row is re-encrypted. Conservative: adds only, never removes — so a Test Manager's running configuration is preserved exactly while the new declaration baseline reflects what they're actually using. (2) `GET /v1/company/datafile` now annotates each scenario whose armed feature isn't declared with `__featureNotDeclaredWarnings: [field, ...]` before serving — on-disk file is unchanged; only the served bytes carry the annotation. (3) `Bruno_Collection/library-bruno/scenarioParser.js` reads the annotation and emits a `[WARNING]` log line per entry at scenario load, explaining which capability isn't declared and how to silence the warning. Wizard UI (`Oscar_Server/public/js/scenarios.js`): an inline amber chip next to the partial-refund block when the scenario arms it without the framework declaring it, plus a top-of-scenarios banner with the count of affected scenarios and a pointer to Test Framework → ✂️ Refund/Exchange row. Both check `fwDeclaresPartialRefund(scenarioType)` against the framework's salesFlows[]. New `tests/unit/framework-gating.test.js` covers 26 cases including the integration scenario: pre-migration warnings → migration runs → re-annotation finds nothing to warn about. No data-file schema change, no backend behaviour change beyond the warnings.",
        "Bruno_Collection/VERSION OTST_V2.0.52 -> OTST_V2.0.53; server 1.11.104 -> 1.11.105. The annotator and warning emission are forward-compatible: pre-v1.11.105 Bruno collections ignore `__featureNotDeclaredWarnings` as an unknown field. The migration runs once per framework and is idempotent."
      ]
    },
    {
      "release": "2026.132",
      "date": "2026-06-08",
      "server": "1.11.104",
      "collection": "OTST_V2.0.52",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#218, partial refund): alignment assertion now compares against the actual wire scope, not the resolver's internal intent. Follow-up to v1.11.103: a Paxone test against a 4-pax / 2-leg booking with both axes armed (per-leg=last + per-pax=last) sent the correct fulfillmentIds=['<nicolas's fulfillment>'] (single, scoped, OSDM-3.5 compatible) and Paxone correctly refunded all 6 of nicolas's parts across both legs (refundableAmount=5000 EUR = sum of the fulfillment.bookingParts). The new alignment assertion however computed expected=2500 (the resolver's per-leg-x-per-pax intersection of 3 parts on the last leg only) and cried wolf — because on OSDM v3.5 the per-leg axis is silently dropped at the wire (refundSpecifications.bookingPartIds is not yet supported). The expected sum now depends on what was actually conveyed: refundSpecifications.bookingPartIds when on the wire (provider refunds exactly those), refundSpecifications.passengerIds-only when on the wire (provider expands to every part owned by those passengers), or fulfillment.bookingParts[].id when only fulfillmentIds[] is on the wire (provider refunds everything in the scoped fulfillment). Plus a new [WARNING] log line when an axis is silently dropped due to osdmVersion < 3.8 — explains in plain English that the per-leg request couldn't be conveyed and the response will cover all legs of the chosen passenger. The error message now also names the wire-scope shape so the failure is self-explanatory.",
        "Bruno_Collection/VERSION OTST_V2.0.51 -> OTST_V2.0.52; server 1.11.103 -> 1.11.104. No data-file schema change, no backend change, no wizard UI change. Existing 21 partial-refund unit tests still pass."
      ]
    },
    {
      "release": "2026.131",
      "date": "2026-06-08",
      "server": "1.11.103",
      "collection": "OTST_V2.0.51",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#218, partial refund): the wizard-generated request was malformed in five distinct ways and the response was treated as a partial refund even when the provider had silently produced a full one. Discovered debugging a Paxone REFUND scenario where the response refundableAmount equalled the booking's full confirmedPrice — i.e. partial scope was never actually applied. Five fixes: (1) `Bruno_Collection/library-bruno/bookings.js` was pushing every fulfillment id TWICE into the env var, so refund / exchange requests carried each id duplicated — providers tolerated it for full-flow but partial scope couldn't work. Dedupe to one push per fulfillment. (2) `library-bruno/requestsBuilder.js` now trims `fulfillmentIds[]` to the scoped fulfillment when partial is armed; the original code kept the full list AND added refundSpecifications, which providers correctly interpreted as 'refund all these in full'. (3) `library-bruno/partialRefund.js` now resolves `RefundSpecification.fulfillmentId` by matching `fulfillment.fulfillmentParts[].passengerRef` against the chosen passenger's externalRef — the old code blindly picked fulfillments[0], which is wrong on providers like Paxone that model one-fulfillment-per-passenger. (4) Leg ordering now reads `booking.trips[*].legs[*].id` (the user-visible leg list the wizard's first/last/outbound/inbound labels refer to), instead of treating the legs × passengers cross-product of admissions as the leg list. (5) Booking-part expansion now walks `admission.reservationRefs[].id` and `admission.ancillaryRefs[].id` instead of looking for `requiredAdmissionKey | admissionRef | admissionId` on the reservation/ancillary side — those fields don't exist in OSDM / Paxone. The two axes also now intersect explicitly (admissions covering the chosen leg AND owned by the chosen passenger), so leg-target and pax-target never disagree on the subject. Plus: when the negotiated `osdmVersion < 3.8` (e.g. Paxone sandbox on 3.5), `refundSpecifications[]` is omitted (the provider would reject it as OSDM_EXTRA_REQUEST_FIELDS_IGNORED and silently full-refund); scoping then happens via the trimmed `fulfillmentIds[]` alone. New after-response alignment assertion compares `response.refundOffers[0].refundableAmount` against the sum of the in-scope booking-part prices computed before the request and fails with a clear message if the provider ignored the scope — the symptom that started this investigation now surfaces as a test failure instead of a silent full refund. Resolver covered by a 4-case smoke trace against a Paxone-shaped fixture (deleted before commit).",
        "Bruno_Collection/VERSION OTST_V2.0.50 -> OTST_V2.0.51; server 1.11.102 -> 1.11.103. No data-file schema change, no backend change."
      ]
    },
    {
      "release": "2026.130",
      "date": "2026-06-08",
      "server": "1.11.102",
      "collection": "OTST_V2.0.50",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(wizard, UX): changing a passenger's category (e.g. ADULT -> CHILD) no longer snaps the viewport back to the top of the scenario detail panel. Reported by a tester after the v1.11.101 cycle: editing in the Passengers section required scrolling all the way down again every time the type dropdown changed value, and any other param-section the tester had open (NHF, Booking Flow Actions, …) collapsed back to default. Two fixes: (1) the canonical reRenderScenarioDetail helper now captures window.scrollY at entry and restores it after the innerHTML swap so the viewport stays where the tester left it; (2) the change-pax-category handler now routes through reRenderScenarioDetail instead of doing an inline `det.innerHTML = buildDetailHTML(...)`, picking up the section-state preservation that the inline path bypassed. Bonus: the category dropdown is re-focused after the re-render so the keyboard / pointer can continue editing without an extra click. Wizard-only change; runtime / data semantics unchanged.",
        "Bruno_Collection/VERSION OTST_V2.0.49 -> OTST_V2.0.50; server 1.11.101 -> 1.11.102. No data-file schema change, no backend change."
      ]
    },
    {
      "release": "2026.129",
      "date": "2026-06-08",
      "server": "1.11.101",
      "collection": "OTST_V2.0.49",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(wizard, UX): RequestedInfo Probe now matches the Passenger external-ref format probe's presentation — dashed-border box with an uppercase mini-header, the dropdown sized to its longest enum value, and the explanation as a full-width hint underneath. Previously it used buildSelect's grid layout, which cramped the multi-sentence hint into a narrow vertical strip next to the dropdown. Same data-action / data-field as before — runtime and save semantics unchanged. Pure CSS / markup change inside buildNonHappyFlowSection.",
        "Bruno_Collection/VERSION OTST_V2.0.48 -> OTST_V2.0.49; server 1.11.100 -> 1.11.101. No data-file schema change, no backend change."
      ]
    },
    {
      "release": "2026.128",
      "date": "2026-06-08",
      "server": "1.11.100",
      "collection": "OTST_V2.0.48",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(wizard, UX): Non Happy Flow customisation section reorganised into two collapsible sub-groups with per-group badge counters. Sub-group 1 'Expiry timers' holds the six expired-X timer pairs that were already there (offer, booking, add-reservation, add-ancillary, refund-offer, exchange-offer). Sub-group 2 'Field-shape & payload probes' holds the requestedInformationProbe (MOVED out of SCENARIO PARAMETERS — the Tester Guide §4.8 always classified it as NHF; the wizard now matches) + the passengerExternalRefFormat probe (introduced in v1.11.99). Each sub-group auto-expands when anything inside it is armed; the top NHF section also auto-expands when total armed > 0. Badge style: amber pill 'N of M armed' when N>0, neutral grey when N=0 — same vocabulary the wizard already uses for the Test Framework pill counters.",
        "Subtitle on the NHF section header broadened from 'expired-X negative tests — wait past a deadline, assert the provider rejects the next request' to 'negative tests and conformance probes' so the description no longer lies about what lives there.",
        "No data-file schema change. No backend / runtime change. requestedInformationProbe field name unchanged — pre-existing scenarios behave identically; only the wizard's rendering location moved.",
        "Bruno_Collection/VERSION OTST_V2.0.47 -> OTST_V2.0.48; server 1.11.99 -> 1.11.100. Manually authored data files don't need any migration."
      ]
    },
    {
      "release": "2026.127",
      "date": "2026-06-08",
      "server": "1.11.99",
      "collection": "OTST_V2.0.47",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(wizard, NHF): new non-happy-flow probe — passengerExternalRefFormat — lets the tester override the wizard-default 00001-style passenger reference with a printf-style pattern (e.g. PAX%04d -> PAX0001, ABC-%03d -> ABC-001, %d -> 1 no padding). The new value is applied at scenario-parse time and propagates through every downstream call (offer, booking, refund, exchange) consistently. Empty/missing pattern keeps the default behaviour. Designed to document provider variance — at time of writing Paxone rejects PAX1-style refs with a catch-all Schema validation error while Bileto / Sqills / Turnit / Benerail accept any non-empty string per OSDM v3.8 spec. Wizard renders a live preview of the first three generated refs as the tester types.",
        "Bruno_Collection/VERSION OTST_V2.0.46 -> OTST_V2.0.47; server 1.11.98 -> 1.11.99. Data-file schema gains an optional passengerExternalRefFormat field on the scenario object (string, no pattern enum).",
        "New unit test Oscar_Server/tests/unit/bruno-externalrefformat.test.js — 16 cases covering the documented happy paths, width edge cases, graceful degradation on null/missing/placeholder-less patterns, and parity with the wizard's live-preview parser."
      ]
    },
    {
      "release": "2026.126",
      "date": "2026-06-08",
      "server": "1.11.98",
      "collection": "OTST_V2.0.46",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(wizard): passenger reference generated by the scenario wizard switched from 'PAX1', 'PAX2', ... to the 5-digit zero-padded shape '00001', '00002', ... — matching what the runtime builders (requestsBuilder.js lines 178 / 455 / 468) already use everywhere else in the codebase. Background: a multi-passenger SALE scenario on Paxone failed POST /offers with four 'Schema validation error' messages (one per passenger from index 1 onwards) because Paxone enforces a stricter-than-OSDM externalRef shape than the spec advertises. OSDM v3.8 itself permits any non-null string; Paxone wants numeric / zero-padded. Three sites updated in scenarios.js: wizGenPassengers() (line 4960), 'add passenger' UI handler (line 5393), and the re-indexer after a passenger is removed (line 5549). Tester action: pre-existing scenarios saved with the old format need to be re-authored OR hand-edited in the data file (rename 'PAXn' to the zero-padded form everywhere it appears: passengersList[].passengers[].reference, bookingPassengerReferences, and any echoed externalRef inside offerPassengerSpecifications / bookingPassengerSpecifications). Bileto / Sqills / Turnit / Benerail accept both formats and are unaffected.",
        "Bruno_Collection/VERSION OTST_V2.0.45 -> OTST_V2.0.46; server 1.11.97 -> 1.11.98. No data-file format change; the schema accepts any non-empty string for passenger.reference."
      ]
    },
    {
      "release": "2026.125",
      "date": "2026-06-07",
      "server": "1.11.97",
      "collection": "OTST_V2.0.45",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#218): wizard partial-refund check used wizData.passengersLists / wizData.tripRequirements but the canonical data lives in state.passengersList / state.tripRequirements (already exposed via getPassengers(id) / getTrip(id) at the top of scenarios.js). Result: passenger count and leg count resolved to 0 every time, the per-pax warning fired on every multi-pax REFUND scenario (reported via screenshot — 5-passenger list showed 'has 0'). Switched to the canonical helpers; warnings now only fire when the configuration genuinely can't be satisfied.",
        "Bruno_Collection/VERSION OTST_V2.0.44 -> OTST_V2.0.45; server 1.11.96 -> 1.11.97. No data-file format change."
      ]
    },
    {
      "release": "2026.124",
      "date": "2026-05-28",
      "server": "1.11.96",
      "collection": "OTST_V2.0.44",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#218): partial refund. New REFUND-only scenario fields partialRefundByLeg + partialRefundLegSelection (first/last/outbound/inbound) + partialRefundByPax + partialRefundPaxSelection (first/last). When armed, OSCAR scopes the POST /bookings/{id}/refund-offers via OSDM's RefundOfferRequest.refundSpecifications[] — each spec carries fulfillmentId + optional bookingPartIds (per-leg axis) + optional passengerIds (per-pax axis). Both axes can be combined.",
        "feat(wizard): partial-refund pair shown inline in the SCENARIO PARAMETERS section for REFUND scenarios. Inline validation warnings when: per-pax with <2 passengers in the resolved passengersList; per-leg with a SPECIFICATION trip that's <2 legs; outbound/inbound with a one-way trip. SEARCH-mode trips show an info note that runtime degradation handles the leg-count check.",
        "feat(runtime): 10. POST Refund Offers' before-request resolves the partial scope from the booking JSON captured in 07. GET Booking after Fulfillments. If the booking has <2 admissions (per-leg) or <2 passengers (per-pax), OSCAR logs [WARNING] Partial refund degraded to full and sends the historical full-refund body — the rest of the refund happy path runs unchanged. Only the partial-scope assertions skip.",
        "feat(validators): refunds.js validateRefundableAmountLocal is partial-aware. When the scenario is in partial mode AND not degraded, asserts refundFee + refundableAmount < confirmedPrice (strict-less) instead of the equality identity. New structural check: refundOfferBreakdownItems[].bookingParts must be a SUBSET of the requested bookingPartIds (no extra parts refunded).",
        "tests: new tests/unit/bruno-partialrefund.test.js covering resolvePartialRefundScope (single-leg degradation, single-pax degradation, first/last/outbound/inbound selection, both-axes combo, fallback to booking.passengers[].id) and buildRefundSpecifications (null when not armed or degraded, correct OSDM shape otherwise).",
        "Bruno_Collection/VERSION OTST_V2.0.43 -> OTST_V2.0.44; server 1.11.95 -> 1.11.96. Closes #218."
      ]
    },
    {
      "release": "2026.123",
      "date": "2026-05-28",
      "server": "1.11.95",
      "collection": "OTST_V2.0.43",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(expired-flow PR B): auto-expansion. When 2+ expired-X timers are armed on the same scenario, OSCAR now runs that scenario N times — one sub-run per timer, in flow order: OTO (offer) -> BTO (booking) -> ARO (add-reservation) -> ATO (add-ancillary) -> RTO (refund) -> ETO (exchange). Each sub-run's assertions are prefixed [NHF_<3-letter>_<scenario_code>]; leading NHF_ on the scenario code is stripped (no double prefix). Single-timer scenarios behave identically to v1.11.94 (no prefix, same flow).",
        "feat(expiredFlow.js): three new exports — buildAndArmExpiredFlowQueue (called by scenarioParser at scenario init, builds the per-scenario queue, disarms all timer flags except queue[0]), advanceExpiredFlowQueueOrFinish (called by each gated YAML's after-response, advances the queue + re-arms next timer + routes back to 01, or returns false to let the caller run its cross-scenario tail), nhfTestPrefix (returns the assertion-name prefix for the current queue position).",
        "feat(scenarioParser.js): sub-run continuation early-return at the top of getScenarioData — when __expiredFlowSubRunPending is set, skip the full scenario re-parse (env state is still valid; only the timer flags changed). Queue build happens after salesFlow_* / scenarioType / placeSelectionMode env vars are set so the gating functions read consistent state. Reset list extended with the three queue state vars.",
        "feat(runner.js): EXPIRED_FLOW_TIMERS budget math switches from MAX to SUM within a scenario (a scenario with N timers takes the SUM of waits + N*60s of buffer); ACROSS scenarios still takes MAX (worker runs one scenario at a time). Clamp at RUN_HARD_MAX_TIMEOUT_MS unchanged. Source line shows '<N> timers summed' when multi-timer.",
        "feat(YAMLs): 6 gated YAMLs updated to call advanceExpiredFlowQueueOrFinish before their cross-scenario tails — 02. POST Create Booking, 07. GET Booking after Fulfillments (booking-expiry tail), 09. POST Add Reservation, 10. POST Add Ancillary, 03-Refund/13. PATCH Refund Offer, 04-Exchange/11. POST Exchange Operations. All assertion names get the nhfTestPrefix() tag.",
        "feat(wizard): footer note in the Non Happy Flow section updated to explain auto-expansion + the NHF_XXX_ naming convention + the sum-vs-max budget. No data-model change.",
        "docs: Tester Guide section 4.8 expanded with a dedicated Auto-expansion subsection (sub-run order, naming convention with worked example, gating skips, wait-budget math, backwards compat).",
        "tests: bruno-expiredflow.test.js adds three test groups (buildAndArmExpiredFlowQueue, advanceExpiredFlowQueueOrFinish, nhfTestPrefix) covering all the gating + order + advance scenarios. runner-effective-timeout.test.js adds three PR-B cases (sum within scenario, max across scenarios, clamp at RUN_HARD_MAX_TIMEOUT_MS when summed exceed cap).",
        "Bruno_Collection/VERSION OTST_V2.0.42 -> OTST_V2.0.43; server 1.11.94 -> 1.11.95."
      ]
    },
    {
      "release": "2026.122",
      "date": "2026-05-28",
      "server": "1.11.94",
      "collection": "OTST_V2.0.42",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(expired-flow PR A): ship Phases 3+4+5 — four new expired-X negative tests built on the shared expiredFlow.js helper. expiredRefundOfferTest (waits past RefundOffer.validUntil, asserts 13. PATCH Refund Offer is rejected — REFUND scenarios only). expiredExchangeOfferTest (waits past ExchangeOffer.preBookableUntil — the spec-naming quirk #25 — asserts 11. POST Exchange Operations is rejected, EXCHANGE only). expiredAddReservationOfferTest (waits past the reservationOfferPart.validUntil, asserts 09. POST Add Reservation rejected). expiredAddAncillaryOfferTest (waits past the earliest ancillary-part validUntil, sourced primarily from 11. additional-offers with a fallback to the original offer, asserts 10. POST Add Ancillary rejected).",
        "feat(wizard): dedicated 'Non Happy Flow customisation' collapsible section in the scenario detail panel. All 6 expired-X timer pairs (Offer / Booking / AddReservation / AddAncillary / RefundOffer / ExchangeOffer) grouped here with consistent layout. RefundOffer / ExchangeOffer rows are scenarioType-gated (only shown for REFUND / EXCHANGE). Replaces the inline expired-X rows that were sprawling the SCENARIO PARAMETERS section.",
        "feat(runner): EXPIRED_FLOW_TIMERS table extended from 2 -> 6 entries. computeEffectiveRunTimeoutMs auto-extends the worker SIGTERM for any of the 6 timers when armed by an in-scope scenario.",
        "docs: Tester Guide section 4.8 expanded with 4 new sections (one per new test); 7.1 field-name table updated; OSDM Spec Deviations doc #25 cross-referenced.",
        "Bruno_Collection/VERSION OTST_V2.0.41 -> OTST_V2.0.42; server 1.11.93 -> 1.11.94. Next: PR B will add auto-expansion (one scenario, N timers -> N sub-runs with NHF_XXX_<scenario_code> sub-run naming convention)."
      ]
    },
    {
      "release": "2026.121",
      "date": "2026-05-28",
      "server": "1.11.93",
      "collection": "OTST_V2.0.41",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(expired-flow): generalize the #204 expired-X negative-test pattern. New shared helper Bruno_Collection/library-bruno/expiredFlow.js exports planExpiredFlow / runExpiredFlowWait / gradeExpiredFlowResponse — extracted byte-identically from the per-resource expired-booking logic that previously lived inline in 06. POST Obtaining Fulfillments. 06.yml refactored to use the helper (~70 lines inline → ~20 lines via helper); behaviour unchanged.",
        "feat(expired-offer): new expiredOfferTest + expiredOfferMaxWaitMinutes scenario fields. When on, OSCAR waits until just past the earliest OfferPart.validUntil from the selected offer (offers.js postOfferResponse now captures this into env vars), then attempts POST /bookings and asserts the provider rejects it (4xx + RFC-9457 Problem). 02. POST Create Booking short-circuits the happy path on the rejection and routes to the next scenario in the loop. The runner's computeEffectiveRunTimeoutMs now scans BOTH expiredBookingMaxWaitMinutes AND expiredOfferMaxWaitMinutes via a generic EXPIRED_FLOW_TIMERS table — any future expired-X timer plugs in here.",
        "feat(wizard): new dropdown + Max wait input for the expired-offer test in the scenario detail panel. JSON schema, scenarioParser env-var resolution, wizard default seed, and reset list updated. Tester User Guide §4.8 expanded with the offer-expiry section and a note on not combining offer + booking expiry in one scenario.",
        "docs(osdm): new entry #25 in OSDM_Spec_Deviations_Observed (\"Offer-expiry field across offer types\") documenting the spec-internal naming inconsistency between OfferPart.validUntil / RefundOffer.validUntil and ExchangeOffer.preBookableUntil — required reading before Phase 4 (expired-exchange-offer).",
        "tests: new tests/unit/bruno-expiredflow.test.js (plan/grade/wait helpers under a mocked displays + auth), and runner-effective-timeout.test.js gets three new cases for the offer-timer auto-extension. Bruno_Collection/VERSION OTST_V2.0.40 -> OTST_V2.0.41; server 1.11.92 -> 1.11.93."
      ]
    },
    {
      "release": "2026.120",
      "date": "2026-05-28",
      "server": "1.11.92",
      "collection": "OTST_V2.0.40",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#204): OAuth token watchdog — full belt-and-braces token freshness for long-running scenario series, on top of #302's expired-booking-wait refresh. Three new layers: (1) loopback endpoint POST /v1/runs/:runId/refresh-access-token now accepts ?force=1 (default: respect server-side per-tester cache). (2) library-bruno/auth.js gains refreshAccessTokenIfNeeded() that wraps the loopback call + env-var update + error logging. Wired into 01. POST Get Offer's before-request so every scenario starts with a fresh-or-cached token (~50ms cache hit). 06. POST Obtaining Fulfillments now uses the shared helper with force=true (replaces the previous inline bru.sendRequest). (3) runner.js spawns a background token-watchdog interval that ticks every TOKEN_WATCHDOG_INTERVAL_MS (default 300000 = 5 min) while the run is in flight, calls resolveAccessToken without force — the cache's safety-margin check decides whether to refetch or no-op. Cleaned up in proc.on(close|error). Skipped for bearer-mode runs. Operator opt-out: TOKEN_WATCHDOG_INTERVAL_MS=0. Bruno_Collection/VERSION OTST_V2.0.39 -> OTST_V2.0.40; server 1.11.91 -> 1.11.92.",
        "Operator action required: none. New TOKEN_WATCHDOG_INTERVAL_MS env var documented in .env.example. Chip shows 2026.120 / server-v1.11.92 / OTST_V2.0.40 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.119",
      "date": "2026-05-28",
      "server": "1.11.91",
      "collection": "OTST_V2.0.39",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#204): refresh the OAuth access token after the long wait so the late POST /fulfillments uses a fresh bearer. Tester reported 403 'not authenticated' from the provider as response to the POST after the ~15 min wait — that's the OAuth token expiring DURING the wait, not the booking being rejected for expiry. New server-side loopback endpoint POST /v1/runs/:runId/refresh-access-token (same loopback gate as /data — 127.0.0.1/::1 + no X-Forwarded-For; no session auth). resolveAccessToken takes a new forceRefresh: true option that skips the cache. Runner injects __runId + oscar_loopback_base into the Bruno env. 06.yml: after the wait completes (before Bruno fires POST /fulfillments), bru.sendRequest to the loopback endpoint, bru.setEnvVar('access_token', newToken) — Bruno re-templates {{access_token}} at request-fire time so the fulfillment uses the fresh token. After-response now distinguishes 401/403 (auth — likely refresh failed) from 4xx + Problem (genuine booking-expiry rejection) with a clear WARNING. Coverage: forceRefresh path added to tests/unit/access-token.test.js. Bruno_Collection/VERSION OTST_V2.0.38 -> OTST_V2.0.39; server 1.11.90 -> 1.11.91.",
        "Operator action required: none. Chip shows 2026.119 / server-v1.11.91 / OTST_V2.0.39 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.118",
      "date": "2026-05-28",
      "server": "1.11.90",
      "collection": "OTST_V2.0.38",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#204): the per-scenario expiredBookingMaxWaitMinutes timer (shipped in 2026.116) silently did not extend the worker SIGTERM because computeEffectiveRunTimeoutMs in runner.js read the datafile with plain fs.readFile + JSON.parse — but since OSCAR v1.11.0 (Phase 2 of issue #60) the datafile on disk is AES-256-GCM encrypted under the OSCAR1 envelope. JSON.parse(ciphertext) threw, the catch block silently swallowed the error, and the helper fell back to base RUN_TIMEOUT_MS — so every scenario opting into Max wait got SIGTERMed at 10 min regardless. Tester report: set Max wait=30 on Paxone (deadline ~15 min) → run died at exactly 10m with exit 1 and no transaction reports. Fix: helper now uses decryptFromFileAsync (same pattern as the /v1/runs POST handler in api/routes/runs.js), which handles both encrypted and legacy plaintext datafiles. Also: the runner now ALWAYS logs the effective RUN_TIMEOUT_MS chosen for the run (with base / requested / hardMax / source / helperError fields) — previously the diagnostic only fired when the extension actually fired, which is exactly when this kind of failure can't be diagnosed. New Jest test tests/unit/runner-effective-timeout.test.js pins the contract: encrypted+plaintext datafiles, sanity (off), clamping at RUN_HARD_MAX_TIMEOUT_MS, helper-error reporting, scenariosToRun=ALL, all four expiredBookingTest truthy forms. Server-only release; no Bruno collection bump. Server 1.11.89 -> 1.11.90.",
        "Operator action required: none. With Max wait set on a scenario, the runner should now extend SIGTERM up to RUN_HARD_MAX_TIMEOUT_MS (default 30 min). The execution log now shows '[runner] Effective RUN_TIMEOUT_MS = …ms; source: …' on every run. Chip shows 2026.118 / server-v1.11.90 / OTST_V2.0.38 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.117",
      "date": "2026-05-28",
      "server": "1.11.89",
      "collection": "OTST_V2.0.38",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "docs(#222 Increment 1): tester-facing guidance for OfferMode=INDIVIDUAL vs COLLECTIVE, ahead of the broader #222 build-out. Wizard hint on the Offer Mode field now spells out the OSDM-defined semantics (INDIVIDUAL = per-pax admissions, refundable individually; COLLECTIVE = shared group admissions, atomic) and the 1-pax edge case (provider may accept, fall back to INDIVIDUAL with a warning, or reject). Tester User Guide §4.3 expanded with the same content plus the spec-mandated fallback behaviour and pointer to #222 for the broader test build-out. No behaviour change — OSCAR still only validates the enum value as today; full assertions (mode fidelity, refund-per-pax restriction, split-booking endpoint) are tracked as Increments 2-5 on #222. Server-only release; no Bruno collection bump. Server 1.11.88 -> 1.11.89.",
        "Operator action required: none. Chip shows 2026.117 / server-v1.11.89 / OTST_V2.0.38 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.116",
      "date": "2026-05-28",
      "server": "1.11.88",
      "collection": "OTST_V2.0.38",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#204): per-scenario Max wait timer for the expired-booking test (tester's personal choice). New optional integer field expiredBookingMaxWaitMinutes (1..60) surfaced in the wizard next to the Expired-booking test dropdown. When set AND expiredBookingTest is on, 06.yml uses this as the wait budget instead of the server's RUN_TIMEOUT_MS, AND the runner auto-extends the worker SIGTERM to cover the wait (clamped to a new server-wide ceiling RUN_HARD_MAX_TIMEOUT_MS, default 30 min). Lets a tester opt into longer waits (Bileto/Paxone deadlines ~15 min → set 20) without needing to also raise the server env. Wired through datafile.schema.json, public/js/scenarios.js (UI + handler + default), library-bruno/scenarioParser.js (env-var resolve + reset), 02-Common Requests/06. POST Obtaining Fulfillments from Booking.yml (budget source switch + clearer SKIP WARNING), src/worker/runner.js (new computeEffectiveRunTimeoutMs helper that scans in-scope scenarios; the SAME effective ms drives the runHardDeadlineMs env injection AND the SIGTERM setTimeout — they MUST agree). New server env var RUN_HARD_MAX_TIMEOUT_MS documented in .env.example. Tester User Guide §4.8 / §7.1 updated. Bruno_Collection/VERSION OTST_V2.0.37 -> OTST_V2.0.38; server 1.11.87 -> 1.11.88.",
        "Operator action required: none for default runs. If you need a per-scenario wait > 30 min, raise RUN_HARD_MAX_TIMEOUT_MS on the server. Chip shows 2026.116 / server-v1.11.88 / OTST_V2.0.38 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.115",
      "date": "2026-05-28",
      "server": "1.11.87",
      "collection": "OTST_V2.0.37",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#204): the expired-booking test now also resolves the booking deadline from the bookingPart level. Paxone sandbox reports confirmableUntil ONLY at bookedOffers[].{admissions|reservations|ancillaries}[].confirmableUntil and not at the booking root — which actually matches OSDM's schema placement for that field. With only the booking-root fallback (#204 follow-up in 2026.112), Paxone's booking-root had neither confirmationTimeLimit nor confirmableUntil, so OSCAR took the 'no deadline found' WARNING branch and fired POST /fulfillments immediately. Now bookings.js falls back, in this order: (1) booking.confirmationTimeLimit, (2) booking.confirmableUntil, (3) the EARLIEST bookedOffers[].{admissions|reservations|ancillaries}[].confirmableUntil — because the booking effectively expires when the first part expires. Each non-standard source emits a [WARNING] documenting the vendor deviation so the report shows which shape was used. 06.yml and Tester User Guide §4.8 / §7.1 updated. Operator note: Paxone's deadline is ~15 min after creation, same as Bileto — raise RUN_TIMEOUT_MS to ≥ ~1100 s on the server before re-running, otherwise the test self-skips with the budget WARNING (by design — the runner injects runHardDeadlineMs to avoid SIGTERM mid-wait). Bruno_Collection/VERSION OTST_V2.0.36 -> OTST_V2.0.37; server 1.11.86 -> 1.11.87.",
        "Operator action required: for providers whose confirmation deadline (anywhere — root or bookingPart) exceeds the run budget (Paxone / Bileto ≈ 15 min vs default RUN_TIMEOUT_MS = 10 min), raise RUN_TIMEOUT_MS on the server before re-running expiredBookingTest. Chip shows 2026.115 / server-v1.11.87 / OTST_V2.0.37 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.114",
      "date": "2026-05-28",
      "server": "1.11.86",
      "collection": "OTST_V2.0.36",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#291, #292): two checks that catch the #287 regression class (literal '</script>' inside an inline <script> block closing the outer script early) BEFORE it ships. (#292) scripts/lint-inline-scripts.js — a Node-only static linter wired into the existing `lint` npm script: walks Oscar_Server/public/*.html with HTML5 script-data state rules, compares raw '</script>' counts to walker-matched closes, names file+line of any stray. Runs in CI Lint/audit/test. (#291) tests/unit/dashboard-pages.test.js — Jest test that does the same walk, asserts every <script> is properly closed, raw and matched close counts match, and run-detail.html's outer inline block reaches its tail marker `poll();` (proving the parser saw the script through to the end). Note: #291 originally proposed Playwright; this implementation deliberately avoids adding @playwright/test (and re-resolving package-lock.json) — the deterministic walker catches the exact regression class at zero dep cost. A real-browser smoke test can be a separate scope if richer DOM-execution coverage is wanted. Server 1.11.85 -> 1.11.86; no Bruno collection change.",
        "Operator action required: none. Chip shows 2026.114 / server-v1.11.86 / OTST_V2.0.36 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.113",
      "date": "2026-05-28",
      "server": "1.11.85",
      "collection": "OTST_V2.0.36",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#287): HOTFIX — the run-detail page was broken on releases 2026.111 and 2026.112 because run-detail.html contained a literal '</script>' substring inside the inline <script> block (in the renderMessage template literal that emits the inline JSON payload envelope for the Tree viewer). HTML parsers terminate the script element at any '</script>' regardless of JS string context, so the outer script ended early and the rest of the inline JS bled into the page as visible text — exactly the broken view a tester reported. Fix: every literal '</script' inside the inline block (the template, plus three comments) now spells '<\\/script' instead. JS interprets '\\/' as '/', so the emitted HTML and resulting DOM are correct. Server-only release; no Bruno collection change. Server 1.11.84 -> 1.11.85.",
        "Operator action required: none. Hard-refresh (Ctrl+Shift+R) the dashboard once Watchtower restarts so the cached broken run-detail.html is replaced. Chip shows 2026.113 / server-v1.11.85 / OTST_V2.0.36."
      ]
    },
    {
      "release": "2026.112",
      "date": "2026-05-28",
      "server": "1.11.84",
      "collection": "OTST_V2.0.36",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#204): the expired-booking test now reads the booking-level confirmation deadline from EITHER booking.confirmationTimeLimit (OSDM-standard) OR booking.confirmableUntil (vendor field at booking level used by e.g. the Bileto sandbox). Previously OSCAR only read confirmationTimeLimit; against a provider that uses confirmableUntil at the booking level (which the OSDM spec actually defines at the bookingPart level), the env var was never set so the #204 test silently took the 'no deadline found' WARNING branch and went straight to POST /fulfillments without waiting. bookings.js postCreateBookingResponse now accepts either field name and emits a [WARNING] when the vendor-deviation path is taken (so the deviation is visible in the report). 06.yml WARNING text updated to name both fields. Tester User Guide §4.8 updated. NOTE: the Bileto sandbox has a ~15 min confirmation deadline; with the default RUN_TIMEOUT_MS of 10 min the test still self-skips with the budget WARNING — raise RUN_TIMEOUT_MS to >= ~1100 s to actually exercise the wait. Bruno_Collection/VERSION OTST_V2.0.35 -> OTST_V2.0.36; server 1.11.83 -> 1.11.84.",
        "Operator action required: for providers whose confirmation deadline exceeds the run budget (Bileto sandbox = ~15 min vs default RUN_TIMEOUT_MS = 10 min), raise RUN_TIMEOUT_MS on the server before re-running expiredBookingTest. Chip shows 2026.112 / server-v1.11.84 / OTST_V2.0.36 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.111",
      "date": "2026-05-28",
      "server": "1.11.83",
      "collection": "OTST_V2.0.35",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#287): structured JSON tree view in the run-detail HTTP Traffic panel (PB-determination aid). Each request/response body now offers a [Raw | Tree] toggle in its header. Raw is the DEFAULT (no behaviour change for users who don't opt in); clicking Tree lazy-loads vanilla-jsoneditor v3.12.0 (ISC) from /vendor/vanilla-jsoneditor/standalone.js and mounts a read-only collapsible tree with built-in key/value search. Guards: a body that isn't valid JSON or is > 2 MB shows the toggle DISABLED with a tooltip explaining why (Raw still works). The standalone bundle is VENDORED under public/vendor/vanilla-jsoneditor/standalone.js (not an npm dep), served by the existing express.static(PUBLIC_DIR), so CSP stays 'self'; no eval, no inline scripts, no CSP changes, no package-lock churn. Bundle (~1.26 MB, ~300 KB gzipped) is fetched only when a tester clicks Tree the first time, then cached. Server-only release; no Bruno collection change. Server 1.11.82 -> 1.11.83.",
        "Operator action required: none. After Watchtower restarts, testers can click Tree on any JSON body to get the structured view. The chip shows 2026.111 / server-v1.11.83 / OTST_V2.0.35."
      ]
    },
    {
      "release": "2026.110",
      "date": "2026-05-27",
      "server": "1.11.82",
      "collection": "OTST_V2.0.35",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#253): v3.8 FulfillmentDocument cross-reference integrity check. OSDM v3.8 moves FulfillmentDocument from a nested fulfillment.fulfillmentDocuments[] to a sibling FulfillmentResponse.fulfillmentDocuments[] / Booking.fulfillmentDocuments[], with fulfillments[].fulfillmentDocumentRefs[] pointing at the sibling .id. validateFulfillments() now accepts the sibling array and asserts each fulfillmentDocumentRef resolves to a sibling fulfillmentDocuments[].id; an unresolved ref or a missing sibling array (when refs are present) is a FAIL with a clear diagnostic listing the unresolved ref(s) and the available sibling ids. Wired through every caller: postCreateBookingResponse (booking.fulfillmentDocuments), 06. POST Obtaining Fulfillments + 04-Exchange/14. POST Obtaining Fulfillments (FulfillmentResponse.fulfillmentDocuments), exchanges.js, refunds.js (exchangeOffer/refundOffer.fulfillmentDocuments). Backwards-compatible: when the sibling array is not supplied (legacy callers / pre-v3.8 providers using the deprecated nested form), the new cross-ref check is SKIPPED with an informational log. Bruno_Collection/VERSION OTST_V2.0.34 -> OTST_V2.0.35; server 1.11.81 -> 1.11.82.",
        "Operator action required: none. Chip shows 2026.110 / server-v1.11.82 / OTST_V2.0.35 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.109",
      "date": "2026-05-27",
      "server": "1.11.81",
      "collection": "OTST_V2.0.34",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#210 pt.3): hardened the data-file trip-datetime parsing. A minimal/hand-authored data file that omitted startDatetime or endDatetime in a tripRequirement previously crashed scenarioParser.js with an opaque 'TypeError: Cannot read properties of undefined (reading replace)'. The %TRIP_DATE% substitution now runs through a guard (subTripDate) that throws a clear, actionable message naming the missing field and the trip context (SEARCH/SPECIFICATION leg N) and pointing to the Test Config UI. UI-generated data files always include both datetimes and are unaffected (no behaviour change for them). Audit of the other #210 points: configurable logging (loggingType FULL/INFO/DEBUG/ERROR) implemented; passenger.reference now consumed as OSDM externalRef; empty trip filters no longer emitted (carrier/vehicle/dataFilter built only when a value exists). Bruno_Collection/VERSION OTST_V2.0.33 -> OTST_V2.0.34; server 1.11.80 -> 1.11.81.",
        "Operator action required: none. Chip shows 2026.109 / server-v1.11.81 / OTST_V2.0.34 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.108",
      "date": "2026-05-27",
      "server": "1.11.80",
      "collection": "OTST_V2.0.33",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#204): expired-booking negative test (OTST_TI_EXPIRED_BOOKING). New scenario field expiredBookingTest (off/on). When on, after the booking is created OSCAR WAITS until just past booking.confirmationTimeLimit (+15s buffer; the wait is derived from the deadline per the issue), then POST /fulfillments must be REJECTED (4xx + RFC-9457 Problem; hard FAIL if the provider fulfills an expired booking), and GET /bookings must show admissions/reservations EXPIRED/RELEASED/CANCELLED (or a 404 purge). If waiting until the deadline would overrun the run budget, the test SKIPS with a [WARNING] telling the operator to raise RUN_TIMEOUT_MS (the runner now injects runHardDeadlineMs into the Bruno env so the wait can make this decision). Default off; existing scenarios unaffected. Bruno_Collection/VERSION OTST_V2.0.32 -> OTST_V2.0.33; server 1.11.79 -> 1.11.80.",
        "Operator action required: none for default runs. To exercise expiredBookingTest against a provider whose confirmationTimeLimit exceeds ~9 min, raise RUN_TIMEOUT_MS accordingly. Chip shows 2026.108 / server-v1.11.80 / OTST_V2.0.33 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.107",
      "date": "2026-05-27",
      "server": "1.11.79",
      "collection": "OTST_V2.0.32",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#202): fulfillment-document validation now recognises the OSDM 'content' field. The check (bookings.js validateFulfillments) previously accepted only downloadLink OR the non-standard 'rawData', so a spec-conformant provider returning the inline document in OSDM's 'content' (base64) field was wrongly FAILED — the exact field #202 names. It now accepts content (OSDM) OR downloadLink (OSDM) OR rawData (vendor extension), and the report states EXACTLY which field delivered the payload and whether it is OSDM-standard or a vendor extension; a document delivered only via 'rawData' is accepted but flagged with a [WARNING] (retrievable but not OSDM-conformant). Bruno_Collection/VERSION OTST_V2.0.31 -> OTST_V2.0.32; server 1.11.78 -> 1.11.79.",
        "Operator action required: none. Chip shows 2026.107 / server-v1.11.79 / OTST_V2.0.32 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.106",
      "date": "2026-05-27",
      "server": "1.11.78",
      "collection": "OTST_V2.0.31",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "docs(#216): newcomer-readability pass over library-bruno. Added a concise file-objective header (what the module does + where it sits in the offer->booking->fulfillment flow) to the 12 modules that lacked one (offers, bookings, fulfillments, passengers, refunds, exchanges, displays, requestsBuilder, scenarioParser, validators, model, schema, reportGenerator) so a new contributor can understand each module's purpose at a glance. Removed the dead, empty, unreferenced library-bruno/swagger.js (the Swagger logic lives in validators.js). Comments/headers only — ZERO runtime/behaviour change. Bruno_Collection/VERSION OTST_V2.0.30 -> OTST_V2.0.31; server 1.11.77 -> 1.11.78.",
        "Operator action required: none. Chip shows 2026.106 / server-v1.11.78 / OTST_V2.0.31 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.105",
      "date": "2026-05-27",
      "server": "1.11.77",
      "collection": "OTST_V2.0.30",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258): PASSENGER negative-probe sweep (companion to the purchaser sweep). With requestedInformationProbe=invalid, 03. PATCH Multi Passenger now tests each passenger field one-by-one on passenger 0 within a SINGLE run: a valid baseline for every field with exactly ONE corrupted per pass (gender->ZZZ, dateOfBirth->not-a-date, email->not-an-email, phoneNumber->not-a-phone, firstName/lastName->omitted), graded on its own line ([passenger0.<field>]), then loops back to PATCH for the next field and stops after the last. Severity per validateProblemResponse: gender/dateOfBirth FAIL (enum/format), email/phoneNumber WARN (unconstrained string), firstName/lastName FAIL (required). STRICTLY gated to requestedInformationProbe=invalid; off/omit/happy paths are byte-identical. NOTE: built without live-flow validation (touches the core passenger PATCH step) — validate against a sandbox before relying on it. Bruno_Collection/VERSION OTST_V2.0.29 -> OTST_V2.0.30; server 1.11.76 -> 1.11.77.",
        "Operator action required: none. Chip shows 2026.105 / server-v1.11.77 / OTST_V2.0.30 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.104",
      "date": "2026-05-27",
      "server": "1.11.76",
      "collection": "OTST_V2.0.29",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258): purchaser negative-probe SWEEP — test every purchaser parameter one-by-one in a SINGLE run. With bookingPurchaserMode=invalid, the purchaser write step now loops over each field (firstName, lastName, email, phoneNumber), corrupting exactly ONE per pass (the rest valid) and grading it on its own line, then loops back to 12. GET Booking Purchaser for the next field — reusing the GET-adaptive create-or-update each pass so it is robust to rejections. email/phoneNumber are sent invalid (unconstrained string → WARN if accepted); firstName/lastName are OMITTED (required in PersonDetail → FAIL if not rejected). validateProblemResponse() gains an optional `label` so each field shows as its own assertion ('[purchaser.email]', …). No need to create one scenario per parameter. Only affects bookingPurchaserMode=invalid. Bruno_Collection/VERSION OTST_V2.0.28 -> OTST_V2.0.29; server 1.11.75 -> 1.11.76.",
        "Operator action required: none. Chip shows 2026.104 / server-v1.11.76 / OTST_V2.0.29 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.103",
      "date": "2026-05-27",
      "server": "1.11.75",
      "collection": "OTST_V2.0.28",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258): provider-fair grading of the requestedInformation negative probe. validateProblemResponse() now FAILS the 'provider must reject' assertion only when a rejection is REQUIRED — i.e. a demanded field is MISSING (omit probe) or an OSDM-constrained field is violated (gender enum / dateOfBirth format). For a malformed value in an UNCONSTRAINED string field (firstName/lastName/email/phoneNumber — bare type:string in the spec, no pattern/format) a non-rejection is now a WARNING, not a FAIL, because a provider that accepts it is still OSDM-conformant. N2 (RFC-9457 Problem shape) and N3 (field pointer) are graded only when an error body is actually returned, and follow the same hard/soft severity. Only changes report severity for negative-probe scenarios; happy-flow runs unaffected. Bruno_Collection/VERSION OTST_V2.0.27 -> OTST_V2.0.28; server 1.11.74 -> 1.11.75.",
        "Operator action required: none. Chip shows 2026.103 / server-v1.11.75 / OTST_V2.0.28 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.102",
      "date": "2026-05-27",
      "server": "1.11.74",
      "collection": "OTST_V2.0.27",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#258/#203): bookingPurchaserMode=invalid now actually sends an invalid purchaser. requestsBuilder.buildBookingPurchaserBody() forced a bad email only when the email was empty (!body.detail.contact.email), so a scenario whose purchaser already had a valid email passed VALID data through and the negative probe never exercised the provider's validation. The invalid mode now OVERWRITES the email with 'not-an-email' so the PATCH/POST purchaser body is guaranteed invalid and the provider must reject with an RFC-9457 Problem (graded by validateProblemResponse). Only affects bookingPurchaserMode=invalid. Bruno_Collection/VERSION OTST_V2.0.26 -> OTST_V2.0.27; server 1.11.73 -> 1.11.74.",
        "Operator action required: none. Chip shows 2026.102 / server-v1.11.74 / OTST_V2.0.27 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.101",
      "date": "2026-05-27",
      "server": "1.11.73",
      "collection": "OTST_V2.0.26",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258/#203): purchaser create-or-update (upsert) — stop guessing POST vs PATCH. The deferred-purchaser flow now PROBES first: new step '12. GET Booking Purchaser' (getBookingPurchaser) checks whether a purchaser exists on the booking, then routes to '13. PATCH Booking Purchaser' (update, when GET returns 2xx) or '14. POST Booking Purchaser' (create, when GET returns 404/none). This works on both provider styles — those that materialise an empty purchaser on the booking (PATCH-update; Bileto) and those that don't (POST-create) — without hardcoding a method. Shared body assembly moved to requestsBuilder.buildBookingPurchaserBody(); 04. GET Passenger routes to the GET probe; smart-run filter gates each step (write steps run only for the method the probe selected). Only affects bookingPurchaserMode=deferred/invalid (default inline unchanged). Bruno_Collection/VERSION OTST_V2.0.25 -> OTST_V2.0.26; server 1.11.72 -> 1.11.73.",
        "Operator action required: none. Chip shows 2026.101 / server-v1.11.73 / OTST_V2.0.26 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.100",
      "date": "2026-05-27",
      "server": "1.11.72",
      "collection": "OTST_V2.0.25",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#258/#203): the deferred-purchaser step now uses PATCH (patchBookingPurchaser) instead of POST (postBookingPurchaser). For a booking created WITHOUT a purchaser, POST-create is the OSDM-canonical call, but providers that already materialise an (empty) purchaser on the booking (e.g. Bileto) 500 on POST-create because the resource already exists; PATCH-update is the call that works there. Step renamed '12. POST Booking Purchaser' -> '12. PATCH Booking Purchaser'; method POST -> PATCH; 04. GET Passenger routes to the new name; body assembly unchanged. Bruno_Collection/VERSION OTST_V2.0.24 -> OTST_V2.0.25; server 1.11.71 -> 1.11.72.",
        "Operator action required: none. Only affects scenarios with bookingPurchaserMode=deferred/invalid (default inline is unchanged). Chip shows 2026.100 / server-v1.11.72 / OTST_V2.0.25 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.99",
      "date": "2026-05-27",
      "server": "1.11.71",
      "collection": "OTST_V2.0.24",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#208, regression — CRITICAL): valid OAuth credentials failed on every run. The 2026.97 fix added users.cached_token_cred_fp, but the ALTER was placed inside the ALREADY-APPLIED v12 migration; the version-gated runner skips applied migrations, so the column was never created on existing DBs. resolveAccessToken() then persisted the token cache with 'UPDATE users SET ... cached_token_cred_fp = ?', which threw 'no such column: cached_token_cred_fp' and FAILED EVERY oauth2 run — including valid, unchanged credentials (e.g. Bileto). Fix: (1) new DB migration v20 'users-cached-token-cred-fp' adds the column on existing DBs (idempotent; companies too for schema parity); (2) access-token.js now treats cache persistence as best-effort — a token just fetched is returned even if the cache write fails, so a DB-bookkeeping error can never fail an otherwise-valid auth. Regression test added (caching throws -> token still returned). Server 1.11.70 -> 1.11.71; Bruno collection unchanged (OTST_V2.0.24).",
        "Operator action required: none. The migration runs automatically on boot; the first oauth2 run per tester re-fetches once and re-populates the cache. Chip shows 2026.99 / server-v1.11.71 / OTST_V2.0.24 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.98",
      "date": "2026-05-27",
      "server": "1.11.70",
      "collection": "OTST_V2.0.24",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258, #203): purchaser-aware requestedInformation + a dedicated purchaser-on-booking step. The requestedInformation engine (library-bruno/requestedInformation.js) is now ROOT-AWARE: a leaf's root decides its subject — passengerSpecifications[i] (an indexed passenger, unchanged) or purchaser[…] (the single purchaser object, index ignored). Each kind has its own evaluation subject, report label ('the purchaser') and auto-feed/probe channel; passenger behaviour and all existing engine tests are unchanged.",
        "New scenario field bookingPurchaserMode (inline | deferred | omit | invalid), authored in the scenario detail panel (scenarios.js) and documented in datafile.schema.json. 'inline' (default) sends the purchaser in the POST /bookings request exactly as before. 'deferred' OMITS it at booking and then POSTs it to /bookings/{id}/purchaser (postBookingPurchaser) to satisfy any purchaser requestedInformation — the OSDM-correct way to exercise a provider that requests purchaser data. 'omit' never supplies it. 'invalid' POSTs a deliberately bad purchaser and asserts the provider rejects with a conformant RFC-9457 Problem.",
        "New Bruno step '12. POST Booking Purchaser' (gated in the smart-run filter, routed from 04. GET Passenger before fulfillment; fully inert when inline/omit) — closes #203 (purchaser endpoints exercised). buildBookingRequest gates the inline purchaser on the new mode; the booking-level RI handler (bookings.js) routes purchaser leaves to the new channel.",
        "Operator action required: none. New scenarios default to bookingPurchaserMode=inline, so existing scenarios behave exactly as before. Bruno_Collection/VERSION OTST_V2.0.23 -> OTST_V2.0.24; server 1.11.69 -> 1.11.70; chip shows 2026.98 / server-v1.11.70 / OTST_V2.0.24 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.97",
      "date": "2026-05-27",
      "server": "1.11.69",
      "collection": "OTST_V2.0.23",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#208, root cause): invalidate the per-tester OAuth token cache when credentials change (server-only; Bruno collection unchanged, OTST_V2.0.23). Real cause of the 'invalid credentials still ran all test cases' report: worker/access-token.js resolveAccessToken reused a still-valid CACHED token (cached_token_enc) regardless of whether the credentials had changed, so switching to invalid creds within the token TTL was never exercised (the token request is fetched server-side before Bruno and never appears in the report; fetchToken DOES throw on a real 401, so only the cache could have returned a token). Now a SHA-256 fingerprint of the credentials (profile + token_url + client_id + client_secret + scope + extra; secret hashed one-way, never stored) is saved with the cached token (new users.cached_token_cred_fp column) and the cache is reused only when the fingerprint matches; a credential change forces a re-fetch, so invalid credentials now throw -> the run is marked FAILED before Bruno with '[runner] Auth failed: HTTP 401 ...' in the Execution Log and no scenarios run. Schema + migration added (existing rows have NULL fp -> one safe re-fetch post-deploy). Server 1.11.68 -> 1.11.69; Bruno collection unchanged.",
        "Operator action required: none. Hard-refresh after Watchtower promotes :stable. Chip shows 2026.97 / server-v1.11.69 / OTST_V2.0.23."
      ]
    },
    {
      "release": "2026.96",
      "date": "2026-05-27",
      "server": "1.11.68",
      "collection": "OTST_V2.0.23",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#208 follow-up): fail-fast on an auth rejection (401/403) mid-flow. The 2026.95 fix stops a run when the OAuth token can't be obtained, but a bearer/static token that is present-but-expired/revoked can't be detected up-front (the server only decrypts it; it can't know it is dead without calling the provider), so the run proceeded and every request 401/403'd. library-bruno/auth.js gains checkAuthRejection(res, reqName), now called from the collection-level after-response (opencollection.yml) for every request: on the first 401/403 from a non-token request it logs a clear [ERROR] ('access/bearer token invalid or expired — update it in Profile > API Configuration'), records a FAILING 'Authentication accepted' assertion, and calls bru.runner.stopExecution() so the dead token does not cascade. Keyed strictly on 401/403 (a rejected credential) — 404/400 (wrong endpoint / bad request) and the token-acquisition request itself are left alone; the failing request is still recorded in the report. Bruno_Collection/VERSION OTST_V2.0.22 -> OTST_V2.0.23; server 1.11.67 -> 1.11.68.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.96 / server-v1.11.68 / OTST_V2.0.23 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.95",
      "date": "2026-05-27",
      "server": "1.11.67",
      "collection": "OTST_V2.0.22",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#208): clear, fail-fast diagnostic when the access token cannot be obtained (invalid OAuth credentials). Previously each vendor 'Access Token' request only console.error'd on a missing access_token — no assertion, no message, no stop — so invalid credentials let the run continue and cascade into misleading 4xx errors on every downstream request (and the success path logged the token value, a leak). New library-bruno/auth.js handleAccessTokenResponse(res, {vendor}) is now called by all six 00-Access Token requests (Benerail/Bileto/Chaps/Paxone/Sqills/Turnit): on success it stores access_token and records a passing assertion WITHOUT logging the token; on failure (non-2xx or no token, trying access_token/accessToken/token) it emits a clear [ERROR] naming the likely cause (client_id/client_secret/scope/token URL), surfaces the provider's OAuth error/error_description, records a FAILING 'access token acquired' assertion, clears any stale token, and calls bru.runner.stopExecution() so the cascade never happens. Token request http definitions (endpoints/grants/bodies) unchanged. Bruno_Collection/VERSION OTST_V2.0.21 -> OTST_V2.0.22; server 1.11.66 -> 1.11.67.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.95 / server-v1.11.67 / OTST_V2.0.22 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.94",
      "date": "2026-05-27",
      "server": "1.11.66",
      "collection": "OTST_V2.0.21",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#250, #251): two offer/booking conformance checks. #250 — after POST /bookings/{id}/fulfillments, the GET /bookings/{id} must embed the generated fulfillments (the provider has to keep the booking object updated). library-bruno/bookings.js validateFulfillments + postCreateBookingResponse gain a requireFulfillments flag; the '07. GET Booking after Fulfillments' step now passes it true, so an empty booking.fulfillments after fulfillment FAILS instead of silently passing (it stays lenient pre-fulfillment / for other callers). #251 — offer->trip link: tripCoverage is optional on an offer part, but when present OSDM requires coveredTripId (TripCoverage.required=[coveredTripId]); offers.js validateOfferParts now asserts tripCoverage.coveredTripId is a non-empty string when tripCoverage is present, and WARNs (does not fail) when the offer carries no tripCoverage at all (clients then derive the link from offerParts). Bruno_Collection/VERSION OTST_V2.0.20 -> OTST_V2.0.21; server 1.11.65 -> 1.11.66.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.94 / server-v1.11.66 / OTST_V2.0.21 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.93",
      "date": "2026-05-27",
      "server": "1.11.65",
      "collection": "OTST_V2.0.20",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#254): a fulfillment document may carry its payload as EITHER a downloadLink (URI) OR inline rawData (content) — fare/eticket providers can return either. library-bruno/bookings.js validateFulfillments previously hard-required doc.downloadLink to be a non-empty string, failing valid rawData-only documents. It now requires at least one of downloadLink / rawData to be present (medium/type/format checks unchanged) and logs which is present. Bruno_Collection/VERSION OTST_V2.0.19 -> OTST_V2.0.20; server 1.11.64 -> 1.11.65.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.93 / server-v1.11.65 / OTST_V2.0.20 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.92",
      "date": "2026-05-27",
      "server": "1.11.64",
      "collection": "OTST_V2.0.19",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#223): flexibility-based offer selection no longer depends on the optional offerSummary. library-bruno/offers.js gains deriveOfferFlexibilityFromProducts(offer) and offerFlexibility(offer): the latter returns offerSummary.overallFlexibility when present, otherwise derives the offer's overall flexibility from its products. Aggregation is MOST RESTRICTIVE wins (NON_FLEXIBLE > SEMI_FLEXIBLE > FULL_FLEXIBLE) — a multi-leg journey is only as flexible as its least-flexible leg (e.g. TGV FULL_FLEXIBLE + TER NON_FLEXIBLE -> NON_FLEXIBLE); unknown/vendor flexibility values are ignored. selectAndSetOffer now filters and asserts on offerFlexibility() (so providers that omit offerSummary still drive selection, instead of dropping every offer and falling back to offers[0] with a failing assertion), and logs a clear [INFO] line stating the flexibility was derived from offer products when offerSummary is absent. validateOfferParts' flexibility consistency check was corrected to the same most-restrictive derivation (it previously used a most-FLEXIBLE-wins heuristic that mis-classified mixed offers) and is now skipped (logged) rather than hard-failing when offerSummary is absent. Jest: tests/unit/bruno-offer-flexibility.test.js. Bruno_Collection/VERSION OTST_V2.0.18 -> OTST_V2.0.19; server 1.11.63 -> 1.11.64.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.92 / server-v1.11.64 / OTST_V2.0.19 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.91",
      "date": "2026-05-27",
      "server": "1.11.63",
      "collection": "OTST_V2.0.18",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "ui(#258, Phase 3c-2): scenario-authoring control for the requestedInformation negative probe (server-only; Bruno collection unchanged, OTST_V2.0.18). public/js/scenarios.js: added requestedInformationProbe to the ENUMS (off/omit/invalid), a labelled dropdown in the scenario detail panel (auto-persists via the set-scenario handler) with an explanatory hint, and initialised the field to null in wizard-generated scenarios. The datafile schema + Bruno-side behaviour shipped in 2026.90; this only makes the probe selectable in the UI instead of hand-editing the data file. No behaviour change for scenarios that leave it off. Server 1.11.62 -> 1.11.63.",
        "Operator action required: none. Hard-refresh after Watchtower promotes :stable. Chip shows 2026.91 / server-v1.11.63 / OTST_V2.0.18."
      ]
    },
    {
      "release": "2026.90",
      "date": "2026-05-27",
      "server": "1.11.62",
      "collection": "OTST_V2.0.18",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258, Phase 3c): negative-flow probe for requestedInformation. A new per-scenario field requestedInformationProbe = off (default) | omit | invalid (datafile.schema.json + scenarioParser.js env var). With omit/invalid, OSCAR deliberately withholds or sets invalid values for the demanded passenger fields (instead of auto-feeding) so the gated step is submitted with bad data, then asserts the provider rejects it correctly. library-bruno/requestedInformation.js: processRequestedInformation now takes mode ('autofeed'|'omit'|'invalid'; legacy autoFeedOn still honoured) and returns probeTargets; new invalidValueForField (gender=ZZZ, email=not-an-email, dateOfBirth=not-a-date; names/type fall back to omit) and validateProblemResponse (Group N: N1 client-error 4xx [FAIL], N2 RFC-9457 Problem with title/detail/code [FAIL], N3 error identifies the offending field via Problem.pointers or detail text [WARN], N4 message non-empty). offers.js/bookings.js compute the mode, apply the probe, and accumulate requestedInfoProbeTargets. The PATCH Multi Passenger step (03) is now probe-aware: when a probe is active AND a field was actually withheld/corrupted, it grades the rejection (validateProblemResponse) and stops the chain; when the probe is off (default for all existing scenarios) the happy path is byte-identical. Authoring UI dropdown follows in 3c-2. Jest extended (invalidValueForField, probe modes, validateProblemResponse incl. pointers). Bruno_Collection/VERSION OTST_V2.0.17 -> OTST_V2.0.18; server 1.11.61 -> 1.11.62.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.90 / server-v1.11.62 / OTST_V2.0.18 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.89",
      "date": "2026-05-27",
      "server": "1.11.61",
      "collection": "OTST_V2.0.17",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258, Phase 3a+3b): auto-feed requestedInformation + static conformance assertions. library-bruno/requestedInformation.js gains: staticIssues(ast, passengerCount) (index-range + unknown-attribute checks), sampleValueForField (valid sample values; gender from the OSDM enum [MALE,FEMALE,X]; type-aware dateOfBirth), applyAutoFeed (fills empty mappable update* fields without overwriting tester data), and processRequestedInformation(...) — a dependency-injected orchestrator (assert/log callbacks) used by both handlers. offers.js validateOfferParts and bookings.js postCreateBookingResponse now: (S1) assert type string<=32768, (S2) assert the expression parses against the OSDM grammar [FAIL], (S4) assert numeric passenger indices are in range [FAIL], (S3) WARN on attributes OSCAR does not recognise; then evaluate against the passenger data OSCAR will send and, by default (auto-feed ON), AUTO-PROVIDE missing demanded fields into passengerAdditionalData (the PATCH Multi Passenger body), re-enabling skipPatchPassengerRequest so the values are actually sent. The report documents what was auto-provided (field, passenger, value). A booking-level P2 check WARNs if the provider re-requests a field OSCAR already provided. Auto-feed is suppressed when a negative probe (requestedInformationProbe != off) is active (Phase 3c, next). Unmappable demands (e.g. taxId) are WARNed, not auto-fed. Jest extended (staticIssues/sampleValueForField/applyAutoFeed/processRequestedInformation with mock sinks). Bruno_Collection/VERSION OTST_V2.0.16 -> OTST_V2.0.17; server 1.11.60 -> 1.11.61.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.89 / server-v1.11.61 / OTST_V2.0.17 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.88",
      "date": "2026-05-27",
      "server": "1.11.60",
      "collection": "OTST_V2.0.16",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258, Phase 2): evaluate requestedInformation against the passenger data OSCAR will send and WARN on unmet requirements. Builds on Phase 1 (2026.87). library-bruno/requestedInformation.js gains evaluateRequestedInformation(ast, model) -> { satisfied, unmetLeaves } (AND/OR logic; ANY index requires every passenger; leaf resolution checks both detail.contact.X and the flat detail.X so a 3.1+ contact demand is satisfied by 3.0 flat data and vice-versa, mirroring #231) and buildPassengerModelFromAdditionalData(additional, specs) which normalises the scenario's passengerAdditionalData (update* fields) + offerPassengerSpecifications type into an OSDM-ish passenger shape. offers.js validateOfferParts now evaluates each offer part's expression against the scenario data and, when not satisfied, WARNs per passenger with the exact field to set; bookings.js postCreateBookingResponse evaluates the booking-level expression against the booking's own passengers. WARN-only by design: a scenario may legitimately omit an optional field, so an unmet requirement is informational (predicts a likely downstream 400) rather than a server non-conformance; absence remains a non-failure. Auto-injecting the demanded data stays a later, separate phase. Jest: tests/unit/bruno-requestedinformation.test.js extended with evaluator + model-builder cases (single/ANY/OR, flat-vs-contact fallback, unmet reporting). Bruno_Collection/VERSION OTST_V2.0.15 -> OTST_V2.0.16; server 1.11.59 -> 1.11.60.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.88 / server-v1.11.60 / OTST_V2.0.16 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.87",
      "date": "2026-05-27",
      "server": "1.11.59",
      "collection": "OTST_V2.0.15",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#258, Phase 1): surface OSDM requestedInformation. The provider can advertise, via the requestedInformation string on each offer part (AbstractOfferPart) and on the post-booking response (Booking), which passenger data must be populated before the next step (provisional booking -> confirmation) — a boolean expression over passengerSpecifications[i] paths (AND/OR/grouping, numeric or ANY index; a leaf is true when that attribute is set). New pure module library-bruno/requestedInformation.js parses the expression, renders it for humans, maps each leaf to the OSCAR scenario field (firstName/lastName/gender/dateOfBirth/email/phoneNumber; keyed on the last path segment so detail.email and detail.contact.email both resolve), and Layer-1 type-checks the raw string (string, <=32768). Wired into offers.js validateOfferParts (per offer part) and bookings.js postCreateBookingResponse (booking level): when present, a Layer-1 type assertion runs and a tester-facing line states exactly what to set and on which passenger, e.g. \"set 'gender' on passenger 0\". Absence is not a failure (nullable/optional); unmet requirements are NOT failed in this phase (evaluation + WARN is Phase 2; auto-injection is a later phase). Unmappable fields (e.g. taxId) are flagged as a scenario-authoring gap rather than ignored. No behavioural change to existing flows beyond the new optional Layer-1 check and logging. Jest: tests/unit/bruno-requestedinformation.test.js covers the parser/describer/summariser incl. the four spec examples. Bruno_Collection/VERSION OTST_V2.0.14 -> OTST_V2.0.15; server 1.11.58 -> 1.11.59.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.87 / server-v1.11.59 / OTST_V2.0.15 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.86",
      "date": "2026-05-26",
      "server": "1.11.58",
      "collection": "OTST_V2.0.14",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "chore: dependency + repo cleanup release (no functional behaviour change). (1) Dropped the uuid dependency entirely in favour of Node's built-in crypto.randomUUID(): every call site used uuid.v4() with no buffer, and the package could not be upgraded (uuid v7+ is ESM-only and breaks OSCAR's CommonJS — the documented PR #110 'SyntaxError: Unexpected token export'). Replaced `const { v4: uuidv4 } = require('uuid')` with `const { randomUUID: uuidv4 } = require('node:crypto')` across 9 src files + 10 test files (call sites unchanged; randomUUID returns an identical RFC-4122 v4 string), and removed uuid from package.json + package-lock.json. This permanently resolves Dependabot alert #1 (uuid < 11.1.1 buffer-bounds in v3/v5/v6, to which OSCAR was never exposed) and obsoletes Dependabot PR #191. (2) Merged Dependabot updates: nodemailer 8.0.7 -> 8.0.9 (patch, #192) and pino 9.5.0 -> 10.3.1 (major, #193; logger.js uses only stable pino APIs and Node 22 satisfies v10's engine floor). (3) Tracked the three sibling Bruno folder.yml files (data_base, json_validator, library-bruno) so collection folder ordering is deterministic; gitignored the local 'Dev working documents/' scratch folder. Server 1.11.57 -> 1.11.58; Bruno_Collection/VERSION OTST_V2.0.13 -> OTST_V2.0.14.",
        "Operator action required: none. Hard-refresh after Watchtower promotes :stable. Chip shows 2026.86 / server-v1.11.58 / OTST_V2.0.14."
      ]
    },
    {
      "release": "2026.85",
      "date": "2026-05-26",
      "server": "1.11.57",
      "collection": "OTST_V2.0.13",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#231): passenger email/phone read is now version-agnostic. library-bruno/passengers.js patchMultiPassengerResponse validated the echoed email/phone behind parseFloat(osdmVersion) >= 3.4, but the OSDM PersonDetail change landed at 3.1 (contact/ContactDetail added; top-level email/phoneNumber marked deprecated but still returned). The 3.4 boundary false-failed on 3.1-3.3 servers returning contact-only, and parseFloat('3.0.4') collapses to 3 so the reported 3.0.4 failure read the wrong field. Replaced the version branch with a contact-first/flat-fallback read (detail.contact?.email ?? detail.email ?? '' and the same for phoneNumber), matching the already-robust purchaser read in fulfillments.js — no version guessing, correct across 3.0.x and 3.1+. Build side (scenarioParser.js DetailContact/Contact vs flat Detail at the 3.4 boundary) unchanged: flat is accepted/deprecated at 3.1-3.3 and correct at 3.0.x, so requests stay valid. Bruno_Collection/VERSION OTST_V2.0.12 -> OTST_V2.0.13; server 1.11.56 -> 1.11.57.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.85 / server-v1.11.57 / OTST_V2.0.13 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.84",
      "date": "2026-05-26",
      "server": "1.11.56",
      "collection": "OTST_V2.0.12",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "ui(#194): scenario-authoring + admin time-display polish (server-only; Bruno collection unchanged, OTST_V2.0.12). (1) Scenario row title shows the code verbatim via scenarioTitle(sc) instead of decodeCode(sc.code) — decodeCode partial-decoded codes with recognised tokens (e.g. SALE_SEARCH_BAS_PAR_2ADT_2LEG -> 'Sale - 2 Adults - 2 Legs', dropping O&D) and looked like a rename; decoded/default name now used only when there is no code. (2) Passenger category preserved: wizGenPassengers (bulk create) omitted the category field the scenario view reads first, so a 3-adult/2-child request rendered as 5 adults (DOBs already child-aged; only the label was wrong) — it now stamps category like the single Add Passenger path; corrected help texts that wrongly claimed names are prefixed with the type. (3) Server Activity: login-events list was capped at the latest 50 with no date filter — added a local-day date picker + Latest button (GET /v1/admin/activity?from=&to= filters by the UTC range derived from the picked local day, matching the local times shown), a scrollable list, empty-state, and local-today clamp. (4) App-wide local-timezone reference: nav.js gains localTzRef() + a clock chip in the nav bar (e.g. Europe/Paris (UTC+02:00), DST-aware) so every page's time columns have an explicit zone; the Activity caption also names the zone. Server 1.11.55 -> 1.11.56.",
        "Operator action required: none. Hard-refresh after Watchtower promotes :stable. Chip shows 2026.84 / server-v1.11.56 / OTST_V2.0.12."
      ]
    },
    {
      "release": "2026.83",
      "date": "2026-05-24",
      "server": "1.11.55",
      "collection": "OTST_V2.0.12",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#188): booking failed with 400 'Invalid request content' because placeSelections.places[] did not match the OSDM SelectedPlace schema. SelectedPlace is additionalProperties:false and requires exactly { coachNumber, placeNumber, passengerRef } — all STRINGS, passengerRef SINGULAR. library-bruno/requestsBuilder.js placesForPassengers emitted passengerRefs (plural array) and potentially numeric coachNumber/placeNumber, so the vendor rejected the booking with 400. Now emits the conformant shape (one entry per passenger; coachNumber/placeNumber coerced to strings; passengerRef singular). The legacy single-place branch in accommodationAndPlaceSelection routes through the same helper. Additionally collectAvailablePlaces now reads the coach number from the OSDM Coach.number field (it was wrongly reading coachNumber, always undefined, so the picked place had no coach number and the required SelectedPlace.coachNumber was missing -> 400); coachNumber is kept only as a fallback for non-spec vendors. Fixes both 02. POST Create Booking and 09. POST Add Reservation (shared helper). Latent until #186 made the offer-time seat map work and places actually populate. Bruno_Collection/VERSION OTST_V2.0.11 -> OTST_V2.0.12.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.83 / server-v1.11.55 / OTST_V2.0.12 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.82",
      "date": "2026-05-24",
      "server": "1.11.54",
      "collection": "OTST_V2.0.11",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#186): plain seat scenarios sent an unresolved {{reservationId}} to the place map -> vendor 400. The place map (08/08b) and add-reservation (09) are keyed on a RESERVATION offer-part (resourceType=RESERVATION), but library-bruno/offers.js handleAccommodationAndPlaceSelection set reservationId/tripLegCoverage only on the COUCHETTE/BERTH branch. A plain seat scenario (SEATMAP_AT_OFFER / ADD_TO_BOOKING, accommodationSelection=NONE) never set them, so 08's URL contained the literal {{reservationId}} (%7B%7BreservationId%7D%7D) and the vendor rejected it with 400 (observed on Bileto). Now, for the seat path, when place selection is enabled and reservationId isn't already set, reservationId/reservationIds/tripLegCoverage are derived from the offer's first reservationOfferPart; offers with no reservation part log 'seat map not applicable'. opencollection.yml smart-run filter additionally skips any place-map request when reservationId is empty, so a malformed URL is never sent. Important: the earlier #182 finding ('Bileto serves no offer-time seat map') may have been based on this same malformed request - with reservationId now resolving, 08 sends a valid request and Bileto's real offer-context response can be observed (re-test recommended). Bruno_Collection/VERSION OTST_V2.0.10 -> OTST_V2.0.11.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.82 / server-v1.11.54 / OTST_V2.0.11 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.81",
      "date": "2026-05-24",
      "server": "1.11.53",
      "collection": "OTST_V2.0.10",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#184): availability-aware seat selection at BOTH selection times (pre-booking OFFER context and post-booking BOOKING context). Follow-up to #182. Previously 08. GET Place Maps (SEATMAP_AT_OFFER) took coaches[0]->compartments[0].place blindly (ignoring availability) and accommodationAndPlaceSelection jammed ALL passengers onto a SINGLE place; the post-booking add-reservation fetched no map and let the system auto-assign. Now: library-bruno/requestsBuilder.js collectAvailablePlaces(vehicle, count) (exported, unit-tested) flattens the whole-vehicle place-map response (coach.places, coach.compartments[].places, coach.decks[].places, or a compartment carrying .place), keeps only AVAILABLE places (boolean available/bookable or enum availability/state/status; no info => assumed available) and returns up to count {coachNumber, placeNumber, layoutId}; placesForPassengers(picked, refs) (exported, unit-tested) maps them one places[] entry per passenger (surplus pax reuse the last place). 08. GET Place Maps (OFFER context) derives passenger count, picks one available place per passenger into preselectedPlaces (+ back-compat singles). New 08b. GET Place Map Post-Booking issues GET /availabilities/place-map?contextType=BOOKING&contextId={bookingId}&resourceId={reservationId}&resourceType=RESERVATION; it runs in ADD_TO_BOOKING mode or as the #182 fallback, routed 02. POST Create Booking -> 08b -> 09. POST Add Reservation, reuses collectAvailablePlaces, and 09 now carries placeSelections.places (one per passenger) instead of auto-assigning. 08b is best-effort: a vendor with no BOOKING-context map is reported via a trackable assertion '[OSDM] Vendor serves a post-booking (BOOKING-context) seat map' (+ [VENDOR GAP] log) and the flow still completes (system assigns). accommodationAndPlaceSelection emits one places entry per passenger when preselectedPlaces is present, and picks enable place selection even without the legacy requiresPlaceSelection flag; single-place back-compat preserved. opencollection.yml smart-run filter gates the OFFER-context map (pre-booking) and BOOKING-context map (post-booking, needs a booking, once) independently. preselectedPlaces / __postBookingPlaceMapDone reset between scenarios/runs. Availability-only (no seat-together optimisation, scope confirmed). No sandbox tested so far serves a place map in either context (vendors hold seats against a BOOKING, see #182), so this is spec-built and unit-tested, not yet live-validated. Bruno_Collection/VERSION OTST_V2.0.9 -> OTST_V2.0.10.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.81 / server-v1.11.53 / OTST_V2.0.10 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.80",
      "date": "2026-05-24",
      "server": "1.11.52",
      "collection": "OTST_V2.0.9",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#182): adaptive place-selection fallback — pick the seat after pre-booking when the offer-time seat map is unavailable. OSDM exposes seat selection at two times: pre-booking (SEATMAP_AT_OFFER, GET /availabilities/place-map with contextType=OFFER, 08. GET Place Maps -> 02. POST Create Booking) and post-booking (ADD_TO_BOOKING, POST /bookings/{id}/booked-offers/{bookedOfferId}/{offer-parts|reservations}, 09. POST Add Reservation to Booking). Some providers (e.g. Bileto) hold seats against a BOOKING, so they serve no seat map for a bare OFFER and the offer-time map call returns non-200 (or 200 with no vehicleAvailability). 08. GET Place Maps now, when the offer-time map is unavailable and salesFlow_placeSelection==='true', sets __placeMapAtOfferFailed='true' and emits a clearly-named FAILING assertion '[OSDM] Vendor serves a pre-booking (OFFER-context) seat map' (+ [VENDOR GAP] log). 02. POST Create Booking routes to the post-booking add-reservation (09) when __placeMapAtOfferFailed==='true' (so the customer still picks a seat, post-booking) even when the scenario's nominal mode was SEATMAP_AT_OFFER. opencollection.yml smart-run filter _runAddReservation is also true when __placeMapAtOfferFailed==='true' (so 09 is not skipped) and resets __placeMapAtOfferFailed at collection start; scenarioParser.resetScenarioEnvVars also resets it. One-way, nominal ADD_TO_BOOKING and working SEATMAP_AT_OFFER scenarios unchanged. Bruno_Collection/VERSION OTST_V2.0.8 -> OTST_V2.0.9.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.80 / server-v1.11.52 / OTST_V2.0.9 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.79",
      "date": "2026-05-24",
      "server": "1.11.51",
      "collection": "OTST_V2.0.8",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#180): return booking adapts when a vendor rejects multi-offer bookings, with a trackable vendor-gap assertion. Follow-up to #178: the two-step return offer flow works, but booking both offers in one booking (OSDM-valid) is rejected by Bileto (400 'Too many offers — Only one offer can be booked at a time, for now'). 02. POST Create Booking now, for a return scenario, first attempts the combined booking; on a multi-offer rejection it emits a clearly-named FAILING assertion '[OSDM] Vendor supports booking multiple offers (round trip) in one booking' (+ [VENDOR GAP] log) and falls back to two separate bookings (sep-out outbound, then sep-in inbound; the inbound continues the normal post-booking flow). library-bruno/requestsBuilder.js buildBookingRequest is mode-aware via __returnBookMode (combined/sep-out/sep-in); one-way scenarios unchanged. New env vars outboundBookingId/__returnBookMode reset between scenarios. Bruno_Collection/VERSION OTST_V2.0.7 -> OTST_V2.0.8.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.79 / server-v1.11.51 / OTST_V2.0.8 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.78",
      "date": "2026-05-24",
      "server": "1.11.50",
      "collection": "OTST_V2.0.7",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#178): full two-step OSDM return trip (inward offer + round-trip booking). Follow-up to #176 (outbound return-aware search). New request 02-Common Requests/01b. POST Get Return Offer: after the outbound offer of a return scenario, 01. POST Get Offer captures the chosen outbound offer (outboundOfferId) and routes to 01b, which POSTs /offers with the trip reversed (O&D swapped, departureTime=inwardReturnDate) and returnSearchParameters.outwardOfferIds=[outboundOfferId], capturing inboundOfferId, then routes to booking. library-bruno/requestsBuilder.js: buildReturnOfferCollectionRequest() builds the inward request; buildBookingRequest() books BOTH offers (offers:[outbound,inbound]) when a return was fetched, else the single offer unchanged. opencollection.yml smart-run filter skips 01b for one-way scenarios and resets the new return env vars (outboundOfferId/inboundOfferId/__returnInboundDone/ReturnOfferCollectionRequest). Detected from the outbound returnSearchParameters.inwardReturnDate so one-way scenarios are unchanged; scoped to SEARCH outbounds. Bruno_Collection/VERSION OTST_V2.0.6 -> OTST_V2.0.7.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.78 / server-v1.11.50 / OTST_V2.0.7 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.77",
      "date": "2026-05-23",
      "server": "1.11.49",
      "collection": "OTST_V2.0.6",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#176): return trips are now valid OSDM and defined as a day-offset. The return date was written to offerSearchCriteria.inboundDate, but inboundDate is not an OSDM field and OfferSearchCriteria is additionalProperties:false, so the whole request was invalid and spec-strict vendors (e.g. Bileto) rejected it with 400 — it was our bug, not a Bileto limitation. Bruno_Collection/library-bruno/scenarioParser.js now expresses the return the OSDM way: returnSearchParameters.inwardReturnDate on tripSearchCriteria (SEARCH) / tripSpecification (SPECIFICATION). The return is derived as outbound departure date + N days (default suggestion 2, for night trains; 0 = same day), with the time mirroring the outbound departure time-of-day (optional HH:MM override) and the trailing timezone offset mirrored from the outbound. public/js/scenarios.js replaces the absolute 'Inbound Date' picker with a 'Return trip' day-offset field (empty = one-way) + optional 'Return time'; stored as returnOffsetDays/returnTime (authoring data only, routed to the trip, never echoed into the OSDM offerSearchCriteria). Bruno_Collection/VERSION OTST_V2.0.5 -> OTST_V2.0.6.",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.77 / server-v1.11.49 / OTST_V2.0.6 after Watchtower restarts. Re-set the return on any scenario that used the old (broken) inbound date."
      ]
    },
    {
      "release": "2026.76",
      "date": "2026-05-23",
      "server": "1.11.48",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#174): collapsible detailed run report (public/run-detail.html). Execution Log is now a collapsible card (collapsed by default; header toggles it, controls hide while collapsed). Assertions collapse on two levels — main area (suite) -> endpoint (request) -> individual assertions — all collapsed by default, with counts at each level; filters unchanged. New 'HTTP Traffic - Request & Response' card uses the same suite->endpoint structure and lazily loads each request's request body + response body (pretty-printed) from /v1/runs/:id/requests/:reqId on expand, with All/Non-2xx/Failed filters. UI-only change. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Hard-refresh the run-detail page after Watchtower promotes :stable."
      ]
    },
    {
      "release": "2026.75",
      "date": "2026-05-23",
      "server": "1.11.47",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#171): restore per-company concurrent runs. The per-company concurrent-run limit was always 1, so batch runs serialized regardless of the configured value (Bileto set to 3, global cap 9, still ran one-by-one). The test_frameworks.config column is encrypted at rest (Phase 2 of #60) but src/api/routes/runs.js read it with a plain JSON.parse(tfRow.config) (no colDecrypt) at both the run-submit and queue-status paths, so parsing the ciphertext threw and concurrentSessionLimit fell back to 1. Both reads now colDecrypt() first (legacy plaintext still passes through); the queue's per-company throttle was already correct.",
        "feat(#172): new scenarios default to a minimal offer search. public/js/scenarios.js wizInitScenario no longer pre-seeds offer-search criteria (requestedOfferParts, service/travel classes, flexibilities, offerMode, currency) from the framework defaults — they start empty, so a search-based scenario sends only the trip (origin+destination+departure date) and an empty offerSearchCriteria; the vendor returns its full default offer. Criteria remain optional and tickable in the wizard; fulfillment defaults unchanged. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Concurrency fix applies to runs submitted after Watchtower promotes :stable; hard-refresh the Test Config page for the scenario-default change."
      ]
    },
    {
      "release": "2026.74",
      "date": "2026-05-23",
      "server": "1.11.46",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#169): Timetable Discovery splits a route into separate train sets by operating-days pattern. src/services/timetable-discovery.js groupAndMerge() now tracks operating days per service across the scan, then splits each route into one set per day-pattern — e.g. Sqills BAS<->AMS yields '... (Mon-Fri)' with the 1xx weekday trains and '... (weekend)' with the 8xx weekend trains, each with its own accurate calendar (consistent with the one-calendar-per-set model from #141) instead of a single Mon-Sun set. The reconcile key now includes the calendar (origin+destination+productCategoryRef+sortedDays) so a re-scan merges into the matching set; set labels carry the day-pattern; summary gains setsDiscovered. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page > Test Data > Train Resources > 'Discover timetable'."
      ]
    },
    {
      "release": "2026.73",
      "date": "2026-05-23",
      "server": "1.11.45",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#167): Timetable Discovery — three fixes from live Sqills testing. (1) public/js/scenarios.js: after discovery the Test Scenarios section stayed locked ('configure Test Data first') because discovery only re-rendered Test Data; it now calls refreshAllSections() (reloads resources + re-renders all sections), removed unused refreshResourcesOnly. (2) src/api/routes/company-test-resources.js: discovery now uses POST /offers only (DISCOVERY_ENDPOINTS=['offers']) — the offer response carries both the timetable (trips[]) and the offered classes/ancillaries (offers[]), strictly more than /trips-collection, and works on every sandbox; Sqills implements /trips-collection so discovery had used it and got no prefill. (3) src/services/timetable-discovery.js harvestOfferCatalog: serviceClass is an object {name,type} in some sandboxes (Sqills), not a string, so it was never prefilled; now reads both forms (prefers .type then .name) and also harvests offerSummary.overallTravelClass/overallServiceClass. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page > Test Data > Train Resources > 'Discover timetable'."
      ]
    },
    {
      "release": "2026.72",
      "date": "2026-05-23",
      "server": "1.11.44",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#165): Timetable Discovery keeps the searched O&D codes at route endpoints. Some sandboxes (e.g. Bileto) echo their internal stop refs (urn:x_bileto:stn:<uuid>) in the offer response, so a discovered set's Origin showed that UUID rather than the UIC code the tester typed. src/services/timetable-discovery.js harvestTrips() now accepts { searchedOrigin, searchedDestination } and substitutes them at the route endpoints — the first timed leg's origin and last timed leg's destination of each trip (every returned trip spans the searched O&D); intermediate connection stations the sandbox resolves are left untouched and leg-to-leg continuity is preserved. src/api/routes/company-test-resources.js passes the normalized searched O&D in. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page > Test Data > Train Resources > 'Discover timetable'."
      ]
    },
    {
      "release": "2026.71",
      "date": "2026-05-23",
      "server": "1.11.43",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#163): Timetable Discovery accepts vendor station URNs and prefills Service Configuration from the offer response. (1) public/js/scenarios.js wizValidateTrain now accepts any urn:<scheme>:stn:<id> (UIC codes and vendor refs like Bileto's urn:x_bileto:stn:<uuid>), which previously failed validation and blocked saving discovered sets. (2) src/services/timetable-discovery.js harvestOfferCatalog() collects travel classes, service classes and ancillary types (ancillaryOfferParts[].type, fallback .category) from an OfferCollectionResponse via a depth-guarded deep scan; groupAndMerge() seeds these onto new sets and fills only empty arrays on existing sets (manual edits, incl. a removed class, are never overwritten/re-added). A /trips-collection response has no offers[] so prefill is a no-op there. src/api/routes/company-test-resources.js accumulates the catalog across days and passes it to the merge. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page > Test Data > Train Resources > 'Discover timetable'."
      ]
    },
    {
      "release": "2026.70",
      "date": "2026-05-23",
      "server": "1.11.42",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#161): Timetable Discovery uses an OffsetDateTime for Bileto's trip search. Discovery against Bileto returned HTTP 400 'Failed to read request' on both /trips-collection and /offers while normal scenario runs worked, because Bileto's deserializer requires the trip-search departureTime to be an OffsetDateTime but discovery sent a bare LocalDateTime (YYYY-MM-DDThh:mm:ss). The Bruno scenarioParser already carves this out (api_base.includes('bileto') -> OffsetDateTime); src/api/routes/company-test-resources.js now does the same: for Bileto it sends ...T00:00:00+00:00, all other vendors keep the LocalDateTime the OSDM TripSearchCriteria pattern specifies. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page > Test Data > Train Resources > 'Discover timetable'."
      ]
    },
    {
      "release": "2026.69",
      "date": "2026-05-23",
      "server": "1.11.41",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#159): Timetable Discovery falls back to POST /offers when a sandbox doesn't implement the optional OJP /trips-collection search. Live-testing #157 against Chaps returned HTTP 400 'Failed to read request' on every /trips-collection call — Chaps (like the other sandboxes) doesn't implement that endpoint; OSCAR's Bruno run flow only ever uses POST {api_base}/offers with the trip search embedded (tripSearchCriteria), and the offer response carries trips[].legs[].timedLeg. src/api/routes/company-test-resources.js now tries /trips-collection first and falls back to POST /offers (an OfferCollectionRequest with the trip search + one anonymous passenger + empty offerSearchCriteria) when it 4xx's or returns no trips; both responses are read by the same harvestTrips(). The working endpoint is locked in for the remaining days (no repeated probing), and the per-day breakdown reports which endpoint served each day (via). The token is fetched server-side exactly as before (this was never an auth issue — that would be a 401). Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page > Test Data > Train Resources > 'Discover timetable'."
      ]
    },
    {
      "release": "2026.68",
      "date": "2026-05-23",
      "server": "1.11.40",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#157): Train Timetable Discovery — reverse-engineer the train sets a sandbox actually runs from POST /trips-collection and auto-fill Test Data. New src/services/timetable-discovery.js (pure, unit-tested): harvestTrips() reads every timed leg of every returned trip as a service on its own sub-route (start/end stop + product category + vehicle #s + departure/arrival + operating day); groupAndMerge() groups by route key (origin+destination+productCategoryRef) and reconciles against existing TRAIN resources — creates new sets, appends new services (dedup on vehicle#+departure+arrival), unions the operating-days calendar, fills operator/product names only when empty, preserves manual catalogs. New src/worker/access-token.js extracts the per-tester OAuth2/bearer token resolution + cache from runner.js so the new endpoint and the Bruno run worker share one path (runner behaviour unchanged). New endpoint POST /v1/company/test-resources/discover-timetable (Test-Manager only, tenant-scoped) loops the next 1-14 days (default 7), calls {api_base}/trips-collection, harvests+merges, persists, returns {summary,created,updated,dayResults}; per-day failures tolerated. public/js/scenarios.js adds a 'Discover timetable' button + modal in Test Data > Train Resources (hidden for testers). Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page > Test Data > Train Resources > 'Discover timetable'. Requires the company's OSDM API base + the tester's credentials to be configured."
      ]
    },
    {
      "release": "2026.67",
      "date": "2026-05-22",
      "server": "1.11.39",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#155): scenario Offer Search Criteria now offers the full OSDM master list so any value can be requested (incl. non-happy-flow scenarios). public/js/scenarios.js: a scenario's offer search criteria is a free request filter — the tester must be able to request any OSDM value (travel class, service class, requested offer parts, flexibilities, offer mode), including ones the train/system doesn't support. Travel class is test data (per train), not a framework setting, so framework/train must not restrict the options. Both the creation wizard (renderWizardStep3) and the scenario detail editor (buildOfferSection) now build each control from the full OSDM enum (WIZ_*/ENUMS.*), unioned with whatever is already selected; framework/train values remain defaults only, never a filter. Completes #153. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page."
      ]
    },
    {
      "release": "2026.66",
      "date": "2026-05-22",
      "server": "1.11.38",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#153): a selected travel/service class can now always be deselected in a scenario's Offer Search Criteria. public/js/scenarios.js buildOfferSection() rendered each multi-select (requested offer parts, service class, travel class, flexibilities) only from the framework's allowed set, so a value seeded from the train (e.g. travelClass FIRST while the framework offer-criteria lists only SECOND) had no pill to untick and kept reaching the request as offerSearchCriteria.travelClasses:[FIRST]. Each list now renders the union of allowed + currently-selected, so anything already set appears (checked) and is deselectable. Collection version unchanged (OTST_V2.0.5).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page."
      ]
    },
    {
      "release": "2026.65",
      "date": "2026-05-22",
      "server": "1.11.37",
      "collection": "OTST_V2.0.5",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#150): add-ancillary now sources bookable ancillaries from the booking's additional-offers, not the pre-booking offer. Post-booking add-ancillary failed with 400 'ancillary not valid for bookedOfferId' because the request reused the pre-booking offer's offerId/ancillaryOfferId (rejected by the booking). New request 02-Common Requests/11. Add Ancillary - Get Additional Offers does GET /bookings/{id}/booked-offers/{bookedOfferId}/additional-offers and captures the first additional offer's offerId + ancillaryOfferParts[].id (valid for this booking), then chains to 10. POST Add Ancillary; if nothing is addable it logs and skips (OSDM allows rejecting post-booking additions). 10. POST Add Ancillary now prefers the additional-offers ids (addAncillaryParentOfferId/addAncillaryOfferIds), falling back to admission-linked refs and the offer's own ancillary parts. 02. POST Create Booking and 09. POST Add Reservation route to the new GET step. Smart filter gates the new step under the existing 'add ancillary' rule; new env vars added to the per-scenario reset list. Bruno_Collection/VERSION OTST_V2.0.4 -> OTST_V2.0.5; server image functionally unchanged (re-release surfaces the new collection version on the chip).",
        "Operator action required: none. Bruno collection refreshes on the VPS at merge; chip shows 2026.65 / server-v1.11.37 / OTST_V2.0.5 after Watchtower restarts."
      ]
    },
    {
      "release": "2026.64",
      "date": "2026-05-22",
      "server": "1.11.36",
      "collection": "OTST_V2.0.4",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Collection version bump OTST_V2.0.3 -> OTST_V2.0.4 (Bruno_Collection/VERSION). Records two Bruno-collection fixes that previously refreshed onto prod without a collection version change (so the version chip looked frozen at 2.0.3): #147 — library-bruno/bookings.js now captures bookedOfferId from the BookedOffer's offerId (the OSDM identifier; there is no .id) so post-booking add-ancillary/add-reservation URLs are populated; #132 — 10. POST Add Ancillary to Booking sources ancillaryOfferIds from the offer's top-level ancillaryOfferParts when no admission-linked refs exist (Sqills). The server image is functionally unchanged — this re-release surfaces the new collection version on the chip (compatibility.json is read once at boot; the chip refreshes when Watchtower restarts on the new :stable digest). Process going forward: every Bruno-collection change bumps Bruno_Collection/VERSION and rides a server release.",
        "Operator action required: none. After Watchtower promotes :stable, the version chip shows 2026.64 / server-v1.11.36 / OTST_V2.0.4."
      ]
    },
    {
      "release": "2026.63",
      "date": "2026-05-22",
      "server": "1.11.35",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Offer-criteria polish (#145), public/js/scenarios.js. (1) The new-scenario wizard's Offer mode can be left empty (a '— none —' option). offerSearchCriteria and all its fields are optional per OSDM (verified v3.4 & v3.8: OfferSearchCriteria has no required properties; offerSearchCriteria is not required on OfferCollectionRequest/ExchangeOfferCollectionRequest); the wizard previously forced a mode while every other criterion was clearable. Selecting 'none' omits offerMode from the request. (2) Journey leg-continuity guard: the journey editor warns (amber banner, live) when a leg departs before the previous leg arrives, or starts at a different station than the previous leg ends — the trap that produced an empty Sqills offer (OSDM_202 arrived 16:35 but OSDM_109 departed 15:00). Soft warning (overnight connections are legitimate). Collection version unchanged (OTST_V2.0.3).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page -> Test Scenarios."
      ]
    },
    {
      "release": "2026.62",
      "date": "2026-05-22",
      "server": "1.11.34",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#143): the new-scenario wizard can now select a Journey (multi-leg). public/js/scenarios.js renderWizardStep3/wizGenerateScenario: the scenario-creation wizard only offered 'Select train resource' (a single train set), so building a fresh multi-leg scenario from a reusable Journey (#137) was a dead end. The Train/Trip Selection step now shows a 'Select a Journey' dropdown when journeys exist; picking one sets wizScenario.journeyResourceId, hides the single-train/trip-mode controls (a journey is inherently a multi-leg SPECIFICATION), shows a route summary, and wizGenerateScenario builds the trip as SPECIFICATION with legs = journeyToTripLegs(journey) — origin/destination/times/vehicle/operator and product category resolved per leg from each train set. Single-train selection unchanged when no journey is chosen. Collection version unchanged (OTST_V2.0.3).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page -> Test Scenarios -> New."
      ]
    },
    {
      "release": "2026.61",
      "date": "2026-05-22",
      "server": "1.11.33",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#141): bundle of train-set/journey usability fixes from testing the timetable (#136) and journeys (#137), all in public/js/scenarios.js. (1) Journey leg picker now labels each option by its service (route · vehicle · departure→arrival · set name) instead of the train-set name. (2) Product category was missing from the offer request (Sqills rejected it): the train set now captures productCategoryRef/Name/ShortName (was a single field; old value migrates to the ref) and all three are copied into the trip leg by every builder (per-service Apply-test-data picker, Apply-a-Journey, new-scenario wizard, datafile import); Bruno already maps these to service.productCategory. (3) Operating-days calendar moved from per-service to the train-set level (one picker for the whole timetable; old per-service days migrate up). (4) Saving a train/journey no longer collapses the panel, wipes the list, or needs a second click to re-expand — wizSaveTrain/wizSaveJourney re-render the Test Data section locally and re-open the saved panel instead of the heavy refreshAllSections(). New 'Save all trains' button persists every open/edited train at once (validates all first). Collection version unchanged (OTST_V2.0.3).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page -> Test Data. Re-open each Sqills train set to confirm the migrated product-category ref and fill name/short name from the vendor data."
      ]
    },
    {
      "release": "2026.60",
      "date": "2026-05-22",
      "server": "1.11.32",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#137): reusable multi-leg Journeys — Phase 3 (final) of the train-set/journey series (duplicate #135 -> timetable #136 -> journeys). New JOURNEY test-resource type (src/api/routes/company-test-resources.js allow-list adds 'JOURNEY'; schema.sql comment). A journey's data is { legs: [ { trainResourceId, serviceIndex } ] } — an ordered list of legs each referencing a train set + a chosen service from its timetable (#136). public/js/scenarios.js gains a Journeys section under Test Resources (replacing the Multimodal 'coming soon' placeholder): add/duplicate/delete journeys; each journey edits its ordered legs (pick train set + service per leg, reorder, remove) with a live route summary. Scenario trip gains an 'Apply a Journey' picker that fills all trip legs from a saved journey (sets SPECIFICATION). Journeys are copied into a scenario's legs at apply-time (not referenced), so deleting one can't orphan a scenario; no datafile-schema change since the generated tripRequirement.legs[] is what the runtime already consumes. Collection version unchanged (OTST_V2.0.3).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page -> Test Data."
      ]
    },
    {
      "release": "2026.59",
      "date": "2026-05-22",
      "server": "1.11.31",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#136): a train set is now a route + a timetable of services — Phase 2 of the train-set/journey series (duplicate #135 -> timetable -> journeys #137). public/js/scenarios.js: a train set's data gains a services[] array, each { vehicleNumber, departureTime, arrivalTime, daysOfWeek? }, so one route (e.g. Sqills IC Basel->Amsterdam) holds the several trains that run it at different hours (OSDM_200/202/204/206). Train Details holds the shared route (label, operator, origin/destination, optional product category); a new Services (timetable) section lists departures with add/remove rows, per-service day-of-week toggles, and a paste box that parses vendor tokens like OSDM_202|OSDM_IC|2026-06-01T09:10:00+02:00|...|8500010|8400058 into rows (and fills empty route fields). The scenario trip 'Apply test data' picker now lists one entry per service. normalizeTrainData() migrates legacy single-service train sets (top-level vehicleNumber/departureTime/arrivalTime) into services[0] on read, so existing trains load and run unchanged; the wizard scenario generator uses the first service and the delete-impact check matches any service's vehicle. No server route or datafile-schema change (train data is an opaque encrypted blob). Collection version unchanged (OTST_V2.0.3).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page -> Test Data."
      ]
    },
    {
      "release": "2026.58",
      "date": "2026-05-22",
      "server": "1.11.30",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#135): duplicate a train set — Phase 1 of the train-set/journey test-data series (duplicate -> timetable #136 -> journeys #137). public/js/scenarios.js: each train row in Test Resources gains a Duplicate button; wizDuplicateTrain() deep-clones the source train's data + label into a fresh unsaved placeholder with a unique '(copy)' label and expands it for editing (mirrors wizAddTrain), so the common 'same route, different hour' case no longer requires re-entering every field. The copy persists as a brand-new test_resources row on Save Train; the original is untouched. Hidden in tester read-only mode alongside add/delete. Collection version unchanged (OTST_V2.0.3).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page -> Test Data."
      ]
    },
    {
      "release": "2026.57",
      "date": "2026-05-22",
      "server": "1.11.29",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#133): scenario title no longer collapses to a bare 'Sale' for custom codes. public/js/scenarios.js decodeCode() rendered the bold scenario title; a code outside the strict OSDM test-suite convention (e.g. SALE_SEARCH_IC_BAS_AMS_1PAX) showed just 'Sale' because every descriptive token after the recognised type prefix (SEARCH/IC/BAS/AMS/1PAX) was silently dropped. decodeCode() now counts unrecognised tokens and, when nothing beyond the bare type marker was recognised, returns the code verbatim so the title matches what the user typed; genuine convention codes still decode to their rich label (OTST_RFND_SRCH_CRIT_1ADT_1LEG -> 'Refund — Search criteria — 1 Adult — 1 Leg'). Also accepts the full COUCHETTE token, not only the abbreviated CCHTTE, so couchette scenarios decode correctly. Collection version unchanged (OTST_V2.0.3).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page."
      ]
    },
    {
      "release": "2026.56",
      "date": "2026-05-22",
      "server": "1.11.28",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#130): configurable ancillary catalog at the Test Framework level. public/js/scenarios.js gains an 'Ancillaries' section in the Test Framework — the OSDM standard AncillaryType examples as toggle pills plus an 'add custom' input for vendor-specific codes (e.g. BIKE); AncillaryType is an x-extensible-enum so custom values are spec-valid. Stored in framework.ancillaries. A train resource's 'Ancillaries available' picker now draws from the framework catalog (framework.ancillaries), unioned with the train's existing selections so nothing is lost — mirroring how ticket types derive from framework.rail.ticketTypes. New emptyFramework() seeds ancillaries with the OSDM standard set; the hard-coded WIZ_ANCILLARIES constant is removed in favour of OSDM_ANCILLARY_TYPES + the editable catalog.",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page → Test Framework → Ancillaries, then per train under Test Data."
      ]
    },
    {
      "release": "2026.55",
      "date": "2026-05-22",
      "server": "1.11.27",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(#128): changing a user's role to Test Manager no longer wipes their company. src/api/routes/admin.js PATCH /v1/admin/users/:id treated test_manager as not company-bound, so it fell into the catch-all else that reassigns to the platform company — changing a Tester to Test Manager silently moved them onto the OSCAR platform company. company_user and test_manager now keep the user's current company when no company_id is supplied, or move to a provided company_id; only administrator maps to the platform company; a company-bound role cannot be left on the platform company (clear 400). public/admin.html: the Users-tab company cell is now an editable select for Test Manager too (not a read-only Platform label), pre-selected to the current company and preserved across role changes, so an admin can keep or change the company. Test-manager-managed user lists are unaffected (scoped to the manager's own company). When a user IS moved to another company, access to the previous company's data is already blocked by per-request tenant scoping; deleting a company's test config when it loses its last user is a separate lifecycle policy, out of scope. Covered by tests/integration/admin-routes.test.js.",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the admin console Users tab."
      ]
    },
    {
      "release": "2026.54",
      "date": "2026-05-22",
      "server": "1.11.26",
      "collection": "OTST_V2.0.3",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Release of the optional sale-flow features initiative (collection OTST_V2.0.3). Bundles the Bruno-collection work that landed after v1.11.25 into one tested, labelled version; behaviour is inert / zero-regression for every current scenario (the new steps activate only when a scenario opts in, authorised by the Test Framework from v1.11.25 / #107). OPT-PLACE Stage A (#104): GET Place Maps relocated from System Information into the sale flow (02-Common Requests/08. GET Place Maps), run as the pre-booking seat map (SEATMAP_AT_OFFER), with PlaceAvailabilityResponse Layer-1 compliance + a no-seat-map mismatch diagnostic. OPT-PLACE Stage B (#124 / issue #123): 09. POST Add Reservation to Booking — post-booking add-reservation (ADD_TO_BOOKING), version-aware endpoint (/offer-parts >=3.7 else the deprecated /reservations), BookedOfferPartResponse compliance; bookings.js now captures bookedOfferId. OPT-ANCILLARY (#125/#126 / issue #108): 10. POST Add Ancillary to Booking (version-aware /offer-parts ancillaryOfferIds >=3.7 else /ancillaries) plus offer-time AncillaryOfferPart compliance wired into 01. POST Get Offer (validates id/type/category on offers that carry ancillaries, e.g. Sqills; no-op otherwise). Optional post-booking steps chain in order Create Booking -> [Add Reservation] -> [Add Ancillary] -> PATCH/GET, each gated and guarded against re-runs. Also bundles CI housekeeping already merged: Dependabot ignores breaking-major bumps (uuid/express/eslint/dotenv/node, #111/#120), SonarCloud skips-with-success on Dependabot PRs (#121), production-deps minor/patch group bump (#122).",
        "Operator action required: none. Server change picked up after Watchtower promotes :stable (hard-refresh the Test Config page). Bruno collection refreshes via the refresh-collection workflow on merge. All new sale-flow steps stay inert until a scenario enables them via the Test Framework."
      ]
    },
    {
      "release": "2026.53",
      "date": "2026-05-21",
      "server": "1.11.25",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(#107): Tier-1 test-system config for optional sale-flow features. The Test Framework gains a 'Seat Selection' section (public/js/scenarios.js, emptyFramework()): a 'seat map' capability toggle + a supported-modes menu (SEATMAP_AT_OFFER = seat map before booking; ADD_TO_BOOKING = reservation added to an existing booking, e.g. SNCF first-class TGV), persisted as framework.placeSelection {seatMap, supportedModes}. Scenario 'Booking Flow Actions' pills are now authorised by the framework (Gate 0 → Gate 1): Place selection requires a reservation ticket type + seat map; Add/Delete ancillary require at least one declared ancillary; unsupported actions render disabled with the reason. A per-scenario placeSelectionMode picker (limited to the framework's supported modes) is written into the generated data file and validated by json_validator/datafile.schema.json (which also gains salesFlowActions). Bruno-side: scenarioParser.resolveSalesFlowActions() (unit-tested) centralises the booking-flow-action defaults — the optional features (placeSelection/addAncillary/deleteAncillary) now default OFF so existing scenarios stop claiming unimplemented steps; patchPassengers (the only flag consumed today) and getBooking keep their historic ON default, so runtime behaviour is unchanged. scenarioParser reads placeSelectionMode (added to both reset lists). Foundation for #104 (place maps in the sale flow) and #108 (ancillaries); no request consumes the new flags yet. Collection version unchanged (OTST_V2.0.2).",
        "Operator action required: none. Server change picked up after Watchtower promotes :stable (hard-refresh the Test Config page to see the new Seat Selection section). Bruno collection refreshes via the refresh-collection workflow on merge."
      ]
    },
    {
      "release": "2026.52",
      "date": "2026-05-20",
      "server": "1.11.24",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "chore(audit #86): dead-code cleanup of CodeQL/ESLint js/unused-local-variable findings, behaviour-neutral. src/api/routes: removed unused destructured imports (isPlatformRole/isTestManagerOrAbove in company-test-framework.js & company-test-resources.js; isTestManagerOrAbove in company.js) and the unused fileHash() helper in company.js (crypto/fs still used). library-bruno: unused top-level 'const x = require()' bindings (display in scenarioParser/offers/refunds/validators/fulfillments/exchanges; requestsBuilder in offers; validators/models in scenarioParser — the latter shadowed by a local re-require) converted to bare require('./…') so each module's globalThis side-effect is preserved with no binding; removed a dead expectedStatuses local in bookings.js. public: removed dead batchStatus (dashboard.html) and email (scenarios.js) locals. The ~140 Sonar 'auto-fixable' style suggestions (optional chaining etc.) are SonarLint IDE quick-fixes, not eslint-fixable (eslint src/ --fix is a no-op here), left as non-blocking advisories; a few ambiguous test-file imports left as-is.",
        "Operator action required: none. Bruno collection refreshes via the refresh-collection workflow on merge."
      ]
    },
    {
      "release": "2026.51",
      "date": "2026-05-20",
      "server": "1.11.23",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "security(audit #87): removed the hardcoded Benerail credential from source. In Bruno_Collection/00-Access Token/Benerail Access Token.yml the jwt-bearer 'assertion' (a signed, expiring JWT) and 'scope' (uic_osdm, effectively an account id) were committed literals; they now reference secret env vars {{benerail_assertion}} / {{benerail_scope}}, matching how all other vendors source secrets. environments/OTST_Benerail_Env.yml declares benerail_assertion and benerail_scope as secret:true (name only, no value) so the values stay in Bruno's local secret store and are never committed — same as Ocp-Apim-Subscription-Key / requestor / access_token. Both committed files are now credential-free. The previously-committed JWT remains in git history and should be allowed to lapse / rotated on the Benerail side.",
        "Operator action required: local Bruno testers of Benerail must set the secret env vars benerail_assertion (the jwt-bearer assertion) and benerail_scope (uic_osdm) in the OTST_Benerail_Env environment; Bruno keeps secret values local. No change for OSCAR-server runs (auth handled server-side)."
      ]
    },
    {
      "release": "2026.50",
      "date": "2026-05-20",
      "server": "1.11.22",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(audit #88): removed the CodeQL HIGH js/file-system-race in library-bruno/reportGenerator.js. The per-run report accumulator did fs.existsSync(tmpFile) then later fs.readFileSync / fs.writeFileSync on the same path — a time-of-check-to-time-of-use race. The load path now just attempts the read and treats a missing file (ENOENT) or corrupt JSON as 'start fresh', with no pre-check; behaviour is identical and the race is eliminated. The ~21 js/unused-local-variable note-severity findings also bundled in #88 are deferred to #86 (eslint --fix), since several are destructured imports where only one identifier is unused.",
        "Operator action required: none. Bruno collection refreshes via the refresh-collection workflow on merge."
      ]
    },
    {
      "release": "2026.49",
      "date": "2026-05-20",
      "server": "1.11.21",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(bruno #84): the Bruno scenario engine no longer swallows exceptions silently. The 16 S2486 'ignored exception' sites in library-bruno now LOG the caught error (visible in the run log) with identical control flow / fallback: 9 trailing Object.assign(globalThis,...) module-exposure catches (envUtils, bookings, displays, exchanges, fulfillments, model, offers, passengers, refunds); envUtils.parseEnvJson getEnvVar throw; reportGenerator tmp-read / bru-context; displays.addReportLog; offers preflight header+body resolution; and mergeReport.prettyJson. prettyJson additionally checks whether a value looks like JSON before parsing, so a plain string is returned as-is instead of routing through an expected throw. Caller behaviour unchanged — only error visibility improves. Existing bruno-envutils / bruno-requestsbuilder Jest suites still pass. Completes the second half of #84 (JSON.parse hardening shipped v1.11.18 / #91). Also makes data-load failures clearer: getScenarioData (network / HTTP / non-absolute data_base) and the parseEnvJson 'required scenario variable not set' message now name the data_base URL and suggest checking that the data-file server is running (e.g. 'python -m http.server 8000' in Bruno_Collection/data_base) — reported from running the collection locally in the Bruno UI.",
        "Operator action required: none. Bruno collection refreshes via the refresh-collection workflow on merge."
      ]
    },
    {
      "release": "2026.48",
      "date": "2026-05-20",
      "server": "1.11.20",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(ops): 'Emergency Stop' kill switch for active runs. New POST /v1/runs/stop-all (src/api/routes/runs.js): QUEUED runs are purged from the in-memory queue (queue.purge) then marked CANCELLED; RUNNING runs have their Bruno child killed via runner.killRun (SIGTERM then SIGKILL after a 3s grace) then marked CANCELLED. The runner's final status write is now guarded with AND status='RUNNING' so a killed run is never resurrected from CANCELLED to FAILED. Scope is deliberately restrictive — only the platform admin can stop other users' runs: company_user AND test_manager stop only the runs they personally launched (red 'Emergency Stop' button on the dashboard Company Queue panel); administrator stops ALL active runs across EVERY company (platform-wide, from a new control in the admin console Server Config tab); certification_user is forbidden (403). Confirmation dialog + audit log. Covered by tests/integration/runs-stop-all.test.js.",
        "Operator action required: none. After Watchtower promotes :stable (hard-refresh to pick up the UI): a per-user Emergency Stop button appears on the dashboard Company Queue panel, and an admin-only platform-wide 'Stop ALL running scenarios' control appears in the admin console Server Config tab."
      ]
    },
    {
      "release": "2026.47",
      "date": "2026-05-20",
      "server": "1.11.19",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(ops): startup reconciliation of orphaned runs. New src/worker/reconcile.js + a boot hook in src/server.js mark any run still RUNNING or QUEUED in the DB as FAILED on startup. The run queue (worker/queue.js) is in-memory, so on a process exit (Watchtower deploy promoting :stable, crash, RUN_TIMEOUT_MS SIGTERM, docker restart) in-flight/pending jobs are lost but their DB rows survive — RUNNING never resumes (Bruno child gone) and QUEUED is never dispatched (in-memory queue empty on boot). Both keep occupying the company concurrency slots, wedging the Company Queue so no new run can start. Observed live: a release auto-deployed mid-batch left 4 orphaned RUNNING rows pinning all 4 slots and 2 QUEUED stuck. Reconciliation frees the slots. RUNNING→FAILED (unresumable); QUEUED→FAILED (resubmit) — auto-re-dispatch deliberately not chosen, to avoid firing vendor API calls unattended after every deploy. Idempotent; runs after migrations and before listen. Covered by tests/unit/reconcile.test.js (injected-run unit + real-schema test).",
        "Operator action required: none. After Watchtower promotes :stable, runs stuck RUNNING/QUEUED from a prior restart show as FAILED ('interrupted by a server restart') and can be deleted normally; resubmit if still needed."
      ]
    },
    {
      "release": "2026.46",
      "date": "2026-05-20",
      "server": "1.11.18",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(bruno #84): hardened the scenario engine's env-var JSON parsing. New library-bruno/envUtils.js exports parseEnvJson(name[,fallback]); the 17 JSON.parse(bru.getEnvVar(...)) sites in requestsBuilder.js, loopback.js and offers.js now use it. A missing/empty variable (previously JSON.parse(undefined) → cryptic 'Unexpected token u in JSON' with no clue which var) now throws an actionable error naming the variable and the likely cause (data file didn't load); malformed JSON names the variable + a value snippet. Happy path unchanged; required vs optional (fallback) behaviour preserved. This is the exact cryptic-failure class hit when running a vendor locally. Adds the first library-bruno Jest test (envUtils, #85). The broader S2486 swallowed-exception cleanup is deferred to pair with more of the test harness (#85).",
        "Operator action required: none. Bruno collection refreshes via the refresh-collection workflow on merge."
      ]
    },
    {
      "release": "2026.45",
      "date": "2026-05-20",
      "server": "1.11.17",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(security/audit #82): hardened the esc() HTML-entity encoder in public/js/scenarios.js to also escape single quotes ('→&#39;), so interpolated values are safe in single-quoted attributes as well as double-quoted attributes and text. Defense-in-depth following the audit. The 3 Sonar S5696 DOM-XSS BLOCKERs flagged on this file are FALSE POSITIVES — every dynamic value already passes through esc(); Sonar's taint engine does not recognise the custom encoder. They should be marked Safe in SonarCloud (see issue #82).",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the Test Config page."
      ]
    },
    {
      "release": "2026.44",
      "date": "2026-05-20",
      "server": "1.11.16",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(ux): clicking 'Share with certifiers' / 'Unshare' on the dashboard now updates just that run's share state in place, instead of re-rendering the whole list. The full re-render (loadRuns) collapsed any expanded batches and jumped the scroll to the top — reported as 'the dashboard collapses when I click share'. Follow-up to v1.11.15: the per-report sharing feature shipped, but this UX polish + the matching news entry were committed minutes after #77 was already squash-merged, so they missed that release.",
        "docs: added the 'per-report certifier sharing' news entry that was meant to ship with v1.11.15.",
        "Operator action required: none. Picked up after Watchtower promotes :stable; hard-refresh the dashboard."
      ]
    },
    {
      "release": "2026.43",
      "date": "2026-05-20",
      "server": "1.11.15",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(sharing): certifier report sharing is now PER-REPORT, decided by the test_manager from the dashboard. Removed the company-wide share_reports_with_certifier master toggle (was in API Config / profile). A test_manager now sees a 'Share with certifiers' / 'Unshare' control on each terminal-status run row on the dashboard, plus a 'Shared with certifiers' badge; certifiers see ONLY the individual runs explicitly shared. Default is private (nothing shared). The per-run mechanism (POST/DELETE /v1/runs/:id/share, shared_with_certifier_at) already existed since v1.10.0 but was only reachable from the single-run detail page and was gated by the now-removed company toggle.",
        "refactor(security): the certifier-visibility gate is now solely shared_with_certifier_at IS NOT NULL, applied consistently across run-access.js (single-run), runs.js (list), reports.js (comparisons — BOTH underlying runs must be shared), and the tenant middleware (no longer refuses certifiers at the company boundary). company.js no longer reads/writes the toggle and rejects it with a 400 + pointer if an old client sends it. The companies.share_reports_with_certifier DB column is retained (no destructive migration) but unused.",
        "Operator action required: none. IMPORTANT behaviour change: any reports that were visible to certifiers purely because the old company toggle was ON are now private until a test_manager explicitly shares them. Communicate this to test_managers. Picked up after Watchtower promotes :stable; hard-refresh the dashboard."
      ]
    },
    {
      "release": "2026.42",
      "date": "2026-05-20",
      "server": "1.11.14",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(auth): login rate-limiter tuning. (1) /logout removed from the limiter — it carries no credential to brute-force and counting it in the same IP bucket as /login halved the effective login budget during rapid user-switching (each switch is logout+login). (2) Default cap raised 20 → 50 per 15-minute window and made env-tunable via AUTH_RATE_LIMIT_MAX. A conformance-testing platform invites rapid switching across many vendor accounts; 20/15min tripped legitimate testers ('Too many attempts. Please try again later.') after ~10 login/logout cycles. 50/15min is still far below a useful brute-force rate. /login, /register, /bootstrap remain protected.",
        "Operator action required: none. Optional: set AUTH_RATE_LIMIT_MAX in OSCAR_Deploy/.env to override the default of 50. Picked up after Watchtower promotes :stable."
      ]
    },
    {
      "release": "2026.41",
      "date": "2026-05-20",
      "server": "1.11.13",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(security/visibility): a plain tester (company_user) now sees ONLY their own runs in GET /v1/runs, not every run in their company. test_manager and administrator keep company-wide visibility (they triage and can delete any run). Previously the tester list was company-scoped while delete was own-only, which both leaked who-ran-what across the team and produced a confusing 'Not the run owner' toast when a tester tried to delete a teammate's run. Change is in Oscar_Server/src/api/routes/runs.js (GET / handler).",
        "fix(tz): full timezone-handling sanity pass after #67 (Europe/Paris container TZ). (1) Server: isRunStale() in runs.js parsed SQLite's TZ-less datetime('now') string with new Date(), which under TZ=Europe/Paris was read as Paris-local — making a just-started run look 1-2h old and get auto-cancelled on delete. Now parsed as UTC. (2) Frontend: nav.js gains parseServerTs() which normalises TZ-less UTC strings to ISO+Z before display; every timestamp render site (dashboard, run-detail, compare, report-builder, run, admin, scenarios.js) now uses it so times localise correctly to each viewer. Storage stays UTC throughout; only display localises. Bruno date helpers (toOffsetDateTime/toLocalDateTime) audited — correct, unchanged.",
        "fix(observability): Grafana OSCAR Logs dashboard 'Errors in range' + 'Errors only' panels now exclude the metrics-stack containers (oscar-grafana|oscar-prometheus|oscar-loki|oscar-promtail|oscar-alertmanager). They were matching Grafana's own tsdb.loki query logs — which contain the literal error|fatal|panic LogQL pattern — producing self-referential false positives.",
        "chore(bruno-env): stripped accidentally-committed Bruno session-state vars from OTST_Paxone_Env.yml and OTST_Turnit_Env.yml (followup to 2026.39 which fixed only their data-file paths). Both are now config-only templates matching Benerail/Sqills; Paxone's stray pinned scenarioTarget was reset to empty.",
        "Operator action required: none. Server change picked up after Watchtower promotes :stable (hard-refresh the dashboard). Bruno collection + Grafana dashboards refresh automatically on merge."
      ]
    },
    {
      "release": "2026.40",
      "date": "2026-05-20",
      "server": "1.11.12",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(critical): dashboard stuck on 'Loading…' for all roles in v1.11.11. fmtDate() in public/dashboard.html called parseServerTs(), a TZ-normalisation helper that lives on the unmerged fix/v1.11.7-frontend-timestamp-tz branch and is NOT defined in the shipped nav.js. The call leaked into #73 from an uncommitted working-tree edit. Result: ReferenceError: parseServerTs is not defined thrown inside renderRuns() on every row, before el.innerHTML was set — so the runs list never replaced its 'Loading…' placeholder, and loadRuns() has no try/catch. Reverted fmtDate() to plain new Date(d).toLocaleString(...). The TZ-normalisation will return properly when the v1.11.7 branch (which defines parseServerTs in nav.js) merges as a unit.",
        "Operator action required: none. Watchtower picks up :stable automatically after the image rebuild; hard-refresh the dashboard once live."
      ]
    },
    {
      "release": "2026.39",
      "date": "2026-05-19",
      "server": "1.11.11",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(ux): dashboard now shows the submitter email on every row and batch-header for every role (not only test_manager / administrator). Previously the field was hidden from testers under the assumption that 'testers see only their own runs', but the tester list endpoint is company-scoped — testers do see teammates' runs. Per-row checkbox is now disabled for runs the current user is not authorised to delete, with a tooltip naming the actual owner; selecting and then clicking Delete used to produce a 'Not the run owner' toast at API time. Same logic applied to the batch-select checkbox (single-owner per batch).",
        "fix(bruno-env): repaired stale local-dev data-file paths across all five vendor environment files (OTST_Benerail_Env.yml, OTST_Bileto_Env.yml, OTST_Paxone_Env.yml, OTST_Sqills_Env.yml, OTST_Turnit_Env.yml). Four of them still pointed at the pre-PR-#68 external-repo convention (http://localhost:8080/collections-bruno/OTST_V2.0.1/...); Turnit had a non-matching Bruno_Collection/ URL prefix. All five now follow the same http://localhost:8080/data_base/<vendor>_datafile.json and http://localhost:8080/json_validator/datafile.schema.json pattern. Chaps was already on the same convention and is unchanged. Local Bruno-UI workflow: cd Bruno_Collection && python -m http.server 8080. Production OSCAR runs are unaffected: the worker writes an ephemeral env file overriding data_base with its internal http://localhost:3001/data/... URL regardless of what the repo template says.",
        "chore(bruno-env): also stripped accidentally-committed Bruno session-state vars from OTST_Sqills_Env.yml (__reportInitDone, productListHasValidData, productListCandidateIds). These get auto-written by Bruno when a developer runs requests locally; they belong in the developer's working copy, not in main. Paxone and Turnit env files still carry similar cruft and are flagged for a follow-up cleanup PR.",
        "Operator action required: none. Bruno collection refreshes automatically via the refresh-collection workflow on merge; the dashboard change is in public/dashboard.html so it's picked up on the next page hard-reload after Watchtower promotes :stable."
      ]
    },
    {
      "release": "2026.38",
      "date": "2026-05-19",
      "server": "1.11.10",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(bruno): stop the infinite-loop regression that #68 introduced and that #70/#71 only partially masked. Symptom: a 1-scenario run of OTST_RFND_PATCH_SRCH_CRIT_1ADT_1LEG against Sqills executed the same scenario 27 times in ~3 minutes before being killed by RUN_TIMEOUT_MS / manual cancel, with no terminal stopExecution() ever firing. Two minimal changes: (1) Bruno_Collection/library-bruno/scenarioParser.js now sets __unitaryLoadedIdx in lock-step with scenariosToRunIndex whenever it advances the index, so the unitary-load wrapper in opencollection.yml sees no spurious change between /versions and request #2 of the same iteration and stops re-firing on every request. (2) Bruno_Collection/opencollection.yml removes the wrap-to-0 in the wrapper's post-load sequential-mode branch — that wrap defeated the terminal-step check `scenariosToRunIndex < __scenariosList.length` and turned a one-shot halt into an infinite loop-back. Side-effect: JSON report download (.bru_results_<runId>.json) is restored, since Bruno CLI now exits normally and gets to flush its --reporter-json output. Full root-cause trace in Documentation/Bruno_Collection/PR68-loop-regression-root-cause.md.",
        "Operator action required: none. Bruno collection refreshes automatically via the refresh-collection workflow on merge; testers see the fix on their next run."
      ]
    },
    {
      "release": "2026.37",
      "date": "2026-05-19",
      "server": "1.11.9",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(bruno): two report-side latent bugs surfaced this week. (1) reportGenerator.js initReport() now preserves the accumulated .report_tmp.json when a scenario loop-back retry of the SAME scenarioCode starts — previously the wipe deleted the System Information requests and the first /offers attempt, leaving the final HTML with only the retry attempt. (2) mergeReport.js now unwraps Bruno CLI's iteration-array reporter output ([{ iterationIndex, results, summary }]) — previously the outer array was mapped as if it were a single phantom request, producing 1-request / 0-assertion merged reports. Neither bug is caused by #68; both are pre-existing and only surfaced because (a) Bileto's /offers is currently returning 500 (triggers loop-back) and (b) #69's Docker rebuild pulled a fresher @usebruno/cli that emits the iteration wrapper.",
        "Operator action required: none. Bruno collection refreshes automatically via the refresh-collection workflow on merge."
      ]
    },
    {
      "release": "2026.36",
      "date": "2026-05-19",
      "server": "1.11.8",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(bruno): clamp scenariosToRunIndex before calling getScenarioData() in the unitary-load wrapper (Bruno_Collection/opencollection.yml). Without the clamp, the wrapper passed an out-of-range index into getScenarioData(), which in turn called bru.runner.stopExecution() and silently halted the run partway through. Symptom on Bileto/Sqills: only the two System Information requests fired (versions + coach-deck-layouts), everything after was truncated.",
        "Operator action required: none. Bruno collection refreshes via the refresh-collection workflow on merge; testers see the fix on their next run."
      ]
    },
    {
      "release": "2026.35",
      "date": "2026-05-19",
      "server": "1.11.7",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(security): Dockerfile runtime stage now runs apt-get update && apt-get upgrade -y before installing Bruno CLI. Clears three Debian-12 HIGH CVEs that Trivy started flagging after 2026-05-16: CVE-2026-0861 (glibc memalign integer overflow → heap corruption, fixed in 2.36-9+deb12u14), CVE-2026-4878 (libcap2 TOCTOU privilege escalation, fixed in 1:2.66-4+deb12u3), CVE-2026-29111 (systemd arbitrary code execution / DoS, fixed in 252.39-1~deb12u2). None reachable in OSCAR's runtime path — OS-level libraries linked by node:22-slim — but Trivy gates the CI pipeline on HIGH/CRITICAL findings, so every PR opened after the Debian security tracker updated was being blocked.",
        "Operator action required: none. Watchtower picks up :stable automatically after promote-release republishes the image."
      ]
    },
    {
      "release": "2026.34",
      "date": "2026-05-16",
      "server": "1.11.6",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(ops): every container in the stack (oscar, loki, promtail, grafana, prometheus, alertmanager, autoheal, watchtower) now defaults to Europe/Paris wall-clock time. Was UTC. Affects log timestamps, audit log entries, 'Sent at' lines in emails, run start/finish times rendered in the dashboard. Storage stays UTC ISO-8601 in the JSON layer (correct, unchanged).",
        "Operator override: set OSCAR_TZ=<IANA zone> in .env (e.g. America/New_York, Asia/Tokyo). Defaults to Europe/Paris because that's where the canonical UIC OSCAR deployment is hosted.",
        "Operator action required: pull the latest OSCAR_Deploy/ on the VPS (git pull) and re-up the stack — docker compose -f docker-compose.yml -f docker-compose.metrics.yml up -d. Existing containers are recreated with the new TZ env var. Watchtower won't auto-pick this up because the change is in the compose YAML, not the image."
      ]
    },
    {
      "release": "2026.33",
      "date": "2026-05-16",
      "server": "1.11.5",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(critical #60 Phase 2 follow-up): three file-reader sites still read OSCAR1-encrypted artifacts/datafiles directly via fs.readFileSync, then handed the ciphertext to JSON.parse. Result: 'Could not parse data file: Unexpected token O, \"OSCAR1...\" is not valid JSON' when a tester re-ran a test config after v1.11.0, or when the dashboard compared two runs. Reported by a Paxone tester after running a test config a second time.",
        "Fixed sites: src/api/routes/runs.js (parallel-mode datafile loader), src/reports/diff.js (Bruno results comparison), src/reports/structureResults.js (Bruno raw results parser). All three now go through utils/at-rest.decryptFromFile() which transparently handles both OSCAR1-encrypted files (post-v1.11.0) and legacy plaintext files (pre-v1.11.0 backfill fall-through).",
        "Operator action required: none. Watchtower picks up :stable automatically. No data migration; the helper transparently handles both file shapes."
      ]
    },
    {
      "release": "2026.32",
      "date": "2026-05-16",
      "server": "1.11.4",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(ux): batch header rows on the dashboard now show the submitter for test_manager + administrator roles, alongside the per-run submitter subtitle shipped in v1.11.3. A test_manager triaging the queue can now see at a glance who in their team launched each batch, without needing to expand it. Within a batch all runs are submitted by the same user; the header pulls submitted_by from the first child."
      ]
    },
    {
      "release": "2026.31",
      "date": "2026-05-15",
      "server": "1.11.3",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(ux): dashboard now shows the submitter email under each run label for test_manager and administrator roles. Closes the gap left by Phase 1 (v1.10.0) — test_managers triaging their company's runs need to know who in the team kicked off each one. Testers see only their own runs so the subtitle is redundant for them.",
        "feat(security): test_manager's delete is now a permanent delete (status DELETED), not a soft delete awaiting admin approval. Follow-on to issue #60 Phase 1: administrators no longer read vendor test data, so the soft-delete → admin-review → permanent-delete flow is stale for company-internal cleanups. Test_managers are the data owners for their company and decide directly. Testers still soft-delete (DELETION_REQUESTED) for the safety net.",
        "feat(ux): delete confirmation modal text + button label adapt to user role. Test_managers get a clear ⚠️ warning that the deletion is permanent and cannot be undone. Testers continue to see the soft-delete language. Administrators see the flagging language.",
        "Operator action required: none. UI change picked up on next page reload. Existing pending DELETION_REQUESTED runs remain in admin's lifecycle queue."
      ]
    },
    {
      "release": "2026.30",
      "date": "2026-05-15",
      "server": "1.11.2",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "docs(security #60): vendor data sovereignty Phase 3 — operational policy. Closes the trilogy. Phase 1 was application-level access control (v1.10.0), Phase 2 at-rest encryption (v1.11.0+.1), Phase 3 is the organisational policy that defines who has root SSH on production, how the four long-lived secrets are managed and rotated, backup encryption posture, the SEV-1 incident playbook, and the procedure when a vendor reports data-leak suspicion.",
        "New file: Documentation/Server_Operations/OSCAR - Security Operations Policy.md. Includes a worked example using this morning's v19 migration outage as a concrete SEV-1 case study with timeline + action items.",
        "Admin Guide § 15.5 updated — Phase 3 marked complete, cross-reference to the policy doc.",
        "Operator action required: none. Docs-only release; OSCAR image unchanged in substance (just the version bump). Watchtower's next pull is a no-op."
      ]
    },
    {
      "release": "2026.29",
      "date": "2026-05-15",
      "server": "1.11.1",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(critical): v19 migration crash on boot, leaving OSCAR in restart-loop. Migration used SELECT rowid + UPDATE WHERE rowid = ?, but Node 22's built-in node:sqlite does not expose rowid as a row property — r.rowid was undefined and binding failed with 'Provided value cannot be bound to SQLite parameter 2'. Switched to the explicit `id` primary key (uniform across all four affected tables). Also added per-row error isolation so a single unbindable row doesn't abort the whole table's migration.",
        "Took down production for ~15 minutes when v1.11.0 :stable rolled out via Watchtower; recovered by pinning to the :edge image tag temporarily. v1.11.1 fixes the migration cleanly. v18 was already applied (additive ALTER TABLE columns survive cleanly across version skews).",
        "Operator action after rollover: docker compose pull oscar && docker compose up -d --force-recreate oscar. The v19 migration will then run successfully, encrypting existing plaintext rows on first boot."
      ]
    },
    {
      "release": "2026.28",
      "date": "2026-05-15",
      "server": "1.11.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(security #60): vendor data sovereignty Phase 2 — at-rest encryption. Sensitive content on disk is now AES-256-GCM encrypted using the existing ENCRYPTION_KEY: HTML reports, JSON results, company datafiles, plus the contents of run_events.message, run_requests.{request_body,request_headers,response_body,response_headers,context}, test_frameworks.config, test_resources.data. Anyone running `cat` on the file or `sqlite3 .dump` on the DB sees the OSCAR1 envelope (file) or 'enc:v1:' prefix + base64 ciphertext (column) — never plaintext.",
        "Backward-compat preserved end-to-end: legacy plaintext files (no OSCAR1 magic header) and legacy plaintext rows (no enc:v1: prefix) remain readable forever — the read helpers detect them and pass through unchanged. The v19 migration backfill is idempotent and can be skipped on small instances if desired.",
        "Schema migration v19 — encrypts existing plaintext rows in run_events, run_requests, test_frameworks, test_resources. Per-table transactions; rollback on failure; logs row counts. Idempotent: rows already prefixed are skipped on subsequent runs.",
        "Threat coverage extended: sysadmin running `sudo cat /opt/OSCAR/.../data/oscar.db | strings` now reveals only structural metadata (status, timestamps, http_status, suite_name) — never log content, HTTP traffic, scenarios, or test framework. Same for artifact + datafile reads. The remaining gap is operator-side: a sysadmin attaching a debugger to the running OSCAR process can extract the in-memory key. That's covered by Phase 3 (operational policy doc, no code).",
        "fix(reports): pre-existing latent bug — the /v1/reports/requests/:id/messages endpoint queried four columns (req_body, req_headers, resp_body, resp_headers) that don't exist in the schema (real names are request_*/response_*). The HTTP message viewer in Report Builder was silently empty. Fixed alongside the encryption work since both touch the same query.",
        "Operator action required after rollover: none. v19 migration runs automatically on first boot; existing artifacts and datafiles remain readable as-is. New writes are encrypted. An optional one-time backfill script for existing artifact files lives in OSCAR_Deploy/scripts/encrypt-existing-artifacts.sh — purely cleanup, not required."
      ]
    },
    {
      "release": "2026.27",
      "date": "2026-05-15",
      "server": "1.10.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(security #60): vendor data sovereignty — Phase 1. Introduces a per-run share-with-certifier toggle (replaces the all-or-nothing company-wide flag), strips the administrator role of all read access to test data (test config, datafile, runs, artifacts, assertions), and closes a static-serve bypass that previously let any anonymous user download an artifact knowing only the run UUID.",
        "BREAKING (admin role): administrators can no longer read /v1/runs (per-run content), /v1/runs/:id, /v1/runs/:id/artifacts, /v1/company/test-framework, /v1/company/datafile, /v1/company/test-resources. The role becomes operations + security only (users, companies metadata, server config, alerts, audit log, observability stack). Aggregate run counts remain visible via /v1/admin/activity. Pending-deletion lifecycle queue remains visible (DELETION_REQUESTED + DELETED_BY_ADMIN status only).",
        "BREAKING (certifier visibility): certifiers no longer see all of a vendor's runs based on the company-wide share toggle. They see ONLY runs the test_manager has explicitly shared via the new 'Share with certifiers' button on each run-detail page. The legacy companies.share_reports_with_certifier toggle becomes a master kill switch — when 0, it overrides every per-run share. v18 migration backfills all terminal runs of currently-shared companies as 'shared' so existing certifier workflows don't break on rollover.",
        "fix(critical): /artifacts/<runId>/<filename> and /data/<filename> were served by express.static with no auth — anyone able to reach OSCAR could download artifacts knowing only a run UUID. Both routes now require authenticated session + per-run-ownership check (artifacts) or company-slug match (datafile). Bruno's loopback fetch of /data continues to work via a no-X-Forwarded-For exception.",
        "Schema migration v18 — runs gains shared_with_certifier_at + shared_with_certifier_by columns; partial index on shared_with_certifier_at; backfill of legacy company-wide-share runs. Idempotent and run-once.",
        "Operator action required after rollover: none for the canonical UIC deployment — every existing certifier-visible run is preserved by the v18 backfill. Per-run sharing becomes the model for all NEW runs; test_managers can revoke or extend access run-by-run via the new UI button. Self-hosters: nothing extra.",
        "Threat model (documented in admin guide § 15): defends against admin-browsing-UI access, anonymous artifact bypass, per-company leakage, and certifier overreach. Does NOT defend against root SSH access reading raw oscar.db on disk (Phase 2 — at-rest DB encryption — separate release)."
      ]
    },
    {
      "release": "2026.26",
      "date": "2026-05-11",
      "server": "1.9.1",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(ux #19): Test Config save confirmation is now a fixed-position toast pinned to the top of the viewport regardless of scroll. Previously the in-flow message rendered at the very top of the page so an admin saving from the bottom of the long Test Config form had no visual confirmation that the save succeeded.",
        "feat(ux #18): dashboard batch summary replaces the single 'X/Y done' pill with three side-by-side counters (✓ passed, ✗ failed, ⌛ running) so failures are immediately visible at a glance instead of being hidden behind a deceptive 'progress' wording.",
        "ops: GitHub Action re-renders the Self-Hosted Quick Start PDF whenever its markdown source changes on main and commits the regenerated file back. Eliminates manual PDF regeneration.",
        "Closes #14 (no code change — root cause was misconfigured SMTP_FROM, hardened in v1.9.0 with field renaming + soft-validation warnings + Send test email button)."
      ]
    },
    {
      "release": "2026.25",
      "date": "2026-05-11",
      "server": "1.9.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(alerting): unified SMTP + alerting config in the admin UI. Server Config tab gains an Alerting card (ALERT_RECIPIENTS, ALERT_REPEAT_CRITICAL, ALERT_REPEAT_WARNING) plus a one-click 'Apply alerting config' button that templates alertmanager.yml from the same SMTP_* values OSCAR uses, writes it to a shared volume, and hot-reloads Alertmanager via /-/reload — no SSH or VPS file edit needed.",
        "feat(smtp-ux): SMTP fields renamed for clarity (SMTP Login vs Display 'From' Address) with inline help text + soft-validation warnings when SMTP_FROM looks like a relay-internal authentication identity (e.g. *@smtp-brevo.com) or duplicates SMTP_USER. Closes the diagnostic gap that produced the SMTP_USER-pasted-into-SMTP_FROM incident.",
        "feat(auth-ux): global fetch interceptor in nav.js detects 401 from any authenticated API call, clears stale localStorage, and redirects to login with a friendly 'Your session has expired' notice. Replaces the v1.8.1 'dead UI after cookie expiry' state with a clean re-auth flow.",
        "Operator action required after rollover: docker compose -f docker-compose.yml -f docker-compose.metrics.yml up -d --force-recreate oscar alertmanager (recreates with the new alertmanager-config shared volume). Then in OSCAR Server Config tab → fill in ALERT_RECIPIENTS → Save → click 'Apply alerting config'. The host-edited /opt/OSCAR/OSCAR_Deploy/alertmanager/alertmanager.yml can be deleted after this — it's no longer used."
      ]
    },
    {
      "release": "2026.24",
      "date": "2026-05-11",
      "server": "1.8.1",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(auth): redirect-loop on welcome.html (and 6 other pages) for users whose session is cookie-only — typically anyone freshly created via the email-verification flow. The auth guards still checked localStorage.oscar_token (the legacy bearer-token model), but the verify-email flow only writes the httpOnly cookie + oscar_user — leaving oscar_token absent and bouncing the page back to / which bounced back to /welcome.html (visible 'blinking' on the welcome banner). Guards now use oscar_user as the session presence proxy; oscar_token is preserved for legacy Bearer-header fetches when present. Affects welcome.html, admin.html, compare.html, dashboard.html, profile.html, run-detail.html, run.html.",
        "Existing administrator sessions were unaffected because they retained oscar_token in localStorage from before the cookie migration."
      ]
    },
    {
      "release": "2026.23",
      "date": "2026-05-10",
      "server": "1.8.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(ops): operational watchdog + email alerting. Docker healthcheck on the OSCAR container probes /health every 30 s; willfarrell/autoheal sidecar restarts any unhealthy container automatically. Prometheus alert rules ship for: oscar down, restart loop, queue stuck, sustained run-failure rate, SMTP degraded, login attack burst, memory leak, event-loop lag. Alertmanager routes firing alerts to admin email via the existing SMTP relay.",
        "feat(ops): two news entries on welcome page — operations monitoring upgrade, and recap of credential redaction + self-service report deletion + password reset by email.",
        "docs: solution architecture (§ 10.1), specification NFRs (§ 5), and admin guide (new §§ 13–14) updated with the full observability + watchdog stack. metrics-and-monitoring.md gets new troubleshooting rows for autoheal + alertmanager.",
        "Operator action required after rollover: (1) cp OSCAR_Deploy/alertmanager/alertmanager.yml.example → alertmanager.yml on the host, fill in SMTP credentials + recipient list. (2) docker compose -f docker-compose.yml -f docker-compose.metrics.yml up -d --force-recreate oscar autoheal alertmanager prometheus. Resource cost rises from ~380 MB to ~415 MB RAM. Full guide in Documentation/Server_Operations/OSCAR - Server Admin Guide.md § 14."
      ]
    },
    {
      "release": "2026.22",
      "date": "2026-05-10",
      "server": "1.7.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(loki): centralised log aggregation via Loki + Promtail (PR #52). Admin Dashboard tile 📝 Logs is now live and deep-links to a pre-built 'OSCAR · Logs' Grafana dashboard with errors-only view, full live tail, per-container filter, and ad-hoc search. All container stdout/stderr is shipped automatically — picks up new containers without config changes.",
        "fix(sso): bake the auth_request Host: localhost + X-Forwarded-Proto fix into source nginx snippet (PR #51) — was applied live on the VPS during v1.6.0 rollout. New deployers no longer hit the 500 Internal Server Error on first /grafana/ visit.",
        "Operator action required after rollover: docker compose up -d loki promtail (and recreate grafana) to start the logs stack. Existing metrics stack unaffected. Resource cost rises from ~270 MB to ~380 MB RAM. Full guide in Documentation/Server_Operations/metrics-and-monitoring.md."
      ]
    },
    {
      "release": "2026.21",
      "date": "2026-05-10",
      "server": "1.6.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(sso): Grafana + Prometheus now SSO'd through OSCAR — admin role required, no separate password. New /v1/auth/sso-check endpoint validates the OSCAR JWT cookie and feeds X-WEBAUTH-USER to Grafana's auth.proxy. Replaces the htpasswd basic-auth setup from v1.5.0/v1.5.1.",
        "feat(metrics): Prometheus web UI exposed at /prometheus/ behind same SSO gate. Useful for raw PromQL queries / scrape-target health.",
        "feat(admin-ui): new 'Admin Dashboard' nav entry (admin-only) → /admin-dashboard.html with Grafana / Prometheus / Loki tiles. Loki tile is a placeholder for the next iteration.",
        "fix(metrics): also bakes post-v1.5.0 fixes into source (GF_SERVER_DOMAIN, datasource UID, nginx proxy_pass trailing-slash) so refresh-collection.sh stops failing on dirty working tree.",
        "fix(security): /v1/auth/sso-check rate-limited 600/5min/IP (CodeQL js/missing-rate-limiting).",
        "Operator action required after rollover: replace the OLD nginx /grafana/ block with the new 3-block snippet (auth_request + /grafana/ + /prometheus/), reload nginx, recreate Grafana to pick up new env vars. Full instructions in Documentation/Server_Operations/metrics-and-monitoring.md."
      ]
    },
    {
      "release": "2026.20",
      "date": "2026-05-10",
      "server": "1.5.1",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "fix(metrics): skip HTTPS-redirect middleware for /metrics. PR #41/v1.5.0 added the endpoint, but PR #7/#23's HTTPS-redirect intercepted Prometheus's docker-internal scrape (400 Bad Request OR 301 → ECONNREFUSED depending on ALLOWED_REDIRECT_HOSTS). Now skipped per-path. Operators do NOT need to add 'oscar' to ALLOWED_REDIRECT_HOSTS — the standard prometheus.yml shipped in v1.5.0 works as-is."
      ]
    },
    {
      "release": "2026.19",
      "date": "2026-05-09",
      "server": "1.5.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "feat(metrics): Prometheus + Grafana integration (opt-in). New /metrics endpoint exposes Node.js process metrics + OSCAR-specific counters (HTTP duration histogram, runs by status, queue depth, active runs, login attempts, SMTP sends).",
        "Optional metrics stack via OSCAR_Deploy/docker-compose.metrics.yml — Prometheus (15s scrape, 15d retention) + Grafana (with auto-provisioned 10-panel OSCAR Overview dashboard). Grafana served at /grafana/ behind nginx basic auth.",
        "Default deployments unaffected — opt-in via the compose overlay. Resource cost when enabled: ~270 MB RAM + ~550 MB disk (15d).",
        "Operator guide: Documentation/Server_Operations/metrics-and-monitoring.md."
      ]
    },
    {
      "release": "2026.18",
      "date": "2026-05-09",
      "server": "1.4.4",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Closes #34 UI gap: dashboard batch header rows now have a select-all checkbox. Click once to select every scenario in the batch (instead of expanding the batch and ticking each scenario individually). Indeterminate gray-dash state when some children are selected. Applies to testers, test_managers, and admins."
      ]
    },
    {
      "release": "2026.17",
      "date": "2026-05-09",
      "server": "1.4.3",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Closes #34: test_manager can now soft-delete any run in their own company (matches the elevated privileges they already had over /v1/company/users and the share_reports_with_certifier toggle). Tenant filter still enforces company boundary — cross-company delete remains impossible. Bulk-admin-action (purge) stays administrator-only."
      ]
    },
    {
      "release": "2026.16",
      "date": "2026-05-09",
      "server": "1.4.2",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Closes #17 third leak path: per-scenario HTML report at /artifacts/<runId>/report_<scenario>.html now redacts sensitive headers and auth-endpoint bodies (Bruno_Collection/library-bruno/reportGenerator.js, mirrors mergeReport.js + structureResults.js redaction).",
        "Migration v17 retroactive scrub of run_requests is also now in place (was rolled into v1.4.1 via re-applied PR #32). Boot logs show '[db] migration v17 — scrubbed credentials from N of M run_requests rows' once.",
        "Old report_*.html files in /artifacts/ are NOT auto-cleaned (file system, not DB). Optional one-time cleanup: 'sudo find /opt/OSCAR/Oscar_Server/data/artifacts -name report_*.html -delete' (forces re-run of affected scenarios)."
      ]
    },
    {
      "release": "2026.15",
      "date": "2026-05-09",
      "server": "1.4.1",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Closes #17 server-side leak: structureResults.js now redacts sensitive headers (Authorization, Ocp-Apim-Subscription-Key, etc.) and auth-endpoint request/response bodies BEFORE storing in run_requests. New runs never accumulate plaintext credentials.",
        "DB migration v17 retroactively scrubs every existing run_requests row using the same redaction logic. Idempotent. Boot logs show '[db] migration v17 — scrubbed credentials from N of M run_requests rows' once.",
        "Deploy automation: refresh-collection.yml now also fires on compatibility.json changes; promote-release.yml SSHes the VPS after :stable push to refresh the host repo. Eliminates the manual 'ssh + git pull' that was needed after every release."
      ]
    },
    {
      "release": "2026.14",
      "date": "2026-05-08",
      "server": "1.4.0",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Closes #15: self-service password reset. Login page gets a 'Forgot password?' link, two new public pages (/forgot-password.html and /reset-password.html), three new public endpoints under /v1/auth/password-reset/*, schema migration v16 (password_reset_tokens table).",
        "Closes #14 diagnostic gap: new admin 'Test SMTP Email' button on Server Config tab — verify SMTP end-to-end with verbatim relay error displayed in the UI, no SSH needed.",
        "Admin escape hatch for #14: 'Reset Link' button on each user row generates a self-service reset URL the admin can deliver out-of-band when SMTP is misconfigured.",
        "UI hardening (#16 follow-up): Token URL, Scope, Requestor Header, Ocp-Apim-Subscription-Key inputs are now type=password (visible only as dots while typing).",
        "esc() helper hardened to escape '\"' and \"'\" in addition to & < > — safe for both text and attribute contexts (CodeQL js/incomplete-html-attribute-sanitization, defense in depth on existing buttons).",
        "Auth rate limiting: password-reset/check-token and /confirm now share a 30 / 15 min / IP limiter (CodeQL js/missing-rate-limiting)."
      ]
    },
    {
      "release": "2026.13",
      "date": "2026-05-08",
      "server": "1.3.4",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Sonar S5146 follow-up: req.url is now validated against a safe-local-path regex before being concatenated into the HTTPS-redirect Location header. Defense in depth on top of the Host: allow-list shipped in 2026.10."
      ]
    },
    {
      "release": "2026.12",
      "date": "2026-05-08",
      "server": "1.3.3",
      "collection": "OTST_V2.0.2",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Closes #16: testers can clear stored API credentials. Server-side me-credentials.js now accepts null/empty to wipe a field; profile UI gets a 🗑 Clear all credentials button.",
        "Closes #17: Bruno mergeReport.js redacts sensitive headers (Authorization, X-Subscription-Key, etc.) and auth-endpoint request/response bodies before serialising into downloadable reports.",
        "Bruno collection bumped to OTST_V2.0.2 to record the redaction change."
      ]
    },
    {
      "release": "2026.11",
      "date": "2026-05-08",
      "server": "1.3.2",
      "collection": "OTST_V2.0.1",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Trivial fix: trimRight → trimEnd (Sonar S7783).",
        "First release cut via the auto-tag-on-merge automation (Layer 2)."
      ]
    },
    {
      "release": "2026.10",
      "date": "2026-05-08",
      "server": "1.3.1",
      "collection": "OTST_V2.0.1",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Open-redirect guard in HTTPS middleware (Sonar S5146) — set ALLOWED_REDIRECT_HOSTS in .env to activate.",
        "Sonar BUG count 8 → 0 (S3403 / S3923 / S2871 mechanical fixes).",
        "Sonar BLOCKER code-smell count 3 → 0 (explicit expect() in auth-middleware tests, S2699)."
      ]
    },
    {
      "release": "2026.09",
      "date": "2026-05-08",
      "server": "1.3.0",
      "collection": "OTST_V2.0.1",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Spawn hardening (Sonar S4721): shell:false on Linux, conditional only on Windows .cmd/.bat shims.",
        "Path-traversal guards on all fs calls taking runId (Sonar S6549): central safeJoinUuid helper + inline UUID-regex guard.",
        "DOM-XSS hardening (Sonar S5247): esc() wrappers on all innerHTML template-literal interpolations.",
        "axios bumped to ^1.15.2 in three Bruno-internal locations (top-level + @usebruno/js + @usebruno/requests) — clears 4 HIGH CVEs (CVE-2026-42033, -42035, -42043, -42264).",
        "express-rate-limit bumped to 8.5.1 (ip-address XSS advisory).",
        "Full SAST suite wired in: CodeQL + SonarCloud + Gitleaks + Dependabot, with branch protection enforcing 7 required green checks before merge.",
        "Privacy toggle and Test Manager user-mgmt features carried over from 2026.08 — first release where they ship under the hardened pipeline."
      ]
    },
    {
      "release": "2026.08",
      "date": "2026-05-05",
      "server": "1.2.0",
      "collection": "OTST_V2.0.1",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Per-company privacy toggle: 'Share test reports with Certifiers' on the API Config page.",
        "Test Manager user management surface (/v1/company/users) — create / edit / delete / reset-password for users in their own company.",
        "DB schema migration v15: companies.share_reports_with_certifier."
      ]
    },
    {
      "release": "2026.07",
      "date": "2026-05-01",
      "server": "1.2.0",
      "collection": "OTST_V2.0.1",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Smoke-test of the auto-deploy pipeline.",
        "Switched Watchtower from containrrr/watchtower (unmaintained) to nickfedor/watchtower active fork.",
        "Adds the version chip to the top banner showing release / server / collection."
      ]
    },
    {
      "release": "2026.04",
      "date": "2026-04-30",
      "server": "1.2.0",
      "collection": "OTST_V2.0.1",
      "min_collection": "OTST_V2.0.0",
      "max_collection": "OTST_V2.0.x",
      "notes": [
        "Multi-stage Dockerfile drops npm from runtime (clears CVE-2026-33671 picomatch).",
        "HTTP message chain viewer.",
        "Fix offerSearchCriteria default injection (no more hardcoded EUR / INDIVIDUAL).",
        "Server config admin panel."
      ]
    }
  ]
}
